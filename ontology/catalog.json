{
  "schemaVersion": 2,
  "disambiguations": {},
  "concepts": {
    "bayes-rule": {
      "aliases": [
        "Bayes theorem",
        "Bayes' theorem"
      ],
      "prerequisiteReasons": {
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: Bayes' rule is the formula that inverts a conditional probability: it converts \"how probable is the evidence given the cause?\" into \"how probable is the cause given the evidence?\" Often we can easily state one direction — a test catches 99% of sick people, P(positive | sick) —",
        "probability": "The stated objective requires Probability: the complete explanation applies it in this mechanism: Bayes' rule is the formula that inverts a conditional probability: it converts \"how probable is the evidence given the cause?\" into \"how probable is the cause given the evidence?\" Often we can easily state one direction — a test catches 99% of sick people, P(positive | sick) —",
        "likelihood": "The stated objective requires Likelihood: the complete explanation applies it in this mechanism: - Prior, P(θ) — the distribution over the possible values of θ before seeing any data. It encodes what we believed (or how common each state is) in advance. - likelihood, P(D | θ) — for each candidate value of θ, how probable the observed data D would be if that value were true."
      }
    },
    "bayes-theorem": {
      "canonicalId": "bayes-rule",
      "prerequisiteReasons": {
        "conditional-probability": "The stated objective requires Conditional Probability: the complete explanation applies it in this mechanism: Bayes' theorem relates two directions of conditional-probability:",
        "probability": "The stated objective requires Probability: the complete explanation applies it in this mechanism: - conditional-probability - probability - likelihood",
        "likelihood": "The stated objective requires Likelihood: the complete explanation applies it in this mechanism: Equating them and dividing by $P(B)$ gives Bayes’ theorem. In inference language, $P(A)$ is the prior, $P(B\\mid A)$ is the likelihood of evidence $B$ under hypothesis $A$, $P(B)$ is the evidence or normalizer, and $P(A\\mid B)$ is the posterior."
      }
    },
    "pgai": {
      "prerequisiteReasons": {
        "pgvector": "Explains the in-database vector storage and similarity search that pgai composes.",
        "pgai-vectorizer": "Explains the managed pipeline that keeps source rows and embeddings synchronized.",
        "llm-from-sql": "Explains the SQL-callable model operations that complete pgai's in-database workflow."
      },
      "relations": [
        {"type": "APPLIES_TO", "target": "retrieval-augmented-generation", "reason": "RAG is a major application assembled from pgai's capabilities, not required background for understanding the suite's composition."}
      ]
    },
    "llm-from-sql": {
      "prerequisiteReasons": {
        "sql": "Explains the query expressions and functions through which the model calls are composed.",
        "embedding": "Explains the vector output returned by SQL-callable embedding functions."
      },
      "relations": [
        {"type": "APPLIES_TO", "target": "retrieval-augmented-generation", "reason": "RAG is one workflow assembled from SQL model calls and retrieval, not required theory for understanding model functions in SQL."}
      ]
    },
    "prefix-caching": {
      "aliases": [
        "prompt caching"
      ],
      "prerequisiteReasons": {
        "kv-cache": "The stated objective requires KV-cache: the complete explanation applies it in this mechanism: A kv-cache reuses keys and values within one sequence as it grows. Prefix caching reuses them across sequences: when many requests begin with the identical token sequence — a shared system prompt, a few-shot preamble, or the frozen history of a chat — that shared prefix produces"
      }
    },
    "prompt-caching": {
      "canonicalId": "prefix-caching",
      "prerequisiteReasons": {
        "kv-cache": "The stated objective—Prompt caching reuses the previously computed key–value state of an identical prompt prefix, reducing repeated prefill work while making prefix stability and cache-lifetime policy part of the request contract.—requires KV-cache as a declared foundation for the article’s central mechanism."
      }
    },
    "empirical-bayes": {
      "relations": [
        {
          "type": "CONTRASTS_WITH",
          "target": "hierarchical-bayes",
          "reason": "Both learn shared prior structure from data, but empirical Bayes plugs in estimated hyperparameters while hierarchical Bayes retains them as uncertain variables in the joint posterior."
        }
      ],
      "prerequisiteReasons": {
        "bayes-rule": "The stated objective requires Bayes' Rule: the complete explanation applies it in this mechanism: Empirical Bayes is a shortcut for choosing the prior in bayes-rule. Recall that the rule combines a prior — a distribution over the unknown quantity before seeing data — with the likelihood of the data to produce the posterior, the updated belief. But the prior itself is usually",
        "likelihood": "The stated objective requires Likelihood: the complete explanation applies it in this mechanism: In bayes-rule the answer is posterior ∝ likelihood × prior: a belief about an unknown quantity θ is the prior P(θ) reweighted by how well each value of θ predicted the observed data D, via the likelihood P(D | θ) — the same expression read as a function of the parameter θ rather"
      }
    },
    "curl-of-gradient-zero": {
      "prerequisiteReasons": {
        "curl": "The stated objective requires Curl (∇×F): the complete explanation applies it in this mechanism: For any scalar function $f$, the curl of its gradient is always the zero vector: $\\nabla \\times (\\nabla f) = 0$. A field that is built as a gradient can never have any local spin — it is irrotational. The reason is that the derivative matrix of a gradient is the hessian, and the",
        "gradient": "The stated objective requires Gradient: the complete explanation applies it in this mechanism: For any scalar function $f$, the curl of its gradient is always the zero vector: $\\nabla \\times (\\nabla f) = 0$. A field that is built as a gradient can never have any local spin — it is irrotational. The reason is that the derivative matrix of a gradient is the hessian, and the",
        "hessian": "The stated objective requires Hessian (H): the complete explanation applies it in this mechanism: For any scalar function $f$, the curl of its gradient is always the zero vector: $\\nabla \\times (\\nabla f) = 0$. A field that is built as a gradient can never have any local spin — it is irrotational. The reason is that the derivative matrix of a gradient is the hessian, and the",
        "partial-derivative": "The stated objective requires Partial derivative: the complete explanation applies it in this mechanism: The concept here is a single identity that holds no matter which scalar function you start from: take any $f$ of several variables, form its gradient $\\nabla f$ (the vector of partial-derivatives, which points uphill), then take the curl of that vector-field. The result is",
        "vector-field": "The stated objective requires Vector Field: the complete explanation applies it in this mechanism: The concept here is a single identity that holds no matter which scalar function you start from: take any $f$ of several variables, form its gradient $\\nabla f$ (the vector of partial-derivatives, which points uphill), then take the curl of that vector-field. The result is"
      },
      "relations": [
        {"type": "USES", "target": "differential-operators", "reason": "The operator-family overview helps place the identity among related vector-calculus rules, but understanding the cancellation only requires the specific curl, gradient, Hessian, partial-derivative, and vector-field mechanisms."}
      ]
    },
    "write-ahead-logging": {
      "prerequisiteReasons": {
        "transaction": "Defines the atomicity and durability promises that WAL implements across a crash.",
        "page-cache": "Explains why changed data can remain only in volatile memory at commit time.",
        "writeback": "Explains deferred page flushing and the force-to-stable-storage operation used for the log at commit."
      },
      "relations": [
        {"type": "USES", "target": "block-layer", "reason": "The sequential-log-versus-scattered-page performance explanation uses the block layer's I/O cost model, but that model is not required to understand WAL's durability and recovery mechanism."}
      ]
    },
    "database-index": {
      "prerequisiteReasons": {
        "binary-search-tree": "Establishes why an ordered branching structure can avoid a linear table scan.",
        "key-value": "Defines the value-to-row-location mapping stored by an index entry.",
        "b-tree": "Explains the shallow, disk-oriented ordered structure used by range-capable indexes."
      },
      "relations": [
        {
          "type": "CONTRASTS_WITH",
          "target": "key-value-store",
          "reason": "An index is a secondary value-to-row-location structure inside a relational database; a key-value store is a primary database architecture with durable, networked, key-only access."
        }
      ]
    },
    "b-tree": {
      "prerequisiteReasons": {
        "binary-search-tree": "Establishes the ordered branching search mechanism that a B-tree generalizes to high fan-out."
      },
      "relations": [
        {"type": "USES", "target": "page", "reason": "A production B-tree sizes a node to a fixed storage page; this substrate detail enriches the fan-out explanation but is not required to understand the search-tree mechanism."},
        {"type": "USES", "target": "block-layer", "reason": "The block layer's per-I/O cost model explains why high fan-out is valuable on storage, but a learner can understand the B-tree mechanism without first learning OS I/O internals."}
      ]
    },
    "softmax": {
      "aliases": [
        "softmax function"
      ],
      "objective": "Turn scores into normalized weights and explain why shifting every score preserves them.",
      "prerequisiteReasons": {
        "exponential-function": "Exponentiating each score produces positive weights.",
        "arithmetic": "Dividing by the sum normalizes the weights.",
        "probability-distribution": "The resulting weights are nonnegative and sum to one."
      }
    },
    "online-softmax": {
      "aliases": [
        "streaming softmax",
        "online softmax normalizer"
      ],
      "objective": "Update a stable softmax normalizer when a new block changes the maximum.",
      "prerequisiteReasons": {
        "softmax": "The streaming recurrence preserves the same softmax denominator."
      },
      "relations": []
    },
    "transformer-attention": {
      "aliases": [
        "scaled dot-product attention"
      ],
      "objective": "Explain how query-key scores select a weighted combination of value vectors.",
      "prerequisiteReasons": {
        "softmax": "Each score row becomes normalized weights over values.",
        "vector-dot-product": "A query-key dot product provides a compatibility score.",
        "matrix-multiplication": "Matrix products compute all scores and weighted outputs efficiently."
      }
    },
    "flash-attention": {
      "aliases": [
        "Flash Attention",
        "FlashAttention"
      ],
      "objective": "Explain why tiling and online normalization avoid storing the full attention matrix.",
      "prerequisiteReasons": {
        "transformer-attention": "Defines the attention computation whose result the tiled algorithm preserves.",
        "online-softmax": "Explains how partial normalizers and weighted outputs combine across tiles."
      }
    },
    "t-sne": {
      "aliases": [
        "t-distributed stochastic neighbor embedding"
      ],
      "prerequisiteReasons": {
        "nearest-neighbor-search": "The stated objective requires Nearest-Neighbor Search: the complete explanation applies it in this mechanism: For a practical use, first choose the observations and their representation, run t-SNE with multiple seeds and parameter settings, and compare the recurring local neighborhoods with the original nearest-neighbor-search results. Treat a stable pattern as a hypothesis to",
        "kl-divergence": "The stated objective requires Kullback–Leibler Divergence: the complete explanation applies it in this mechanism: The method represents neighbor relationships in the input and map spaces as probability distributions. It then minimizes a kl-divergence from the input-neighborhood distribution to the map-neighborhood distribution. Because that penalty weights pairs that are neighbors in the"
      }
    },
    "quantization": {
      "prerequisiteReasons": {
        "arithmetic": "Quantization defines scale selection, code rounding, reconstruction, and the error bound with division, multiplication, subtraction, and absolute-value comparisons."
      }
    },
    "arithmetic-intensity": {
      "prerequisiteReasons": {
        "arithmetic": "Its objective is the operations-per-byte ratio, so counting numerical operations and dividing by transferred bytes are required to calculate or interpret it.",
        "numeric-precision-formats": "A byte-traffic calculation depends on the bytes occupied by each value, and the article compares FP32 and FP16 representations directly."
      }
    },
    "memory-hierarchy": {
      "prerequisiteReasons": {
        "arithmetic": "The objective compares capacity, bandwidth, compute rate, and balance points through counts, multiplication, and ratios.",
        "arithmetic-intensity": "Distinguishing memory-bound from compute-bound work requires comparing the workload's operations-per-byte intensity with the machine-format balance point.",
        "numeric-precision-formats": "The machine balance point and transferred-byte count vary with the representation, and the article's FP64, FP32, and FP16 comparison depends on that distinction."
      }
    },
    "abstract-base-class": {
      "prerequisiteReasons": {
        "object-oriented-inheritance": "The stated objective requires Object-Oriented Inheritance: the complete explanation applies it in this mechanism: Direct subclasses participate in ordinary object-oriented-inheritance and method resolution. ABCMeta.register() can instead mark an unrelated class as a virtual subclass: isinstance and issubclass recognize it, but the ABC is absent from the class's method resolution order and",
        "python-descriptor": "The stated objective requires Python Descriptor: the complete explanation applies it in this mechanism: A class derives from ABC and marks requirements with @abstractmethod. Abstract methods may still contain an implementation that subclasses call through super(). When abstract properties, class methods, or static methods combine decorators, abstractmethod must be the innermost",
        "static-type-checking": "The stated objective requires Static Type Checking: the complete explanation applies it in this mechanism: ABCs provide runtime nominal checks and instantiation constraints; they are not the same as static-type-checking. Static protocols can describe structural behavior without runtime registration, and composition may be simpler when implementations need shared collaborators rather"
      }
    },
    "activation-checkpointing": {
      "prerequisiteReasons": {
        "computation-graph": "The stated objective requires Computation graph: the complete explanation applies it in this mechanism: Backpropagation traverses a computation-graph in reverse and needs intermediate forward values to calculate gradients. Keeping all of them avoids repeated work but consumes memory proportional to the saved activations. Checkpointing chooses a subset to retain in fast storage",
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: Activation checkpointing trades computation for training memory. Instead of retaining every intermediate activation from a neural-network forward pass, it stores selected boundaries and recomputes the missing interior activations when backpropagation reaches that region.",
        "memory-hierarchy": "The stated objective requires Memory Hierarchy: the complete explanation applies it in this mechanism: Backpropagation traverses a computation-graph in reverse and needs intermediate forward values to calculate gradients. Keeping all of them avoids repeated work but consumes memory proportional to the saved activations. Checkpointing chooses a subset to retain in fast storage"
      }
    },
    "active-learning": {
      "prerequisiteReasons": {
        "loss-function": "The stated objective requires Loss function: the complete explanation applies it in this mechanism: Begin with a labeled set $L$, an unlabeled pool $U$, an annotation budget $b$, and a model trained to minimize a loss-function. An acquisition function assigns a score to candidates in $U$. Select a batch, obtain its labels from an oracle such as a human annotator, move those",
        "model-calibration": "The stated objective requires Model Calibration: the complete explanation applies it in this mechanism: This score is derived from the model's probability-distribution, not from the unknown label. It is only meaningful to the extent that scores express useful uncertainty; model-calibration checks whether predicted probabilities agree with observed frequencies but does not by",
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: This score is derived from the model's probability-distribution, not from the unknown label. It is only meaningful to the extent that scores express useful uncertainty; model-calibration checks whether predicted probabilities agree with observed frequencies but does not by"
      }
    },
    "adaptive-layer-normalization": {
      "prerequisiteReasons": {
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: Here $c$ is encoded by a neural-network, and $\\gamma(c)$ and $\\beta(c)$ scale and shift feature channels. A residual variant can also predict a gate $\\alpha(c)$ for the block output.",
        "transformer-attention": "The stated objective requires Transformer attention: the complete explanation applies it in this mechanism: In a diffusion transformer, the timestep and class embedding are global conditions for the denoising-diffusion-probabilistic-model. Applying their modulation inside each transformer-attention block avoids adding condition tokens to the sequence. This can be cheaper than",
        "denoising-diffusion-probabilistic-model": "The stated objective requires Denoising Diffusion Probabilistic Model: the complete explanation applies it in this mechanism: In a diffusion transformer, the timestep and class embedding are global conditions for the denoising-diffusion-probabilistic-model. Applying their modulation inside each transformer-attention block avoids adding condition tokens to the sequence. This can be cheaper than",
        "embedding": "The stated objective requires Embedding: the complete explanation applies it in this mechanism: In a diffusion transformer, the timestep and class embedding are global conditions for the denoising-diffusion-probabilistic-model. Applying their modulation inside each transformer-attention block avoids adding condition tokens to the sequence. This can be cheaper than"
      }
    },
    "address-space-layout": {
      "prerequisiteReasons": {
        "virtual-memory": "The stated objective requires Virtual Memory: the complete explanation applies it in this mechanism: Address space layout is the standard floor plan the kernel imposes on a virtual-memory address space. Recall that virtual-memory hands each running program its own private, contiguous range of fake addresses — but that range is not a featureless expanse the program scatters data",
        "page-table": "The stated objective requires Page Table: the complete explanation applies it in this mechanism: Picture the address space as a tall column of bytes, address 0 at the bottom, the highest address at the top. The layout carves it into named regions (also called segments). Define each one, because each has a distinct purpose and, crucially, a distinct permission — a rule the"
      }
    },
    "agent-memory": {
      "prerequisiteReasons": {
        "retrieval-augmented-generation": "The stated objective requires Retrieval-Augmented Generation: the complete explanation applies it in this mechanism: Agent memory is the machinery by which an AI agent — a language model that takes actions and holds conversations over many turns — retains information it has seen and recalls it later, across turns, tasks, and whole sessions, beyond what fits in a single context-window. The",
        "context-window": "The stated objective requires Context Window: the complete explanation applies it in this mechanism: Agent memory is the machinery by which an AI agent — a language model that takes actions and holds conversations over many turns — retains information it has seen and recalls it later, across turns, tasks, and whole sessions, beyond what fits in a single context-window. The"
      }
    },
    "agent-session-state": {
      "prerequisiteReasons": {
        "agent-memory": "The stated objective requires Agent Memory: the complete explanation applies it in this mechanism: Agent session state is the conversation-scoped record that connects several independent agent runs into one interaction. It preserves the user-visible history and domain facts needed by the next turn, but it does not preserve the previous run's completed-step flags, cached",
        "trace-span": "The stated objective requires Trace Span: the complete explanation applies it in this mechanism: 1. resolve the session identifier and load the session's ordered conversation state; 2. create a fresh run record with no completed steps or cached outputs from the prior turn; 3. combine the permitted session history with the new input under a bounded context policy; 4. execute",
        "transaction": "The stated objective requires Transaction (ACID): the complete explanation applies it in this mechanism: Session storage must also preserve ordering. Two messages for the same session can arrive concurrently; without a sequence number, version check, transaction, or per-session serialization rule, both runs may read the same old history and overwrite one another's updates. The"
      }
    },
    "agentic-rag": {
      "prerequisiteReasons": {
        "retrieval-augmented-generation": "The stated objective requires Retrieval-Augmented Generation: the complete explanation applies it in this mechanism: Agentic retrieval-augmented generation adds control decisions around retrieval-augmented-generation: whether retrieval is needed, which source or query to use, whether evidence is sufficient, and whether to retrieve again or answer.",
        "multi-agent-orchestration": "The stated objective requires Multi-Agent Orchestration: the complete explanation applies it in this mechanism: Self-RAG is one researched instance: a language model learns special reflection tokens that control on-demand retrieval and critique retrieved passages and generations. Other systems implement the controller with prompts, code, or multi-agent-orchestration. Splitting “retriever”",
        "llm-as-a-judge": "The stated objective requires LLM as a Judge: the complete explanation applies it in this mechanism: Each extra decision can improve hard queries but adds latency, cost, nondeterminism, and new failure paths. An external fallback also changes freshness, trust, privacy, and prompt-injection exposure. Evidence assessment by an llm-as-a-judge can be wrong or biased, so it needs"
      }
    },
    "all-gather": {
      "prerequisiteReasons": {
        "gather": "The stated objective requires Gather: the complete explanation applies it in this mechanism: All-gather is gather followed by broadcast: every process contributes its chunk, the chunks are collected into the full buffer, and then every process — not just the root — receives that complete buffer. It is to gather what all-reduce is to reduce: same collection, but the",
        "broadcast": "The stated objective requires Broadcast: the complete explanation applies it in this mechanism: All-gather is gather followed by broadcast: every process contributes its chunk, the chunks are collected into the full buffer, and then every process — not just the root — receives that complete buffer. It is to gather what all-reduce is to reduce: same collection, but the"
      }
    },
    "all-reduce": {
      "prerequisiteReasons": {
        "reduce": "The stated objective requires Reduce: the complete explanation applies it in this mechanism: All-reduce is reduce followed by broadcast: every process contributes a value, the values are combined into one result, and then every process — not just the root — receives that result. Same combine as reduce, but the answer ends up everywhere. It is the workhorse collective of",
        "broadcast": "The stated objective requires Broadcast: the complete explanation applies it in this mechanism: All-reduce is reduce followed by broadcast: every process contributes a value, the values are combined into one result, and then every process — not just the root — receives that result. Same combine as reduce, but the answer ends up everywhere. It is the workhorse collective of",
        "reduction-operation": "The stated objective requires Reduction operation: the complete explanation applies it in this mechanism: 1. The reduce part. Exactly as in reduce, every rank's value is folded by the reduction-operation into a single result — 3 + 5 + 2 + 7 = 17 in the figure (here the operation is sum, but any associative op — max, min, product — works just as well). The 17 is computed, so it"
      }
    },
    "all-to-all": {
      "prerequisiteReasons": {
        "scatter": "The stated objective requires Scatter: the complete explanation applies it in this mechanism: All-to-all is best understood as scatter and gather happening from every process at once:",
        "gather": "The stated objective requires Gather: the complete explanation applies it in this mechanism: All-to-all is best understood as scatter and gather happening from every process at once:",
        "all-gather": "The stated objective requires All-gather: the complete explanation applies it in this mechanism: The contrast with all-gather is the key: all-gather gives everyone the same full collection; all-to-all gives everyone a personalized selection — process j keeps only the j-th piece from each source, not the whole thing. That personalization is why it costs the most: n × n"
      }
    },
    "atomic-operation": {
      "prerequisiteReasons": {
        "cuda-thread-hierarchy": "The stated objective requires CUDA Thread Hierarchy: the complete explanation applies it in this mechanism: An atomic operation performs a read-modify-write on one memory location indivisibly — the hardware guarantees that once a thread starts reading that location, no other thread can touch it until the same thread has written its new value back. (A read-modify-write means: read the"
      }
    },
    "attention-sink": {
      "prerequisiteReasons": {
        "sliding-window-attention": "The stated objective requires Sliding-Window Attention: the complete explanation applies it in this mechanism: When you run sliding-window-attention for very long, streaming generation and evict the oldest tokens as the window slides forward, generation quality can collapse — the model's predictions become garbage almost the moment the very first tokens fall out of the cache. The culprit",
        "softmax": "The stated objective requires Softmax: the complete explanation applies it in this mechanism: When you run sliding-window-attention for very long, streaming generation and evict the oldest tokens as the window slides forward, generation quality can collapse — the model's predictions become garbage almost the moment the very first tokens fall out of the cache. The culprit"
      }
    },
    "autoencoder": {
      "prerequisiteReasons": {
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: An autoencoder is a neural-network trained to reconstruct its input through a constrained intermediate code: an encoder maps input to code, and a decoder maps code back to a reconstruction.",
        "loss-function": "The stated objective requires Loss function: the complete explanation applies it in this mechanism: Let encoder $e\\phi$ produce $z=e\\phi(x)$ and decoder $d\\theta$ produce $\\hat x=d\\theta(z)$. Training minimizes a reconstruction loss-function such as"
      }
    },
    "bank-conflict": {
      "prerequisiteReasons": {
        "shared-memory": "The stated objective requires Shared Memory: the complete explanation applies it in this mechanism: A bank conflict is the one way that shared-memory — the fast, on-chip, block-scoped space that is supposed to be roughly as quick as a register — can silently turn slow. The hardware does not build shared memory as a single monolithic block of storage; it physically splits it",
        "warp": "The stated objective requires Warp: the complete explanation applies it in this mechanism: A bank conflict is the one way that shared-memory — the fast, on-chip, block-scoped space that is supposed to be roughly as quick as a register — can silently turn slow. The hardware does not build shared memory as a single monolithic block of storage; it physically splits it"
      }
    },
    "barrier": {
      "prerequisiteReasons": {
        "collective-operation": "The stated objective requires Collective operation: the complete explanation applies it in this mechanism: Barrier is the collective-operation that synchronizes the group: every process that reaches the barrier blocks until all processes have reached it, and only then is everyone released to continue. It is the one collective that moves no data at all — its entire effect is on"
      }
    },
    "bellman-equation": {
      "prerequisiteReasons": {
        "markov-decision-process": "The stated objective requires Markov Decision Process: the complete explanation applies it in this mechanism: A Bellman equation decomposes the value of a state in a markov-decision-process into one reward now plus the discounted value expected after one transition. The recursion is valid because the Markov state makes the distribution of that next transition depend only on the current",
        "expectation": "The stated objective requires Expectation: the complete explanation applies it in this mechanism: Here $R{t+1}$ is the reward produced by the next transition, $S{t+1}$ is the next state, and $\\gamma$ is the MDP's discount factor. The equation does not assume that one sample transition reveals the value. Its expectation averages over the possible actions and environment"
      }
    },
    "bernoulli-distribution": {
      "prerequisiteReasons": {
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: The Bernoulli distribution is the simplest non-trivial probability-distribution: a single trial with exactly two outcomes — call them success (coded as the number $1$) and failure (coded as $0$). One number $p$, the probability of success, fixes everything; failure then has"
      }
    },
    "beta-distribution": {
      "prerequisiteReasons": {
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: The Beta distribution is a continuous probability-distribution whose values live strictly inside the interval $(0,1)$ — the open stretch between $0$ and $1$, not including the endpoints. Because every number in $(0,1)$ is itself a valid probability, the Beta distribution is a"
      }
    },
    "binary-search": {
      "prerequisiteReasons": {
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: The one step, and why it is allowed to discard half. Pick the middle index of the current range — roughly the average of the low and high indices, using the + and ÷ of arithmetic (and rounding down to land on a whole index). Compare the value at that middle index against the"
      }
    },
    "binary-search-tree": {
      "prerequisiteReasons": {
        "binary-search": "The stated objective requires Binary Search: the complete explanation applies it in this mechanism: A binary search tree (BST) stores ordered data not as a contiguous array but as a branching structure of linked nodes, arranged so that the same halving idea behind binary-search works on the structure itself. Each node holds one key and links to at most two children, a left and"
      }
    },
    "block-layer": {
      "prerequisiteReasons": {
        "vfs": "The stated objective requires Virtual File System: the complete explanation applies it in this mechanism: The block layer is the kernel layer that sits between a filesystem — reached through the vfs — and the driver for an actual storage device. The filesystem decides which pieces of a file are needed; it asks for them in terms of logical blocks, fixed-size numbered chunks of the",
        "device-driver": "The stated objective requires Device Driver: the complete explanation applies it in this mechanism: 1. The program's read enters the kernel and the vfs routes it to the filesystem's read routine, exactly as the vfs node describes. 2. The filesystem consults the file's on-disk layout and maps that byte range to logical block N of the file — suppose, with 4-kilobyte blocks,",
        "dma": "The stated objective requires Direct Memory Access: the complete explanation applies it in this mechanism: When a request finally reaches the front and is dispatched, the driver does the hardware- specific work: it programs the device to move the requested sectors directly into the waiting memory (this direct memory transfer, set up by the driver and carried out by the device's own",
        "interrupt": "The stated objective requires Interrupt: the complete explanation applies it in this mechanism: 1. The program's read enters the kernel and the vfs routes it to the filesystem's read routine, exactly as the vfs node describes. 2. The filesystem consults the file's on-disk layout and maps that byte range to logical block N of the file — suppose, with 4-kilobyte blocks,",
        "queue": "The stated objective requires Queue: the complete explanation applies it in this mechanism: Each block device has its request queue — a list, held in memory, of the read and write operations currently waiting to go to that device. When the filesystem needs a block, the block layer does not rush it straight to the driver. It builds a request describing the sector range"
      }
    },
    "bm25": {
      "prerequisiteReasons": {
        "inverted-index": "The stated objective requires Inverted Index: the complete explanation applies it in this mechanism: Here $f(q,d)$ is the term frequency from the inverted-index, $|d|$ is document length, $L$ is average length, $k10$ controls frequency saturation, and $0\\le b\\le1$ controls length normalization. IDF is larger when few documents contain $q$; it can be derived from the probability",
        "probability": "The stated objective requires Probability: the complete explanation applies it in this mechanism: Here $f(q,d)$ is the term frequency from the inverted-index, $|d|$ is document length, $L$ is average length, $k10$ controls frequency saturation, and $0\\le b\\le1$ controls length normalization. IDF is larger when few documents contain $q$; it can be derived from the probability"
      }
    },
    "boot-process": {
      "prerequisiteReasons": {
        "kernel": "The stated objective requires Kernel: the complete explanation applies it in this mechanism: The boot process is the chain of steps that carries a machine from the instant power is applied to a fully running system. Its defining shape is a relay race: a sequence of separate programs, each of which does just enough to load and hand control to the next, then is discarded"
      }
    },
    "broadcast": {
      "prerequisiteReasons": {
        "collective-operation": "The stated objective requires Collective operation: the complete explanation applies it in this mechanism: Broadcast is the collective-operation in which one process — the root-process — holds a piece of data and, after the call, every process in the group holds an identical copy of it. It is the simplest collective: pure one → all movement, with no computation. Data flows out from a",
        "root-process": "The stated objective requires Root process: the complete explanation applies it in this mechanism: Broadcast is the collective-operation in which one process — the root-process — holds a piece of data and, after the call, every process in the group holds an identical copy of it. It is the simplest collective: pure one → all movement, with no computation. Data flows out from a"
      }
    },
    "brownian-motion": {
      "prerequisiteReasons": {
        "stochastic-process": "The stated objective requires Stochastic Process: the complete explanation applies it in this mechanism: Brownian motion $Wt$ is a continuous-path stochastic-process starting at zero, with independent increments and $W{t+h}-Wt$ distributed normally with mean $0$ and variance $h$.",
        "normal-distribution": "The stated objective requires Normal Distribution: the complete explanation applies it in this mechanism: where $N(0,h)$ is a normal-distribution with standard deviation $\\sqrt h$. The square-root scaling makes fluctuations shrink more slowly than elapsed time, so Brownian paths are continuous but not smooth enough to possess an ordinary derivative."
      }
    },
    "bytecode-vm": {
      "prerequisiteReasons": {
        "interpreter": "The stated objective requires Interpreter: the complete explanation applies it in this mechanism: A bytecode virtual machine (VM) is a two-stage way to run a program that sits between a slow tree-walking interpreter and a full native compiler. First a compiler translates the source once into bytecode — a made-up, compact instruction set with small fixed-format operations (an",
        "stack": "The stated objective requires Stack: the complete explanation applies it in this mechanism: A bytecode virtual machine (VM) is a two-stage way to run a program that sits between a slow tree-walking interpreter and a full native compiler. First a compiler translates the source once into bytecode — a made-up, compact instruction set with small fixed-format operations (an"
      }
    },
    "capabilities": {
      "prerequisiteReasons": {
        "process": "The stated objective requires Process: the complete explanation applies it in this mechanism: Capabilities are how Linux breaks the historically all-or-nothing power of the superuser — the special user, named root and numbered user id 0, that the kernel traditionally let do anything — into roughly forty independent privilege bits, each one guarding a single class of"
      }
    },
    "categorical-data-encoding": {
      "prerequisiteReasons": {
        "tensor": "The stated objective requires Tensor: the complete explanation applies it in this mechanism: Categorical data encoding turns a discrete value such as bronze, silver, or gold into a numeric tensor that a model can consume. The representation is part of the model contract: it must be fitted on training data and applied identically at inference.",
        "temporal-data-leakage": "The stated objective requires Temporal Data Leakage: the complete explanation applies it in this mechanism: An encoder is fitted on the training partition, which fixes its vocabulary, category order, and handling of unseen values. Fitting it before a split lets held-out categories influence the representation and is a form of temporal-data-leakage when the split simulates a decision"
      }
    },
    "causal-structure-learning": {
      "prerequisiteReasons": {
        "conditional-independence": "The stated objective requires Conditional Independence: the complete explanation applies it in this mechanism: 1. Constraint-based learning asks which conditional-independence statements the data support and removes or orients edges to match them. 2. Score-based learning assigns each candidate DAG a score balancing fit to the observations against excessive complexity, then searches for a",
        "directed-acyclic-graph": "The stated objective requires Directed Acyclic Graph: the complete explanation applies it in this mechanism: Represent measured variables as vertices in a directed-acyclic-graph. An arrow $X\\to Y$ says that $X$ is a direct causal parent of $Y$ relative to the included variables. A path through intermediate vertices represents an indirect relation, so the goal is not to connect every"
      }
    },
    "cgroup": {
      "prerequisiteReasons": {
        "process": "The stated objective requires Process: the complete explanation applies it in this mechanism: A cgroup (control group) is a kernel feature that limits, accounts for, and prioritizes the resource usage of a group of processes — how much CPU time they may consume, how much memory they may hold, how much disk bandwidth they may use, even how many processes they may create."
      }
    },
    "chain-of-thought": {
      "prerequisiteReasons": {
        "transformer-attention": "The stated objective requires Transformer attention: the complete explanation applies it in this mechanism: Chain-of-thought (CoT) is the practice of having a language model write out its intermediate reasoning steps as tokens before it commits to a final answer, rather than emitting the answer directly. A token here is one discrete unit of text the model reads and produces (a word or"
      }
    },
    "change-of-variables": {
      "prerequisiteReasons": {
        "cumulative-distribution-function": "The stated objective requires Cumulative Distribution Function: the complete explanation applies it in this mechanism: You have a random variable $X$ with a known density, and you build a new variable $Y$ by feeding $X$ through a function, $Y = g(X)$ — for instance squaring it, taking its logarithm, or scaling it. Change of variables answers: what is the density of $Y$? The naive guess \"just",
        "derivative": "The stated objective requires Derivative: the complete explanation applies it in this mechanism: You have a random variable $X$ with a known density, and you build a new variable $Y$ by feeding $X$ through a function, $Y = g(X)$ — for instance squaring it, taking its logarithm, or scaling it. Change of variables answers: what is the density of $Y$? The naive guess \"just",
        "random-variable": "The stated objective requires Random Variable: the complete explanation applies it in this mechanism: We start with a continuous random-variable $X$ — a quantity whose value is uncertain — whose probability is described by a density $fX$, where probability is area under the curve $fX$ and $fX(x)$ measures probability per unit length near $x$. We also have its",
        "differential": "The stated objective requires Differential (Total Derivative): the complete explanation applies it in this mechanism: That the relocation conserves the total is not a separate fact to be checked but an automatic consequence of the stretch factor — and this is where the jacobian (the local stretch $|g'|$) reveals its true job. Write the substitution that pairs the axes: $y = \\ln x$, whose",
        "jacobian": "The stated objective requires Jacobian (J): the complete explanation applies it in this mechanism: That the relocation conserves the total is not a separate fact to be checked but an automatic consequence of the stretch factor — and this is where the jacobian (the local stretch $|g'|$) reveals its true job. Write the substitution that pairs the axes: $y = \\ln x$, whose"
      }
    },
    "chunked-prefill": {
      "prerequisiteReasons": {
        "kv-cache": "The stated objective requires KV-cache: the complete explanation applies it in this mechanism: LLM inference has two phases. Prefill reads the whole prompt in one shot to fill the kv-cache; its cost grows with prompt length, so a long prompt is a single large compute burst. Decode then emits one token per step, each step cheap. The problem: when many requests share a",
        "transformer-attention": "The stated objective requires Transformer attention: the complete explanation applies it in this mechanism: LLM inference has two phases. Prefill reads the whole prompt in one shot to fill the kv-cache; its cost grows with prompt length, so a long prompt is a single large compute burst. Decode then emits one token per step, each step cheap. The problem: when many requests share a"
      }
    },
    "class-weighted-loss": {
      "prerequisiteReasons": {
        "loss-function": "The stated objective requires Loss function: the complete explanation applies it in this mechanism: Start from the logarithmic loss-function for binary outcomes. For a fixed input, define:",
        "derivative": "The stated objective requires Derivative: the complete explanation applies it in this mechanism: Its derivative is $-ap/q+b(1-p)/(1-q)$. Setting it to zero and collecting terms gives",
        "model-calibration": "The stated objective requires Model Calibration: the complete explanation applies it in this mechanism: Take $p=0.01$, $a=99$, and $b=1$. Then $ap=0.99$ and $b(1-p)=0.99$, giving $q^=0.5$. A score of 0.5 can therefore correspond to only 1% positives. Calling it a 50% probability would violate model-calibration. Under precisely these weighting assumptions, the inverse relationship",
        "precision-recall": "The stated objective requires Precision and Recall: the complete explanation applies it in this mechanism: Use weights to express a justified training objective and compare with unweighted learning. Select the deployed threshold separately using precision-recall and operational costs. Evaluate on data with representative class proportions, rather than assuming a balanced test sample"
      }
    },
    "clone": {
      "prerequisiteReasons": {
        "fork-exec": "The stated objective requires Fork and Exec: the complete explanation applies it in this mechanism: clone() is the single Linux system call that creates a new task — and the surprising claim of this node is that it is the only one. The everyday distinctions you know — \"making a process,\" \"starting a thread,\" \"launching a container\" — are not three separate kernel mechanisms",
        "thread": "The stated objective requires Thread: the complete explanation applies it in this mechanism: clone() is the single Linux system call that creates a new task — and the surprising claim of this node is that it is the only one. The everyday distinctions you know — \"making a process,\" \"starting a thread,\" \"launching a container\" — are not three separate kernel mechanisms",
        "system-call": "The stated objective requires System Call: the complete explanation applies it in this mechanism: clone() is the system-call that creates a new task by making a new taskstruct. By itself that is unremarkable — what makes it the universal primitive is its argument: a set of flags. A flag is a single switch, named in the source with the prefix CLONE, and each switch governs",
        "virtual-memory": "The stated objective requires Virtual Memory: the complete explanation applies it in this mechanism: The two flags that matter most are the one governing memory (in the source, CLONEVM — \"VM\" for the task's virtual-memory, its address space) and the one governing the open file-descriptors (CLONEFILES). Each, set on, means SHARE that resource; left off, the new task gets its own",
        "file-descriptor": "The stated objective requires File Descriptor: the complete explanation applies it in this mechanism: The two flags that matter most are the one governing memory (in the source, CLONEVM — \"VM\" for the task's virtual-memory, its address space) and the one governing the open file-descriptors (CLONEFILES). Each, set on, means SHARE that resource; left off, the new task gets its own",
        "namespace": "The stated objective requires Namespace: the complete explanation applies it in this mechanism: Setting 3 — give the task its own isolated views: the first task of a container. Beyond sharing-or-copying memory and files, clone() offers the NEW-family flags (the source names CLONENEWPID, CLONENEWNET, CLONENEWNS, and others). Each of these gives the new task its OWN COPY of"
      }
    },
    "closure": {
      "prerequisiteReasons": {
        "first-class-function": "The stated objective requires First-Class Function: the complete explanation applies it in this mechanism: A closure is what you get when a language combines two features: functions that are values (first-class-function) and names that are resolved by where they are written (lexical-scoping). A closure is a function packaged together with a snapshot of the surrounding variables it",
        "lexical-scoping": "The stated objective requires Lexical Scoping: the complete explanation applies it in this mechanism: A closure is what you get when a language combines two features: functions that are values (first-class-function) and names that are resolved by where they are written (lexical-scoping). A closure is a function packaged together with a snapshot of the surrounding variables it"
      }
    },
    "collective-algorithm": {
      "prerequisiteReasons": {
        "collective-operation": "The stated objective requires Collective operation: the complete explanation applies it in this mechanism: A single collective-operation — say, \"broadcast this array from one process to all the others\" — names what must happen to the data, but it does not say how the underlying one-to-one messages are wired together to make it happen. A collective algorithm is one such wiring: a"
      }
    },
    "collective-operation": {
      "prerequisiteReasons": {
        "communicator": "The stated objective requires Communicator: the complete explanation applies it in this mechanism: Plain message-passing is point-to-point: one process names another and they exchange a message. A collective operation lifts that from a pair to a whole communicator — the named group of processes and their ranks. Three things define it:",
        "message-passing": "The stated objective requires Message passing: the complete explanation applies it in this mechanism: A collective operation is a single communication step that the whole group of processes participates in together — every process in the group calls it, and the operation is only complete once all of them have. Instead of one process talking to one other, data is moved and/or"
      }
    },
    "column-pruning": {
      "prerequisiteReasons": {
        "query-planning": "The stated objective requires Query Planning & Optimization: the complete explanation applies it in this mechanism: The query-planning stage derives required columns from output projections, filters, joins, grouping, sorting, and expressions. A column used only by a filter may be required at the scan but discarded afterward. SELECT , opaque user-defined functions, schema-dependent code, or a",
        "memory-hierarchy": "The stated objective requires Memory Hierarchy: the complete explanation applies it in this mechanism: Verify with an execution plan and storage metrics: projected schema, files and row groups touched, bytes read, bytes decoded, peak memory, transfer volume, and elapsed time. Compare identical results and warm/cold cache states. A library benchmark does not establish that one"
      }
    },
    "communicator": {
      "prerequisiteReasons": {
        "message-passing": "The stated objective requires Message passing: the complete explanation applies it in this mechanism: Plain message-passing lets one process send a message to another, but to name \"another\" you need a stable way to refer to processes and to know which ones form a group. A communicator supplies exactly that:"
      }
    },
    "computation-graph": {
      "prerequisiteReasons": {
        "graph": "The stated objective requires Graph (Adjacency List): the complete explanation applies it in this mechanism: Start with a graph: vertices are things and directed edges say which thing feeds which next thing. In a computation graph, each vertex invokes an operation, while the edges carry named tensor values. Graph inputs enter without a producing vertex; graph outputs leave after their",
        "tensor": "The stated objective requires Tensor: the complete explanation applies it in this mechanism: Start with a graph: vertices are things and directed edges say which thing feeds which next thing. In a computation graph, each vertex invokes an operation, while the edges carry named tensor values. Graph inputs enter without a producing vertex; graph outputs leave after their"
      }
    },
    "compute-capability": {
      "prerequisiteReasons": {
        "cuda-kernel": "The stated objective requires CUDA Kernel: the complete explanation applies it in this mechanism: Compute capability is the version number of an NVIDIA GPU's architecture — a number NVIDIA assigns to each generation of its hardware design, written as a major.minor pair (8.0, 9.0, 12.0) or, in the compiler's spelling, sm80, sm90, sm120. That number is a contract: it says",
        "streaming-multiprocessor": "The stated objective requires Streaming Multiprocessor: the complete explanation applies it in this mechanism: What the concept is. A GPU is not one fixed design that NVIDIA has shipped unchanged for twenty years; it is a line of designs, one new architecture per generation, each with a codename — Volta, then Ampere, then Hopper, then Blackwell. Compute capability is the formal version"
      }
    },
    "concept-drift": {
      "prerequisiteReasons": {
        "conditional-probability": "The stated objective requires Conditional Probability: the complete explanation applies it in this mechanism: Concept drift occurs when the predictive relationship changes over time—formally, when the conditional-probability of the target given the input is no longer the one learned from historical data.",
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: for relevant times $t1$ and $t2$. This is distinct from a change only in $Pt(X)$, the input probability-distribution. Input drift can alter what cases arrive without changing the correct label for a given case; concept drift changes that correct relationship itself. Both can"
      }
    },
    "concurrency-control": {
      "prerequisiteReasons": {
        "transaction": "The stated objective requires Transaction (ACID): the complete explanation applies it in this mechanism: Concurrency control is the set of mechanisms a database uses to run many transactions at the same time while guaranteeing the outcome is the same as if they had run one after another in some order — a property called serializability. The prerequisite atomic-operation node solved",
        "atomic-operation": "The stated objective requires Atomic Operation: the complete explanation applies it in this mechanism: Concurrency control is the set of mechanisms a database uses to run many transactions at the same time while guaranteeing the outcome is the same as if they had run one after another in some order — a property called serializability. The prerequisite atomic-operation node solved"
      }
    },
    "conditional-independence": {
      "prerequisiteReasons": {
        "conditional-probability": "The stated objective requires Conditional Probability: the complete explanation applies it in this mechanism: This factorization uses conditional-probability. An equivalent form, whenever its terms are defined, is"
      }
    },
    "conditional-probability": {
      "prerequisiteReasons": {
        "probability": "The stated objective requires Probability: the complete explanation applies it in this mechanism: Conditional probability asks how likely event $A$ is after learning that event $B$ occurred. It uses probability inside the reduced world selected by $B$ rather than across all outcomes."
      }
    },
    "confusion-matrix": {
      "prerequisiteReasons": {
        "set": "The stated objective requires Set (membership): the complete explanation applies it in this mechanism: For a set of classes $\\{0,\\ldots,K-1\\}$,",
        "measurement": "The stated objective requires Measurement: the complete explanation applies it in this mechanism: Raw counts preserve prevalence and sample size. Row normalization estimates the distribution of predictions given each true class; column normalization estimates the distribution of true classes given each prediction. Normalizing the whole table gives population proportions."
      }
    },
    "conjugate-prior": {
      "prerequisiteReasons": {
        "bayes-rule": "The stated objective requires Bayes' Rule: the complete explanation applies it in this mechanism: A conjugate prior is a prior distribution chosen so that, after the bayes-rule update, the posterior lands in the same family of distributions as the prior — only its parameters have changed. When this happens, Bayesian updating stops being a calculus problem and becomes simple",
        "beta-distribution": "The stated objective requires Beta Distribution: the complete explanation applies it in this mechanism: A conjugate prior is a prior distribution chosen so that, after the bayes-rule update, the posterior lands in the same family of distributions as the prior — only its parameters have changed. When this happens, Bayesian updating stops being a calculus problem and becomes simple",
        "bernoulli-distribution": "The stated objective requires Bernoulli Distribution: the complete explanation applies it in this mechanism: A conjugate prior is a prior distribution chosen so that, after the bayes-rule update, the posterior lands in the same family of distributions as the prior — only its parameters have changed. When this happens, Bayesian updating stops being a calculus problem and becomes simple",
        "likelihood": "The stated objective requires Likelihood: the complete explanation applies it in this mechanism: Recall the inference setup from bayes-rule. We have an unknown quantity $\\theta$ we want to learn, a prior $P(\\theta)$ encoding belief before data, a likelihood $P(D\\mid\\theta)$ saying how probable the observed data $D$ is for each candidate $\\theta$, and the posterior"
      }
    },
    "container": {
      "prerequisiteReasons": {
        "namespace": "The stated objective requires Namespace: the complete explanation applies it in this mechanism: A container is not a special kind of machine and not a tiny virtual computer. It is an ordinary process — a running program the kernel schedules on the same CPUs as everything else — that the kernel has wrapped in a combination of three features that already existed on their",
        "cgroup": "The stated objective requires Cgroup: the complete explanation applies it in this mechanism: A container is not a special kind of machine and not a tiny virtual computer. It is an ordinary process — a running program the kernel schedules on the same CPUs as everything else — that the kernel has wrapped in a combination of three features that already existed on their",
        "vfs": "The stated objective requires Virtual File System: the complete explanation applies it in this mechanism: A container is not a special kind of machine and not a tiny virtual computer. It is an ordinary process — a running program the kernel schedules on the same CPUs as everything else — that the kernel has wrapped in a combination of three features that already existed on their",
        "system-call": "The stated objective requires System Call: the complete explanation applies it in this mechanism: A container makes the opposite trade. It does not run a second kernel at all — the contained process is scheduled by the host's one kernel, on the host's CPUs, through the host's system-calls, the same as every other process. The three mechanisms above only change what that one",
        "overlayfs": "The stated objective requires OverlayFS: the complete explanation applies it in this mechanism: Third, a private root filesystem — built on the vfs. A process needs files: the program to run, its libraries, its configuration. We want each container to see its own root directory — its own /bin, /etc, /lib — without physically copying those files for every container, which"
      }
    },
    "container-networking": {
      "prerequisiteReasons": {
        "namespace": "The stated objective requires Namespace: the complete explanation applies it in this mechanism: Container networking gives each container — a regular process the system runs in isolation — its own private network world, and then wires that world out to everything else. The private world comes from a network namespace: a kind of namespace that hands the container its own",
        "socket": "The stated objective requires Socket: the complete explanation applies it in this mechanism: Container networking gives each container — a regular process the system runs in isolation — its own private network world, and then wires that world out to everything else. The private world comes from a network namespace: a kind of namespace that hands the container its own",
        "container": "The stated objective requires Container: the complete explanation applies it in this mechanism: A container, recall from namespace, is not a new kernel object: it is a name for an ordinary process placed inside fresh namespaces so it sees private instances of the machine's global resources. Container networking is the networking slice of that picture — specifically, the",
        "network-stack": "The stated objective requires Network Stack: the complete explanation applies it in this mechanism: Among the namespace types is the network namespace, which isolates the network-stack: the interfaces (the network \"cards,\" real or virtual, that a machine can send and receive through), the addresses assigned to them, the routing table, and the space of port numbers. When a"
      }
    },
    "container-runtime": {
      "prerequisiteReasons": {
        "container": "The stated objective requires Container: the complete explanation applies it in this mechanism: A container runtime is the software stack that actually creates and runs a container — the tooling that does the concrete work of assembling, around one ordinary process, the kernel features a container is made of. A container itself is not a kernel object; it is a regular",
        "capabilities": "The stated objective requires Capabilities: the complete explanation applies it in this mechanism: 1. Create fresh isolation. It launches a new process into brand-new namespaces of each kind at once — its own process-number space (so it will be number 1 inside), its own network, its own set of mounts, its own hostname space. This is the isolated view half of a container. 2.",
        "namespace": "The stated objective requires Namespace: the complete explanation applies it in this mechanism: 1. Create fresh isolation. It launches a new process into brand-new namespaces of each kind at once — its own process-number space (so it will be number 1 inside), its own network, its own set of mounts, its own hostname space. This is the isolated view half of a container. 2.",
        "cgroup": "The stated objective requires Cgroup: the complete explanation applies it in this mechanism: 1. Create fresh isolation. It launches a new process into brand-new namespaces of each kind at once — its own process-number space (so it will be number 1 inside), its own network, its own set of mounts, its own hostname space. This is the isolated view half of a container. 2.",
        "seccomp": "The stated objective requires seccomp: the complete explanation applies it in this mechanism: 1. Create fresh isolation. It launches a new process into brand-new namespaces of each kind at once — its own process-number space (so it will be number 1 inside), its own network, its own set of mounts, its own hostname space. This is the isolated view half of a container. 2.",
        "system-call": "The stated objective requires System Call: the complete explanation applies it in this mechanism: A container runtime is the software stack that actually creates and runs a container — the tooling that does the concrete work of assembling, around one ordinary process, the kernel features a container is made of. A container itself is not a kernel object; it is a regular"
      }
    },
    "content-addressable-memory": {
      "prerequisiteReasons": {
        "key-value": "The stated objective requires Key–Value Mapping: the complete explanation applies it in this mechanism: Content-addressable memory (CAM) — also called associative memory — is a kind of memory hardware that you query by content instead of by address. With ordinary memory (RAM) you supply an address and it returns the data stored there; a CAM does the opposite: you supply a data"
      }
    },
    "context-parallelism": {
      "prerequisiteReasons": {
        "ring-attention": "The stated objective requires Ring Attention: the complete explanation applies it in this mechanism: Context parallelism splits one single training sample along its sequence (token) dimension across $D$ devices, so a sequence too long to fit on any one device can still be processed. Each device permanently holds a contiguous block of the sample's tokens — their activations and",
        "all-reduce": "The stated objective requires All-reduce: the complete explanation applies it in this mechanism: Context parallelism splits one single training sample along its sequence (token) dimension across $D$ devices, so a sequence too long to fit on any one device can still be processed. Each device permanently holds a contiguous block of the sample's tokens — their activations and",
        "gather": "The stated objective requires Gather: the complete explanation applies it in this mechanism: Why this is not trivial — attention is global. Most of a transformer's per-token work (the feed-forward layers, layer norms, the elementwise parts) is local: token $t$'s activation only depends on token $t$, so device $i$ can compute it on its own block with no communication.",
        "online-softmax": "The stated objective requires Online Softmax: the complete explanation applies it in this mechanism: The enabling mechanism: cross-device attention IS ring-attention. This is exactly the problem ring-attention was built for, and context parallelism uses it verbatim as its attention layer. Recall the ring-attention schedule: queries stay home (device $i$ never moves $Qi$); the"
      }
    },
    "context-propagation": {
      "prerequisiteReasons": {
        "telemetry-context": "The stated objective requires Telemetry Context: the complete explanation applies it in this mechanism: Context propagation moves correlation identity across a process or service boundary. A sender serializes selected fields from its telemetry-context into a transport carrier; a receiver extracts those fields and derives its own local context before doing work.",
        "http": "The stated objective requires HTTP: the complete explanation applies it in this mechanism: The carrier is whatever the transport can convey alongside the message. For http, it is normally request headers. OpenTelemetry's default W3C Trace Context representation uses a traceparent header containing a version, trace ID, parent span ID, and flags. Injection writes the"
      }
    },
    "context-switch": {
      "prerequisiteReasons": {
        "process": "The stated objective requires Process: the complete explanation applies it in this mechanism: A context switch is the maneuver by which one CPU runs more processes than it has cores: the kernel pauses whichever process is currently executing, saves everything that process needs in order to resume later, then loads a different process's saved state and lets it run",
        "interrupt": "The stated objective requires Interrupt: the complete explanation applies it in this mechanism: - The timer interrupt (time-slicing). The hardware is configured to fire an interrupt at a fixed cadence — say every few milliseconds. An interrupt forcibly diverts the CPU into the kernel. On each such tick the kernel gets a chance to ask \"should someone else run now?\" If a"
      }
    },
    "context-window": {
      "prerequisiteReasons": {
        "transformer-attention": "The stated objective requires Transformer attention: the complete explanation applies it in this mechanism: The context window is the largest number of tokens — the small chunks of text (words or word-pieces) a language model reads as its atomic units — that the model can take as input and process together in a single turn. It is a hard ceiling: everything the model is allowed to"
      }
    },
    "continuous-batching": {
      "prerequisiteReasons": {
        "kv-cache": "The stated objective requires KV-cache: the complete explanation applies it in this mechanism: When a language model serves many requests at once, the cheap way to feed the GPU is to run a batch — several sequences advancing together so one big matrix multiply does the work of many small ones. The naive scheme, static batching, locks the batch's membership for the whole",
        "queue": "The stated objective requires Queue: the complete explanation applies it in this mechanism: When a language model serves many requests at once, the cheap way to feed the GPU is to run a batch — several sequences advancing together so one big matrix multiply does the work of many small ones. The naive scheme, static batching, locks the batch's membership for the whole"
      }
    },
    "convex-transformation-order": {
      "prerequisiteReasons": {
        "convexity": "The stated objective requires Convexity: the complete explanation applies it in this mechanism: Van Zwet's convex transformation order is a rigorous, strong notion of skewness that — unlike the moment measure or the nonparametric (mean-minus-median) measure, which are only rules of thumb and can let the three centers scramble — pins down the three-center order for good.",
        "skewness": "The stated objective requires Skewness: the complete explanation applies it in this mechanism: Van Zwet's convex transformation order is a rigorous, strong notion of skewness that — unlike the moment measure or the nonparametric (mean-minus-median) measure, which are only rules of thumb and can let the three centers scramble — pins down the three-center order for good."
      }
    },
    "convexity": {
      "prerequisiteReasons": {
        "derivative": "The stated objective requires Derivative: the complete explanation applies it in this mechanism: A tangent line at an input $x0$ is the straight line that touches the graph at $(x0, f(x0))$ and has the same slope there — and that slope is precisely the derivative $f'(x0)$, the instantaneous rate of change of $f$ at $x0$. The tangent line's height at any input $x$ is"
      }
    },
    "copy-on-write": {
      "prerequisiteReasons": {
        "demand-paging": "The stated objective requires Demand Paging: the complete explanation applies it in this mechanism: Copy-on-write (COW) is the trick that makes fork-exec's fork() instant and memory-cheap. fork() is supposed to give the child a private copy of all the parent's memory — but physically duplicating it would mean copying every byte the parent holds, possibly gigabytes, before the",
        "fork-exec": "The stated objective requires Fork and Exec: the complete explanation applies it in this mechanism: Copy-on-write (COW) is the trick that makes fork-exec's fork() instant and memory-cheap. fork() is supposed to give the child a private copy of all the parent's memory — but physically duplicating it would mean copying every byte the parent holds, possibly gigabytes, before the",
        "page-table": "The stated objective requires Page Table: the complete explanation applies it in this mechanism: First, the substrate, in plain terms. A process does not address physical memory directly. Its memory is divided into fixed-size chunks — pages, typically 4 KB each — and the kernel keeps a per-process page-table: a lookup that, for each page of the process's view of memory,",
        "page-fault": "The stated objective requires Page Fault: the complete explanation applies it in this mechanism: First, the substrate, in plain terms. A process does not address physical memory directly. Its memory is divided into fixed-size chunks — pages, typically 4 KB each — and the kernel keeps a per-process page-table: a lookup that, for each page of the process's view of memory,"
      }
    },
    "coroutine": {
      "prerequisiteReasons": {
        "stack": "The stated objective requires Stack: the complete explanation applies it in this mechanism: An ordinary function runs from start to finish on the shared call stack: it is called, it runs, it returns, and when it returns its frame and all its locals are gone. A coroutine relaxes this. It has its own private call stack, and it can yield — suspend itself partway through,"
      }
    },
    "cosine-similarity": {
      "prerequisiteReasons": {
        "vector-dot-product": "The stated objective requires Vector dot product: the complete explanation applies it in this mechanism: Cosine similarity measures how aligned two vectors are — how nearly they point in the same direction — independent of how long either one is. It takes the vector-dot-product of the two vectors and divides it by the product of their lengths, leaving a single number between $-1$"
      }
    },
    "cpu-vs-gpu": {
      "prerequisiteReasons": {
        "memory-hierarchy": "The stated objective requires Memory Hierarchy: the complete explanation applies it in this mechanism: A CPU and a GPU are both processors that do arithmetic, and both face the same problem: the data they need lives far down a slow memory-hierarchy, where moving a number costs vastly more than computing on it. They answer that problem with opposite bets. The CPU is"
      }
    },
    "crawl-frontier": {
      "prerequisiteReasons": {
        "graph": "The stated objective requires Graph (Adjacency List): the complete explanation applies it in this mechanism: A web crawler repeatedly fetches a page, extracts links, and follows those links, so its pending work grows like a graph traversal. The crawl frontier is the control layer between link discovery and network fetching. It decides whether a discovered URI is already represented,",
        "queue": "The stated objective requires Queue: the complete explanation applies it in this mechanism: Suppose 200 workers share one FIFO queue. If its first 200 entries all name resources on https://docs.example, all workers can issue an http request to that origin at once. The queue preserves discovery order, but it expresses no constraint shared by requests to the same server.",
        "hash-set": "The stated objective requires Hash Set: the complete explanation applies it in this mechanism: For membership, the frontier stores a normalized request key in a hash-set. Safe normalization applies equivalences defined by URI and HTTP semantics, such as normalizing a scheme or host's case where allowed and removing dot segments. It does not blindly remove query parameters",
        "http": "The stated objective requires HTTP: the complete explanation applies it in this mechanism: Suppose 200 workers share one FIFO queue. If its first 200 entries all name resources on https://docs.example, all workers can issue an http request to that origin at once. The queue preserves discovery order, but it expresses no constraint shared by requests to the same server."
      }
    },
    "cromwell-rule": {
      "prerequisiteReasons": {
        "bayes-rule": "The stated objective requires Bayes' Rule: the complete explanation applies it in this mechanism: Cromwell's rule is a discipline on how you choose a prior in bayes-rule: never assign a prior probability of exactly 0 (or exactly 1) to any proposition that is not logically certain. The reason follows directly from the rule's own arithmetic. bayes-rule says the posterior is"
      }
    },
    "cross-product": {
      "prerequisiteReasons": {
        "vector-dot-product": "The stated objective requires Vector dot product: the complete explanation applies it in this mechanism: The cross product $u \\times v$ takes two vectors in ordinary three-dimensional space and returns a third vector — one that points perpendicular to both inputs, whose length equals the area of the parallelogram the two inputs span. Where the vector-dot-product collapses two"
      }
    },
    "cubin": {
      "prerequisiteReasons": {
        "ptx": "The stated objective requires PTX (Parallel Thread Execution): the complete explanation applies it in this mechanism: A cubin (CUDA binary) is the compiled GPU machine code for one specific GPU architecture, packaged in an ELF object file; it runs only on chips of that exact generation. A fatbinary is a container that bundles several cubins — one per targeted architecture — together with the",
        "compute-capability": "The stated objective requires Compute Capability: the complete explanation applies it in this mechanism: Start from a tension that ptx already half-resolves. ptx is the portable virtual instruction set: a stable, text-form intermediate that the GPU driver can translate (\"JIT\", just-in-time, compile) into the real machine code of whatever GPU is present. That portability has a cost",
        "sass": "The stated objective requires SASS (GPU Machine Code): the complete explanation applies it in this mechanism: Start from a tension that ptx already half-resolves. ptx is the portable virtual instruction set: a stable, text-form intermediate that the GPU driver can translate (\"JIT\", just-in-time, compile) into the real machine code of whatever GPU is present. That portability has a cost"
      }
    },
    "cuda-kernel": {
      "prerequisiteReasons": {
        "cuda-thread-hierarchy": "The stated objective requires CUDA Thread Hierarchy: the complete explanation applies it in this mechanism: A CUDA kernel is a function you write once but that every thread of a GPU launch executes in parallel. It comes in two coupled halves. The first is the kernel function: a piece of GPU code (in CUDA C++, marked with the qualifier global) that describes what one thread does,",
        "streaming-multiprocessor": "The stated objective requires Streaming Multiprocessor: the complete explanation applies it in this mechanism: Why split it this way (the key insight). The split cleanly separates what one unit of work is from how much of it to run. The kernel function fixes the what: \"given my element, compute this.\" The launch fixes the how much: \"make this many threads.\" This is powerful for two"
      }
    },
    "cuda-stream": {
      "prerequisiteReasons": {
        "cuda-kernel": "The stated objective requires CUDA Kernel: the complete explanation applies it in this mechanism: Overlap is conditional. A copy may overlap a cuda-kernel only when the transfer type, memory placement, copy engine, device capability, and absence of dependencies permit it. Two kernels may serialize because they exhaust compute or memory resources even when placed in separate",
        "thread-synchronization": "The stated objective requires Thread Synchronization: the complete explanation applies it in this mechanism: Events express cross-stream dependencies without forcing the host to wait: record an event after producing data in one stream, then make another stream wait on it before consuming that data. Broad device or stream synchronization is simpler but can erase intended concurrency."
      }
    },
    "cuda-thread-hierarchy": {
      "prerequisiteReasons": {
        "warp": "The stated objective requires Warp: the complete explanation applies it in this mechanism: When you run a program on an NVIDIA GPU, you do not start one execution — you start a whole army of them at once, and CUDA (NVIDIA's name for both the GPU and the programming model you write it in) organizes that army into a strict three-level hierarchy. At the bottom is a",
        "streaming-multiprocessor": "The stated objective requires Streaming Multiprocessor: the complete explanation applies it in this mechanism: When you run a program on an NVIDIA GPU, you do not start one execution — you start a whole army of them at once, and CUDA (NVIDIA's name for both the GPU and the programming model you write it in) organizes that army into a strict three-level hierarchy. At the bottom is a"
      }
    },
    "cumulative-distribution-function": {
      "prerequisiteReasons": {
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: A probability-distribution tells you how a random-variable's probability is spread across its values — as point-weights (a pmf) when the values are separate, or as area under a height curve (a pdf) when they form a continuous range. The cumulative distribution function (CDF),",
        "random-variable": "The stated objective requires Random Variable: the complete explanation applies it in this mechanism: A probability-distribution tells you how a random-variable's probability is spread across its values — as point-weights (a pmf) when the values are separate, or as area under a height curve (a pdf) when they form a continuous range. The cumulative distribution function (CDF),"
      }
    },
    "curl": {
      "prerequisiteReasons": {
        "del-operator": "The stated objective requires Del operator (∇): the complete explanation applies it in this mechanism: It is the cross-product of the del-operator with the field; each component is a mirror-pair difference of partials:",
        "vector-field": "The stated objective requires Vector Field: the complete explanation applies it in this mechanism: Curl turns a vector-field into a vector measuring the local rotation (spin) at each point. Input: vector → output: vector.",
        "cross-product": "The stated objective requires Cross Product: the complete explanation applies it in this mechanism: It is the cross-product of the del-operator with the field; each component is a mirror-pair difference of partials:",
        "jacobian": "The stated objective requires Jacobian (J): the complete explanation applies it in this mechanism: Curl is the only one of the operators with minus signs — it captures the antisymmetric part of the jacobian (the rigid-rotation component of a flow)."
      }
    },
    "cyclical-feature-encoding": {
      "prerequisiteReasons": {
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: The calculation is a fixed numeric transformation built from arithmetic. It does not create information or establish a causal relationship; it makes the known circular geometry available to a downstream model."
      }
    },
    "data-parallelism": {
      "prerequisiteReasons": {
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: Data parallelism scales training by putting a full copy of the same neural-network on every GPU, giving each copy a different slice of the data batch, and keeping the copies identical by all-reduce-ing the gradients every step. Each GPU runs gradient-descent on its own data,",
        "gradient-descent": "The stated objective requires Gradient descent: the complete explanation applies it in this mechanism: Data parallelism scales training by putting a full copy of the same neural-network on every GPU, giving each copy a different slice of the data batch, and keeping the copies identical by all-reduce-ing the gradients every step. Each GPU runs gradient-descent on its own data,",
        "all-reduce": "The stated objective requires All-reduce: the complete explanation applies it in this mechanism: Data parallelism scales training by putting a full copy of the same neural-network on every GPU, giving each copy a different slice of the data batch, and keeping the copies identical by all-reduce-ing the gradients every step. Each GPU runs gradient-descent on its own data,"
      }
    },
    "database-trigger": {
      "prerequisiteReasons": {
        "sql": "The stated objective requires SQL (Structured Query Language): the complete explanation applies it in this mechanism: A database trigger is a procedure the database engine runs automatically when a specified data-change event happens on a table. You register it once with CREATE TRIGGER, naming the event — an INSERT, UPDATE, or DELETE written in sql — and from then on the engine fires the",
        "transaction": "The stated objective requires Transaction (ACID): the complete explanation applies it in this mechanism: A database trigger is a procedure the database engine runs automatically when a specified data-change event happens on a table. You register it once with CREATE TRIGGER, naming the event — an INSERT, UPDATE, or DELETE written in sql — and from then on the engine fires the"
      }
    },
    "dataset-lineage": {
      "prerequisiteReasons": {
        "directed-acyclic-graph": "The stated objective requires Directed Acyclic Graph: the complete explanation applies it in this mechanism: Represent immutable dataset versions and transformation executions as vertices in a directed-acyclic-graph. Input-to-run and run-to-output edges express derivation. Versioned outputs prevent a later retraining cycle from becoming a literal cycle back into its own historical"
      }
    },
    "definite-integral": {
      "prerequisiteReasons": {
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: Split $[a,b]$ into subintervals of widths $\\Delta xi$, choose a point $xi^$ in each, and form the arithmetic sum"
      }
    },
    "del-operator": {
      "prerequisiteReasons": {
        "partial-derivative": "The stated objective requires Partial derivative: the complete explanation applies it in this mechanism: The building block. Each entry of $\\nabla$ is a partial-derivative operator — $\\tfrac{\\partial}{\\partial x}$ means \"measure how the input changes as $x$ moves, holding the other variables fixed.\" On its own, $\\tfrac{\\partial}{\\partial x}$ is incomplete: it is a verb waiting for",
        "vector-dot-product": "The stated objective requires Vector dot product: the complete explanation applies it in this mechanism: Why this substitution is legitimate. It is not a sloppy abuse of notation; it works for a precise reason. The vector products — scalar multiplication, the vector-dot-product, and the cross-product — are built by distributing a multiplication across sums and pulling scalar",
        "cross-product": "The stated objective requires Cross Product: the complete explanation applies it in this mechanism: Why this substitution is legitimate. It is not a sloppy abuse of notation; it works for a precise reason. The vector products — scalar multiplication, the vector-dot-product, and the cross-product — are built by distributing a multiplication across sums and pulling scalar"
      }
    },
    "demand-paging": {
      "prerequisiteReasons": {
        "page-table": "The stated objective requires Page Table: the complete explanation applies it in this mechanism: Demand paging is the kernel's policy of refusing to commit real physical memory to a mapped region until the program actually touches it. When a process asks for memory, the kernel sets up the page-table records for the whole region but marks every entry not present — no",
        "page-fault": "The stated objective requires Page Fault: the complete explanation applies it in this mechanism: Demand paging is the kernel's policy of refusing to commit real physical memory to a mapped region until the program actually touches it. When a process asks for memory, the kernel sets up the page-table records for the whole region but marks every entry not present — no",
        "interrupt": "The stated objective requires Interrupt: the complete explanation applies it in this mechanism: Here is the step that looks like magic and the justification for it. The program executes an instruction that reads or writes some virtual address. The mmu splits that address as the page-table node described — a page number and an offset — and walks the page-table to the entry",
        "mmu": "The stated objective requires Memory Management Unit: the complete explanation applies it in this mechanism: Here is the step that looks like magic and the justification for it. The program executes an instruction that reads or writes some virtual address. The mmu splits that address as the page-table node described — a page number and an offset — and walks the page-table to the entry"
      }
    },
    "denoising-diffusion-probabilistic-model": {
      "prerequisiteReasons": {
        "stochastic-process": "The stated objective requires Stochastic Process: the complete explanation applies it in this mechanism: A denoising diffusion probabilistic model (DDPM) defines a fixed forward stochastic-process that gradually adds Gaussian noise, then trains a neural-network to approximate the reverse transitions needed to generate data from noise.",
        "conditional-probability": "The stated objective requires Conditional Probability: the complete explanation applies it in this mechanism: with $\\alphat=1-\\betat$. Define $\\bar\\alphat=\\prod{s=1}^t\\alphas$. Because the transitions are linear Gaussians, their marginal conditional-probability has the closed form",
        "divergence": "The stated objective requires Divergence (∇·F): the complete explanation applies it in this mechanism: - stochastic-process - conditional-probability - divergence - kl-divergence - reparameterization-trick - neural-network",
        "kl-divergence": "The stated objective requires Kullback–Leibler Divergence: the complete explanation applies it in this mechanism: The exact forward posterior $q(x{t-1}\\mid xt,x0)$ is Gaussian. A common reverse model uses a Gaussian $p\\theta(x{t-1}\\mid xt)$ whose mean is parameterized through predicted noise, clean data, or velocity. Terms in the variational objective then involve analytic Gaussian",
        "reparameterization-trick": "The stated objective requires Reparameterization Trick: the complete explanation applies it in this mechanism: Using the reparameterization-trick, training can sample any timestep directly:",
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: A denoising diffusion probabilistic model (DDPM) defines a fixed forward stochastic-process that gradually adds Gaussian noise, then trains a neural-network to approximate the reverse transitions needed to generate data from noise."
      }
    },
    "dentry": {
      "prerequisiteReasons": {
        "vfs": "The stated objective requires Virtual File System: the complete explanation applies it in this mechanism: A dentry (short for directory entry) is the vfs object that maps a single name to a file's identity. The identity itself is held in an inode — the kernel record that stores a file's real properties (its type, permissions, owner, size, timestamps, and the pointers to where its",
        "inode": "The stated objective requires Inode: the complete explanation applies it in this mechanism: A dentry (short for directory entry) is the vfs object that maps a single name to a file's identity. The identity itself is held in an inode — the kernel record that stores a file's real properties (its type, permissions, owner, size, timestamps, and the pointers to where its"
      }
    },
    "deque": {
      "prerequisiteReasons": {
        "linked-list": "The stated objective requires Linked List: the complete explanation applies it in this mechanism: A deque — short for double-ended queue, and pronounced \"deck\" — is a sequence of values that lets you add a value at either end and remove a value from either end, and lets you do all four of those things cheaply: each one is a fixed amount of work that does not grow as the",
        "queue": "The stated objective requires Queue: the complete explanation applies it in this mechanism: A deque — short for double-ended queue, and pronounced \"deck\" — is a sequence of values that lets you add a value at either end and remove a value from either end, and lets you do all four of those things cheaply: each one is a fixed amount of work that does not grow as the"
      }
    },
    "derivative": {
      "prerequisiteReasons": {
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: The slope of $f$ at $x$ is the ratio of a tiny output change to the input change that caused it (a difference divided by a difference — arithmetic):"
      }
    },
    "determinant": {
      "prerequisiteReasons": {
        "linear-transformation": "The stated objective requires Linear Transformation: the complete explanation applies it in this mechanism: For a square matrix $A$, the determinant $\\det(A)$ is the signed factor by which its linear-transformation scales oriented volume.",
        "matrix-multiplication": "The stated objective requires Matrix Multiplication: the complete explanation applies it in this mechanism: This mirrors composition by matrix-multiplication. Determinants are not defined as a single scalar for arbitrary rectangular matrices, and computing them is not the preferred numerical test for solving a large linear system. Factorizations and condition estimates are usually",
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: - linear-transformation - matrix-multiplication - arithmetic - linear-independence",
        "linear-independence": "The stated objective requires Linear independence: the complete explanation applies it in this mechanism: - linear-transformation - matrix-multiplication - arithmetic - linear-independence"
      }
    },
    "device-driver": {
      "prerequisiteReasons": {
        "kernel": "The stated objective requires Kernel: the complete explanation applies it in this mechanism: A device driver is the piece of kernel code that knows how to talk to one specific piece of hardware. The kernel wants to stay hardware-agnostic: it would like to say \"read this block of data\" the same way to every disk, and \"send these bytes\" the same way to every network card,",
        "dma": "The stated objective requires Direct Memory Access: the complete explanation applies it in this mechanism: The same split explains the driver's other duties, the ones that face the hardware. The driver is the only code that knows this device's registers, so it is also the natural place to do the three privileged things only the kernel may do for this device. It programs the hardware",
        "interrupt": "The stated objective requires Interrupt: the complete explanation applies it in this mechanism: The same split explains the driver's other duties, the ones that face the hardware. The driver is the only code that knows this device's registers, so it is also the natural place to do the three privileged things only the kernel may do for this device. It programs the hardware"
      }
    },
    "differential": {
      "prerequisiteReasons": {
        "partial-derivative": "The stated objective requires Partial derivative: the complete explanation applies it in this mechanism: The differential $Df$ of a scalar function $f$ of several inputs is its full (total) first derivative: a single row of numbers, the partial-derivatives laid side by side, that behaves as a linear map — feed it a direction and it returns the rate at which $f$ changes along that"
      }
    },
    "differential-equation": {
      "prerequisiteReasons": {
        "derivative": "The stated objective requires Derivative: the complete explanation applies it in this mechanism: A differential equation is an equation whose unknown is a function and whose rule relates that function to its derivative or derivatives."
      }
    },
    "differential-operators": {
      "prerequisiteReasons": {
        "del-operator": "The stated objective requires Del operator (∇): the complete explanation applies it in this mechanism: Gradient, divergence, curl, Laplacian, Jacobian, Hessian — six names, one machine: take the del-operator $\\nabla$, combine it with a field through a product, then optionally compose.",
        "gradient": "The stated objective requires Gradient: the complete explanation applies it in this mechanism: - Three products with a field: scalar-multiply → gradient (scalar→vector), dot → divergence (vector-field→scalar), cross → curl (vector field→vector). - Two compositions / full derivatives: gradient then divergence → laplacian ($\\nabla\\cdot\\nabla$); the jacobian is the",
        "divergence": "The stated objective requires Divergence (∇·F): the complete explanation applies it in this mechanism: - Three products with a field: scalar-multiply → gradient (scalar→vector), dot → divergence (vector-field→scalar), cross → curl (vector field→vector). - Two compositions / full derivatives: gradient then divergence → laplacian ($\\nabla\\cdot\\nabla$); the jacobian is the",
        "curl": "The stated objective requires Curl (∇×F): the complete explanation applies it in this mechanism: - Three products with a field: scalar-multiply → gradient (scalar→vector), dot → divergence (vector-field→scalar), cross → curl (vector field→vector). - Two compositions / full derivatives: gradient then divergence → laplacian ($\\nabla\\cdot\\nabla$); the jacobian is the",
        "laplacian": "The stated objective requires Laplacian (∇²f): the complete explanation applies it in this mechanism: - Three products with a field: scalar-multiply → gradient (scalar→vector), dot → divergence (vector-field→scalar), cross → curl (vector field→vector). - Two compositions / full derivatives: gradient then divergence → laplacian ($\\nabla\\cdot\\nabla$); the jacobian is the",
        "jacobian": "The stated objective requires Jacobian (J): the complete explanation applies it in this mechanism: - Three products with a field: scalar-multiply → gradient (scalar→vector), dot → divergence (vector-field→scalar), cross → curl (vector field→vector). - Two compositions / full derivatives: gradient then divergence → laplacian ($\\nabla\\cdot\\nabla$); the jacobian is the",
        "hessian": "The stated objective requires Hessian (H): the complete explanation applies it in this mechanism: - Three products with a field: scalar-multiply → gradient (scalar→vector), dot → divergence (vector-field→scalar), cross → curl (vector field→vector). - Two compositions / full derivatives: gradient then divergence → laplacian ($\\nabla\\cdot\\nabla$); the jacobian is the",
        "vector-field": "The stated objective requires Vector Field: the complete explanation applies it in this mechanism: - Three products with a field: scalar-multiply → gradient (scalar→vector), dot → divergence (vector-field→scalar), cross → curl (vector field→vector). - Two compositions / full derivatives: gradient then divergence → laplacian ($\\nabla\\cdot\\nabla$); the jacobian is the",
        "differential": "The stated objective requires Differential (Total Derivative): the complete explanation applies it in this mechanism: - Three products with a field: scalar-multiply → gradient (scalar→vector), dot → divergence (vector-field→scalar), cross → curl (vector field→vector). - Two compositions / full derivatives: gradient then divergence → laplacian ($\\nabla\\cdot\\nabla$); the jacobian is the"
      }
    },
    "diffusion-noise-schedule": {
      "prerequisiteReasons": {
        "denoising-diffusion-probabilistic-model": "The stated objective requires Denoising Diffusion Probabilistic Model: the complete explanation applies it in this mechanism: A diffusion noise schedule chooses the sequence $\\betat$ in a denoising-diffusion-probabilistic-model. Through the cumulative product $\\bar\\alphat$, it determines how much original signal remains at each timestep.",
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: - denoising-diffusion-probabilistic-model - probability-distribution - expectation",
        "expectation": "The stated objective requires Expectation: the complete explanation applies it in this mechanism: - denoising-diffusion-probabilistic-model - probability-distribution - expectation"
      }
    },
    "diffusion-transformer": {
      "prerequisiteReasons": {
        "denoising-diffusion-probabilistic-model": "The stated objective requires Denoising Diffusion Probabilistic Model: the complete explanation applies it in this mechanism: A diffusion transformer (DiT) uses transformer blocks as the prediction backbone of a denoising-diffusion-probabilistic-model. A common design processes patches from a compressed image representation and conditions every block on noise level and class or text information.",
        "transformer-attention": "The stated objective requires Transformer attention: the complete explanation applies it in this mechanism: tokens. The sequence is processed with transformer-attention, then projected back to a spatial prediction used by the diffusion objective. An autoencoder often maps between pixels and this lower-resolution latent space.",
        "adaptive-layer-normalization": "The stated objective requires Adaptive Layer Normalization: the complete explanation applies it in this mechanism: Global timestep or class information is commonly injected by adaptive-layer-normalization. Rich text can instead use attention-based conditioning. Evaluate designs at matched training compute and matched inference budgets; report loss and image metrics against operations,",
        "autoencoder": "The stated objective requires Autoencoder: the complete explanation applies it in this mechanism: tokens. The sequence is processed with transformer-attention, then projected back to a spatial prediction used by the diffusion objective. An autoencoder often maps between pixels and this lower-resolution latent space."
      }
    },
    "directed-acyclic-graph": {
      "prerequisiteReasons": {
        "graph": "The stated objective requires Graph (Adjacency List): the complete explanation applies it in this mechanism: Start with a graph, but give each edge an orientation $u\\to v$. A directed path follows these arrow directions. The graph is acyclic when no nonempty directed path begins and ends at the same vertex."
      }
    },
    "directional-derivative": {
      "prerequisiteReasons": {
        "partial-derivative": "The stated objective requires Partial derivative: the complete explanation applies it in this mechanism: The directional derivative of a scalar function $f$ (a function whose output is a single number) is the rate at which $f$ changes as you step away from a chosen point in a chosen direction $v$. It generalizes the partial-derivative, which only measures change along a coordinate",
        "vector-dot-product": "The stated objective requires Vector dot product: the complete explanation applies it in this mechanism: The directional derivative of a scalar function $f$ (a function whose output is a single number) is the rate at which $f$ changes as you step away from a chosen point in a chosen direction $v$. It generalizes the partial-derivative, which only measures change along a coordinate",
        "differential": "The stated objective requires Differential (Total Derivative): the complete explanation applies it in this mechanism: Now the central formula and the why behind it. Collect the partial derivatives of $f$ into a single vector called the gradient, written $\\nabla f$: for $f(x, y)$ it is $\\nabla f = (\\partial f / \\partial x,\\; \\partial f / \\partial y)$. (The gradient is just this stacking of"
      }
    },
    "distributed-tracing": {
      "prerequisiteReasons": {
        "trace-span": "The stated objective requires Trace Span: the complete explanation applies it in this mechanism: Distributed tracing reconstructs the path of one request through many processes as related trace-span records. context-propagation preserves the identifiers needed to attach each remote operation to the same causal history.",
        "context-propagation": "The stated objective requires Context Propagation: the complete explanation applies it in this mechanism: Distributed tracing reconstructs the path of one request through many processes as related trace-span records. context-propagation preserves the identifiers needed to attach each remote operation to the same causal history."
      }
    },
    "divergence": {
      "prerequisiteReasons": {
        "del-operator": "The stated objective requires Del operator (∇): the complete explanation applies it in this mechanism: It is the vector-dot-product of the del-operator with the field:",
        "vector-dot-product": "The stated objective requires Vector dot product: the complete explanation applies it in this mechanism: It is the vector-dot-product of the del-operator with the field:",
        "vector-field": "The stated objective requires Vector Field: the complete explanation applies it in this mechanism: Divergence turns a vector-field into a scalar measuring the net outflow at each point. Input: vector field → output: scalar.",
        "differential": "The stated objective requires Differential (Total Derivative): the complete explanation applies it in this mechanism: — the sum of the diagonal partials; each $\\partial Fi/\\partial xi$ is one entry from the differential of the corresponding field component (the trace of the jacobian of F). Positive divergence means the field is spreading out (a source); negative means it is converging (a sink).",
        "jacobian": "The stated objective requires Jacobian (J): the complete explanation applies it in this mechanism: — the sum of the diagonal partials; each $\\partial Fi/\\partial xi$ is one entry from the differential of the corresponding field component (the trace of the jacobian of F). Positive divergence means the field is spreading out (a source); negative means it is converging (a sink)."
      }
    },
    "divergence-of-curl-zero": {
      "prerequisiteReasons": {
        "divergence": "The stated objective requires Divergence (∇·F): the complete explanation applies it in this mechanism: For any vector-field $F$, the divergence of its curl is exactly zero: $\\nabla \\cdot (\\nabla \\times F) = 0$. A curl field can never spread out or converge — it has no sources and no sinks anywhere. Such a field is called solenoidal (or source-free). The flip side is just as",
        "curl": "The stated objective requires Curl (∇×F): the complete explanation applies it in this mechanism: For any vector-field $F$, the divergence of its curl is exactly zero: $\\nabla \\cdot (\\nabla \\times F) = 0$. A curl field can never spread out or converge — it has no sources and no sinks anywhere. Such a field is called solenoidal (or source-free). The flip side is just as",
        "partial-derivative": "The stated objective requires Partial derivative: the complete explanation applies it in this mechanism: Expanding the six terms, every one is a mixed second partial — a partial-derivative taken twice, once by each of two different variables. Here is the key step. Mixed partials are equal regardless of the order you take them: differentiating $R$ first by $y$ then by $x$ gives the",
        "vector-field": "The stated objective requires Vector Field: the complete explanation applies it in this mechanism: For any vector-field $F$, the divergence of its curl is exactly zero: $\\nabla \\cdot (\\nabla \\times F) = 0$. A curl field can never spread out or converge — it has no sources and no sinks anywhere. Such a field is called solenoidal (or source-free). The flip side is just as"
      }
    },
    "dma": {
      "prerequisiteReasons": {
        "memory-hierarchy": "The stated objective requires Memory Hierarchy: the complete explanation applies it in this mechanism: When a device — a disk, a network card, a GPU — needs to move a block of data into or out of main memory (the RAM rung of the memory-hierarchy), there are two ways to do it. The naive way is to make the processor (the CPU, the chip that executes the program's instructions) carry",
        "interrupt": "The stated objective requires Interrupt: the complete explanation applies it in this mechanism: When a device — a disk, a network card, a GPU — needs to move a block of data into or out of main memory (the RAM rung of the memory-hierarchy), there are two ways to do it. The naive way is to make the processor (the CPU, the chip that executes the program's instructions) carry"
      }
    },
    "dns": {
      "prerequisiteReasons": {
        "socket": "The stated objective requires Socket: the complete explanation applies it in this mechanism: DNS (the Domain Name System) is the distributed directory that turns a human-readable name — like example.com — into the numeric IP address — like 93.184.216.34 — that the network actually needs to deliver packets. People remember names; the network routes only to numbers, so",
        "udp": "The stated objective requires UDP: the complete explanation applies it in this mechanism: Two terms used above, defined before going on. UDP (udp) is the network's datagram style of communication — fire one self-contained message, no guarantee it arrives, no automatic resend — which in the socket node was the behavior of a datagram socket; DNS rides on it because a"
      }
    },
    "document-model": {
      "prerequisiteReasons": {
        "key-value": "The stated objective requires Key–Value Mapping: the complete explanation applies it in this mechanism: Start from the key-value mapping: a set of (key, value) pairs with unique keys, accessed by key content. A document is that mapping made recursive — each value may itself be a scalar, an array, or another key-value mapping. So a document is a finite tree: internal nodes are",
        "relational-model": "The stated objective requires Relational Model: the complete explanation applies it in this mechanism: The document data model stores each record as a document: a self-contained, nested tree of key-value pairs, in practice written as JSON. Two choices define it against the relational-model. First, values may be structured: where a relational column holds a flat typed value, a"
      }
    },
    "dora": {
      "prerequisiteReasons": {
        "lora": "The stated objective requires LoRA (Low-Rank Adaptation): the complete explanation applies it in this mechanism: Weight-Decomposed Low-Rank Adaptation (DoRA) separates the magnitude and direction of a pretrained weight matrix, then uses lora to adapt direction while learning magnitude directly.",
        "low-rank-factorization": "The stated objective requires Low-rank factorization: the complete explanation applies it in this mechanism: where $BA$ is the low-rank-factorization learned by LoRA. The effective adapted weight is",
        "fine-tuning": "The stated objective requires Fine-tuning: the complete explanation applies it in this mechanism: DoRA is still a fine-tuning method, not a guarantee of better quality at every rank or task. Normalization adds training computation and numerical considerations; parameter count, optimizer state, and merge behavior differ from plain LoRA. After training, implementations can"
      }
    },
    "double-descent": {
      "prerequisiteReasons": {
        "regularization": "The stated objective requires Regularization: the complete explanation applies it in this mechanism: To evaluate a suspected double-descent effect, vary only the capacity control while keeping the split, optimizer schedule, and preprocessing fixed; repeat seeds; report training and test error, uncertainty, and the interpolation threshold. Compare against explicit regularization",
        "loss-function": "The stated objective requires Loss function: the complete explanation applies it in this mechanism: This is an observed regime, not a promise that extra parameters improve generalization. The curve depends on data distribution, label noise, architecture, optimizer, training duration, and the metric. The training loss-function can remain near zero on both sides of the peak, so"
      }
    },
    "dropblock": {
      "prerequisiteReasons": {
        "regularization": "The stated objective requires Regularization: the complete explanation applies it in this mechanism: DropBlock is a training-time regularization method for convolutional networks that zeros a contiguous block of activations rather than independently masking individual activations.",
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: - regularization - neural-network"
      }
    },
    "dropout": {
      "prerequisiteReasons": {
        "bernoulli-distribution": "The stated objective requires Bernoulli Distribution: the complete explanation applies it in this mechanism: A layer can otherwise make later layers depend too closely on particular paths through a network. During each training forward pass, dropout makes the path variable: for every element of an activation tensor, it either retains the value with probability $q=1-p$ or replaces it",
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: Dropout is a regularization method for a neural-network that, during training, independently replaces some activation values with zero and scales every retained value to preserve the expected activation scale used at inference.",
        "regularization": "The stated objective requires Regularization: the complete explanation applies it in this mechanism: Dropout is a regularization method for a neural-network that, during training, independently replaces some activation values with zero and scales every retained value to preserve the expected activation scale used at inference.",
        "tensor": "The stated objective requires Tensor: the complete explanation applies it in this mechanism: A layer can otherwise make later layers depend too closely on particular paths through a network. During each training forward pass, dropout makes the path variable: for every element of an activation tensor, it either retains the value with probability $q=1-p$ or replaces it"
      }
    },
    "dynamic-array": {
      "prerequisiteReasons": {
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: — a single multiplication and a single addition, both of which are arithmetic. It does not matter whether i is 2 or 2,000,000: the work is the same fixed amount. We call an operation whose cost does not grow with the number of stored elements O(1), or constant time (the \"n\""
      }
    },
    "elastic-weight-consolidation": {
      "prerequisiteReasons": {
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: In a neural-network, large $Fi$ makes parameter $i$ stable while small $Fi$ leaves it plastic. The coefficient $\\lambda$ controls the global stability–plasticity trade-off. If it is too small, old-task quality can collapse; if it is too large, learning the new task can stall.",
        "regularization": "The stated objective requires Regularization: the complete explanation applies it in this mechanism: Elastic weight consolidation (EWC) is a continual-learning form of regularization. After learning an old task, it penalizes movement away from the old parameters in directions estimated important to that task.",
        "likelihood": "The stated objective requires Likelihood: the complete explanation applies it in this mechanism: The original method estimates $Fi$ from the diagonal Fisher information under the old task. The Fisher estimate is built from score gradients of the likelihood, connecting parameter sensitivity to maximum-likelihood-estimation: parameters whose training-objective gradients carry",
        "maximum-likelihood-estimation": "The stated objective requires Maximum Likelihood Estimation: the complete explanation applies it in this mechanism: The original method estimates $Fi$ from the diagonal Fisher information under the old task. The Fisher estimate is built from score gradients of the likelihood, connecting parameter sensitivity to maximum-likelihood-estimation: parameters whose training-objective gradients carry"
      }
    },
    "embedding": {
      "prerequisiteReasons": {
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: An embedding is a dense vector — an ordered list of real numbers, often a few hundred to a couple thousand of them (for example 768 or 1536 numbers) — that represents the meaning of some input, such as a word, a sentence, an image, or a catalogue item. A neural-network produces"
      }
    },
    "execution-provider": {
      "prerequisiteReasons": {
        "computation-graph": "The stated objective requires Computation graph: the complete explanation applies it in this mechanism: Each node in a computation-graph invokes an operation defined by an operator-set. A hardware backend may implement only some of those operations, types, and shapes. An execution provider reports that capability to the runtime, supplies implementations for the claimed work, and",
        "operator-set": "The stated objective requires Operator set: the complete explanation applies it in this mechanism: Each node in a computation-graph invokes an operation defined by an operator-set. A hardware backend may implement only some of those operations, types, and shapes. An execution provider reports that capability to the runtime, supplies implementations for the claimed work, and",
        "graph": "The stated objective requires Graph (Adjacency List): the complete explanation applies it in this mechanism: Providers are considered in priority order. Using the node-edge connectivity of a graph, the first provider claims the largest regions it can handle; the next provider considers what remains. A default provider comes last and supplies fallback implementations, so partial"
      }
    },
    "expectation": {
      "prerequisiteReasons": {
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: A probability-distribution tells you how likely each value of a random-variable is, but not what value to expect on average. The expectation (or mean) $E[X]$ answers that: it is the probability-weighted average of the values — the center of mass of the distribution, the point",
        "random-variable": "The stated objective requires Random Variable: the complete explanation applies it in this mechanism: A probability-distribution tells you how likely each value of a random-variable is, but not what value to expect on average. The expectation (or mean) $E[X]$ answers that: it is the probability-weighted average of the values — the center of mass of the distribution, the point"
      }
    },
    "expectation-maximization": {
      "prerequisiteReasons": {
        "maximum-likelihood-estimation": "The stated objective requires Maximum Likelihood Estimation: the complete explanation applies it in this mechanism: Expectation-Maximization (EM) is the way to compute a maximum-likelihood-estimation estimate when a latent variable — a hidden cause you never get to observe — makes the likelihood impossible to maximize directly. Recall that MLE picks the parameter $\\theta$ under which the",
        "bayes-rule": "The stated objective requires Bayes' Rule: the complete explanation applies it in this mechanism: Expectation-Maximization (EM) is the way to compute a maximum-likelihood-estimation estimate when a latent variable — a hidden cause you never get to observe — makes the likelihood impossible to maximize directly. Recall that MLE picks the parameter $\\theta$ under which the",
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: where $\\pic$ is the prior probability that source $c$ was the one selected (the mixing weight, with $\\sumc \\pic = 1$) and $P(d \\mid c, \\theta)$ is the probability-distribution of source $c$ evaluated at $d$ — how that one source would have generated $d$. The log-likelihood of",
        "likelihood": "The stated objective requires Likelihood: the complete explanation applies it in this mechanism: Expectation-Maximization (EM) is the way to compute a maximum-likelihood-estimation estimate when a latent variable — a hidden cause you never get to observe — makes the likelihood impossible to maximize directly. Recall that MLE picks the parameter $\\theta$ under which the",
        "expectation": "The stated objective requires Expectation: the complete explanation applies it in this mechanism: This is the E-step (for expectation: the responsibilities are the expectations — probability-weighted averages — of the hidden 0/1 \"did source $c$ produce point $i$?\" indicators under the current $\\theta$)."
      }
    },
    "expert-parallelism": {
      "prerequisiteReasons": {
        "mixture-of-experts": "The stated objective requires Mixture-of-Experts (MoE): the complete explanation applies it in this mechanism: A mixture-of-experts layer holds many expert FFNs and routes each token to only a few. Expert parallelism (EP) is how you run that layer across GPUs: place different experts on different GPUs. Because a token's chosen expert usually lives on another GPU, the routed tokens are",
        "all-to-all": "The stated objective requires All-to-all: the complete explanation applies it in this mechanism: A mixture-of-experts layer holds many expert FFNs and routes each token to only a few. Expert parallelism (EP) is how you run that layer across GPUs: place different experts on different GPUs. Because a token's chosen expert usually lives on another GPU, the routed tokens are"
      }
    },
    "exponential-function": {
      "prerequisiteReasons": {
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: Built on arithmetic (repeated multiplication, extended to all reals):"
      }
    },
    "fair-scheduler": {
      "prerequisiteReasons": {
        "scheduler": "The stated objective requires Scheduler: the complete explanation applies it in this mechanism: The fair scheduler is the specific policy that Linux's scheduler uses to answer the recurring question \"which runnable task runs next?\" — and the structure that makes the answer fast. Its one governing idea is to give every task an equal share of the CPU over time, bent only by",
        "binary-search-tree": "The stated objective requires Binary Search Tree: the complete explanation applies it in this mechanism: A red-black tree is a self-balancing binary-search-tree: a binary tree (each node has up to two children) ordered so that everything in a node's left subtree sorts before it and everything in the right sorts after — here, \"sorts before\" means \"smaller vruntime.\" The BST property"
      }
    },
    "feature-hashing": {
      "prerequisiteReasons": {
        "hash-map": "The stated objective requires Hash Map: the complete explanation applies it in this mechanism: The same location calculation appears in a hash-map, but feature hashing does not keep keys beside the buckets to resolve collisions. It adds the feature's numeric value directly into coordinate $j(x)$ of a one-dimensional tensor. An optional second hash supplies a sign",
        "tensor": "The stated objective requires Tensor: the complete explanation applies it in this mechanism: The same location calculation appears in a hash-map, but feature hashing does not keep keys beside the buckets to resolve collisions. It adds the feature's numeric value directly into coordinate $j(x)$ of a one-dimensional tensor. An optional second hash supplies a sign",
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: Choose a width $m$. For every feature name $x$, compute a stable integer hash $h(x)$ and use modulo arithmetic to select coordinate"
      }
    },
    "federated-learning": {
      "prerequisiteReasons": {
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: Federated learning trains a shared neural-network without first collecting every training example in one place. A coordinator sends a current model to selected clients; each client improves that copy using its local data; the coordinator combines the returned changes into the",
        "gradient-descent": "The stated objective requires Gradient descent: the complete explanation applies it in this mechanism: The central constraint is data location. In ordinary centralized training, examples are moved to a training service, which applies gradient-descent to one combined dataset. Federated learning instead keeps the examples at clients such as devices or organizations. Only model"
      }
    },
    "file-descriptor": {
      "prerequisiteReasons": {
        "system-call": "The stated objective requires System Call: the complete explanation applies it in this mechanism: A file descriptor (fd) is a small non-negative integer that a program uses to name one open resource — a file, a network socket, a pipe, a device, anything the kernel can hand it. The program never holds the resource itself; it holds only the number. The kernel keeps, for each"
      }
    },
    "fine-tuning": {
      "prerequisiteReasons": {
        "gradient-descent": "The stated objective requires Gradient descent: the complete explanation applies it in this mechanism: Start from a neural-network whose weights were already learned on a large general corpus (pretraining). Then run more gradient-descent steps on task-specific data, updating the weights:",
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: Start from a neural-network whose weights were already learned on a large general corpus (pretraining). Then run more gradient-descent steps on task-specific data, updating the weights:"
      }
    },
    "flow-matching": {
      "prerequisiteReasons": {
        "vector-field": "The stated objective requires Vector Field: the complete explanation applies it in this mechanism: Flow matching trains a time-dependent vector-field by regression so that its ordinary differential-equation transports a simple base distribution along a selected probability path toward the data distribution.",
        "differential-equation": "The stated objective requires Differential Equation: the complete explanation applies it in this mechanism: Flow matching trains a time-dependent vector-field by regression so that its ordinary differential-equation transports a simple base distribution along a selected probability path toward the data distribution.",
        "differential": "The stated objective requires Differential (Total Derivative): the complete explanation applies it in this mechanism: - vector-field - differential-equation - differential - probability-distribution - conditional-probability - neural-network - score-function",
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: After training, sampling draws $X0$ from the base probability-distribution and solves",
        "conditional-probability": "The stated objective requires Conditional Probability: the complete explanation applies it in this mechanism: A parameterized field $v\\theta(t,x)$ is trained to match a target field associated with conditional-probability paths. Conditional flow matching samples a conditioning endpoint $Z$ and regresses a tractable conditional velocity $ut(x\\mid Z)$. Under the construction in Lipman et",
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: to the data endpoint. A neural-network commonly represents the field.",
        "score-function": "The stated objective requires Score Function: the complete explanation applies it in this mechanism: A velocity is not generally a score-function: it describes particle transport, while a score is a log-density gradient. A particular probability-flow construction may relate them through schedule-dependent drift and diffusion coefficients."
      }
    },
    "flow-network": {
      "prerequisiteReasons": {
        "graph": "The stated objective requires Graph (Adjacency List): the complete explanation applies it in this mechanism: A flow network turns a graph into a transport system: each directed edge has a capacity, each edge receives a nonnegative flow no larger than that capacity, and every ordinary vertex sends out exactly what enters it.",
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: The second equation is conservation: an intermediate vertex cannot create or destroy material. Sources inject flow and sinks remove it. The value of a source-to-sink flow is the total amount leaving the source, computed with ordinary arithmetic."
      }
    },
    "fma": {
      "prerequisiteReasons": {
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: Recall from arithmetic that multiplication (×) and addition (+) are two of the four primitive operations on numbers. An FMA simply combines one of each into a fixed pattern: given three numbers a, b, and c, it produces d = a × b + c. The result d is the product of a and b, added"
      }
    },
    "fork-exec": {
      "prerequisiteReasons": {
        "process": "The stated objective requires Process: the complete explanation applies it in this mechanism: Fork and exec is how Unix creates a new program and starts it running — and the surprising thing is that it takes two separate system-calls, not one. fork() duplicates the calling process: the kernel makes a near-identical copy — same code, same memory contents, copies of the",
        "system-call": "The stated objective requires System Call: the complete explanation applies it in this mechanism: Fork and exec is how Unix creates a new program and starts it running — and the surprising thing is that it takes two separate system-calls, not one. fork() duplicates the calling process: the kernel makes a near-identical copy — same code, same memory contents, copies of the",
        "file-descriptor": "The stated objective requires File Descriptor: the complete explanation applies it in this mechanism: Fork and exec is how Unix creates a new program and starts it running — and the surprising thing is that it takes two separate system-calls, not one. fork() duplicates the calling process: the kernel makes a near-identical copy — same code, same memory contents, copies of the"
      }
    },
    "fsdp": {
      "prerequisiteReasons": {
        "data-parallelism": "The stated objective requires Data parallelism: the complete explanation applies it in this mechanism: FSDP (Fully Sharded Data Parallel, a.k.a. ZeRO) is data-parallelism in which each GPU permanently stores only 1/N of every layer's parameters, gradients, and optimizer state instead of a full replica. A layer's full weights are reconstructed just-in-time with an all-gather right",
        "all-gather": "The stated objective requires All-gather: the complete explanation applies it in this mechanism: FSDP (Fully Sharded Data Parallel, a.k.a. ZeRO) is data-parallelism in which each GPU permanently stores only 1/N of every layer's parameters, gradients, and optimizer state instead of a full replica. A layer's full weights are reconstructed just-in-time with an all-gather right",
        "reduce-scatter": "The stated objective requires Reduce-scatter: the complete explanation applies it in this mechanism: FSDP (Fully Sharded Data Parallel, a.k.a. ZeRO) is data-parallelism in which each GPU permanently stores only 1/N of every layer's parameters, gradients, and optimizer state instead of a full replica. A layer's full weights are reconstructed just-in-time with an all-gather right"
      }
    },
    "full-bayesian-inference": {
      "prerequisiteReasons": {
        "bayes-rule": "The stated objective requires Bayes' Rule: the complete explanation applies it in this mechanism: Full Bayesian inference is the choice to keep the entire posterior distribution P(θ | D) that bayes-rule produces, instead of crushing it down to a single representative number. Recall that bayes-rule turns a prior belief P(θ) about an unknown quantity θ and the likelihood P(D |",
        "likelihood": "The stated objective requires Likelihood: the complete explanation applies it in this mechanism: Full Bayesian inference is the choice to keep the entire posterior distribution P(θ | D) that bayes-rule produces, instead of crushing it down to a single representative number. Recall that bayes-rule turns a prior belief P(θ) about an unknown quantity θ and the likelihood P(D |"
      }
    },
    "garbage-collection": {
      "prerequisiteReasons": {
        "graph": "The stated objective requires Graph (Adjacency List): the complete explanation applies it in this mechanism: Garbage collection (GC) is automatic memory management: instead of the programmer explicitly releasing each object when done with it, the language runtime periodically figures out which objects are still reachable and reclaims the memory of the rest. The key insight is that an"
      }
    },
    "gather": {
      "prerequisiteReasons": {
        "collective-operation": "The stated objective requires Collective operation: the complete explanation applies it in this mechanism: Gather is the collective-operation in which every process sends its chunk to the root-process, which collects them into one buffer. It is the exact inverse of scatter: where scatter deals distinct chunks out from the root, gather pulls distinct chunks in to the root. The shape",
        "root-process": "The stated objective requires Root process: the complete explanation applies it in this mechanism: Gather is the collective-operation in which every process sends its chunk to the root-process, which collects them into one buffer. It is the exact inverse of scatter: where scatter deals distinct chunks out from the root, gather pulls distinct chunks in to the root. The shape"
      }
    },
    "gaussian-mixture-model": {
      "prerequisiteReasons": {
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: A Gaussian mixture model (GMM) is a latent-variable probability-distribution formed from a finite weighted sum of Gaussian component densities.",
        "likelihood": "The stated objective requires Likelihood: the complete explanation applies it in this mechanism: expectation-maximization uses an expectation step for responsibilities and alternates between computing them and updating weights, means, and covariances to increase the data likelihood. Convergence is to a stationary point, often a local optimum, so initialization and multiple",
        "expectation": "The stated objective requires Expectation: the complete explanation applies it in this mechanism: expectation-maximization uses an expectation step for responsibilities and alternates between computing them and updating weights, means, and covariances to increase the data likelihood. Convergence is to a stationary point, often a local optimum, so initialization and multiple",
        "expectation-maximization": "The stated objective requires Expectation-Maximization: the complete explanation applies it in this mechanism: expectation-maximization uses an expectation step for responsibilities and alternates between computing them and updating weights, means, and covariances to increase the data likelihood. Convergence is to a stationary point, often a local optimum, so initialization and multiple"
      }
    },
    "geometric-distribution": {
      "prerequisiteReasons": {
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: Imagine repeating the same yes/no experiment over and over — flipping a coin until it lands heads, say — where each attempt succeeds with the same fixed probability $p$ and the attempts don't influence one another. The geometric distribution is the probability-distribution of",
        "random-variable": "The stated objective requires Random Variable: the complete explanation applies it in this mechanism: Each such repetition is called a trial. The quantity we track is the random-variable $K$ = the number of failed trials that occur before the first success (a function that maps each run of the experiment to a non-negative integer). The geometric distribution is the"
      }
    },
    "gigathread": {
      "prerequisiteReasons": {
        "streaming-multiprocessor": "The stated objective requires Streaming Multiprocessor: the complete explanation applies it in this mechanism: The GigaThread engine is the GPU's single global hardware scheduler — a small fixed circuit on the chip, not a program — whose one job is to hand out the work of a launched job to the chip's streaming-multiprocessors (SMs). A GPU job is a kernel (one computation applied across a",
        "warp": "The stated objective requires Warp: the complete explanation applies it in this mechanism: So there are two things that have to be married. On the software side is the grid — the name for the entire collection of blocks produced by one kernel launch, the complete pile of work to be done. On the hardware side is the physical SM array — the fixed number of SMs etched"
      }
    },
    "gil": {
      "prerequisiteReasons": {
        "thread": "The stated objective requires Thread: the complete explanation applies it in this mechanism: The Global Interpreter Lock (GIL) is a process-wide lock used by the default build of CPython, the most common Python implementation. A thread must hold it while executing Python bytecode, so two threads in the same interpreter cannot execute that bytecode simultaneously on"
      }
    },
    "glibc-wrapper": {
      "prerequisiteReasons": {
        "system-call": "The stated objective requires System Call: the complete explanation applies it in this mechanism: A program almost never issues a raw system-call by hand. Instead it calls an ordinary C-library function — read(), write(), open() — and the glibc wrapper does the dirty work of turning that friendly function call into the bare kernel request. glibc is the GNU C Library: a"
      }
    },
    "gpu-data-flow": {
      "prerequisiteReasons": {
        "cuda-kernel": "The stated objective requires CUDA Kernel: the complete explanation applies it in this mechanism: GPU data flow is the round trip a piece of work makes between the CPU and the GPU. The right mental model is that these are two separate computers, not a boss and a helper: the CPU (the host) and the GPU (the device) each have their own processor and their own memory, joined",
        "dma": "The stated objective requires Direct Memory Access: the complete explanation applies it in this mechanism: GPU data flow is the round trip a piece of work makes between the CPU and the GPU. The right mental model is that these are two separate computers, not a boss and a helper: the CPU (the host) and the GPU (the device) each have their own processor and their own memory, joined"
      }
    },
    "gpu-memory-spaces": {
      "prerequisiteReasons": {
        "memory-hierarchy": "The stated objective requires Memory Hierarchy: the complete explanation applies it in this mechanism: When you write a GPU program, every piece of data you touch lives in one of a few distinct memory spaces — named regions like registers, shared memory, and global memory. These spaces are not new hardware; they are the programmer-visible faces of the memory-hierarchy's physical",
        "cuda-thread-hierarchy": "The stated objective requires CUDA Thread Hierarchy: the complete explanation applies it in this mechanism: When you write a GPU program, every piece of data you touch lives in one of a few distinct memory spaces — named regions like registers, shared memory, and global memory. These spaces are not new hardware; they are the programmer-visible faces of the memory-hierarchy's physical"
      }
    },
    "gpu-tile-programming": {
      "prerequisiteReasons": {
        "cuda-kernel": "The stated objective requires CUDA Kernel: the complete explanation applies it in this mechanism: GPU tile programming describes one operation over a rectangular block of a tensor instead of describing one scalar thread. The compiler then maps that logical tile onto physical threads, vector operations, and memory transfers. It raises the programming level above a",
        "simt": "The stated objective requires SIMT: the complete explanation applies it in this mechanism: A thread-oriented cuda-kernel asks the programmer to compute a thread index, decide which element that thread owns, choose launch dimensions, and coordinate any shared-memory cooperation. The hardware ultimately executes those threads using simt, but the source program exposes",
        "tensor": "The stated objective requires Tensor: the complete explanation applies it in this mechanism: GPU tile programming describes one operation over a rectangular block of a tensor instead of describing one scalar thread. The compiler then maps that logical tile onto physical threads, vector operations, and memory transfers. It raises the programming level above a"
      }
    },
    "gpudirect": {
      "prerequisiteReasons": {
        "nvlink": "The stated objective requires NVLink: the complete explanation applies it in this mechanism: GPUDirect is an umbrella name for a family of NVIDIA technologies whose single defining idea is this: let a GPU's memory exchange data with another device directly, without first copying the bytes into the host computer's main memory and back out again. The default way to move a",
        "dma": "The stated objective requires Direct Memory Access: the complete explanation applies it in this mechanism: The single contribution that names this whole family is directness — the bytes move straight from one device's memory to another's, with the host's memory and the CPU's byte-moving labor cut out of the data path. The word for a device moving memory on its own, without the CPU"
      }
    },
    "gqa": {
      "prerequisiteReasons": {
        "transformer-attention": "The stated objective requires Transformer attention: the complete explanation applies it in this mechanism: In transformer-attention with $h$ heads, each head $i$ has its own $Qi, Ki, Vi$. During decoding the kv-cache must store the K and V of every head for every past token, so its size scales with the number of KV heads:",
        "kv-cache": "The stated objective requires KV-cache: the complete explanation applies it in this mechanism: Standard multi-head-attention gives every query head its own key/value head — so the kv-cache stores one K,V per head. Since that cache is the inference bottleneck, multi-query attention (MQA) has all query heads share a single K,V head, and grouped-query attention (GQA) is the",
        "multi-head-attention": "The stated objective requires Multi-Head Attention: the complete explanation applies it in this mechanism: Standard multi-head-attention gives every query head its own key/value head — so the kv-cache stores one K,V per head. Since that cache is the inference bottleneck, multi-query attention (MQA) has all query heads share a single K,V head, and grouped-query attention (GQA) is the"
      }
    },
    "gradient": {
      "prerequisiteReasons": {
        "derivative": "The stated objective requires Derivative: the complete explanation applies it in this mechanism: where each entry is a partial-derivative of $f$ — a derivative taken with respect to one input while holding the rest fixed. The vector points uphill, so $-\\nabla f$ points toward the steepest decrease — the direction gradient-descent steps in to reduce a loss.",
        "partial-derivative": "The stated objective requires Partial derivative: the complete explanation applies it in this mechanism: where each entry is a partial-derivative of $f$ — a derivative taken with respect to one input while holding the rest fixed. The vector points uphill, so $-\\nabla f$ points toward the steepest decrease — the direction gradient-descent steps in to reduce a loss."
      }
    },
    "gradient-descent": {
      "prerequisiteReasons": {
        "gradient": "The stated objective requires Gradient: the complete explanation applies it in this mechanism: Take the gradient of the loss-function with respect to the parameters, then move a small step against it:",
        "loss-function": "The stated objective requires Loss function: the complete explanation applies it in this mechanism: Take the gradient of the loss-function with respect to the parameters, then move a small step against it:"
      }
    },
    "graph": {
      "prerequisiteReasons": {
        "hash-map": "The stated objective requires Hash Map: the complete explanation applies it in this mechanism: A graph is a set of vertices (also called nodes) — things — together with a set of edges — connections between pairs of those things. It is the natural model whenever data is a web of relationships rather than a single line of items: a road map (cities joined by roads), a social"
      }
    },
    "graph-memory": {
      "prerequisiteReasons": {
        "agent-memory": "The stated objective requires Agent Memory: the complete explanation applies it in this mechanism: Graph memory is a form of agent-memory in which the agent's long-term store is not a flat pile of text snippets but a knowledge-graph: as the agent converses and acts, it extracts entities and typed relations from what it sees and accumulates them as triples — (User, worksAt,",
        "knowledge-graph": "The stated objective requires Knowledge Graph: the complete explanation applies it in this mechanism: Graph memory is a form of agent-memory in which the agent's long-term store is not a flat pile of text snippets but a knowledge-graph: as the agent converses and acts, it extracts entities and typed relations from what it sees and accumulates them as triples — (User, worksAt,"
      }
    },
    "graph-optimization": {
      "prerequisiteReasons": {
        "computation-graph": "The stated objective requires Computation graph: the complete explanation applies it in this mechanism: Graph optimization rewrites a computation-graph into an equivalent graph that performs less work, moves less data, or better matches the target executor. The structure changes, but every permitted input must still produce the same result.",
        "graph": "The stated objective requires Graph (Adjacency List): the complete explanation applies it in this mechanism: A computation-graph makes calculation structure explicit: its graph skeleton provides nodes and edges that a tool can change before execution. A rewrite matches a known pattern, checks the conditions that make the replacement valid, and substitutes a cheaper pattern. The key"
      }
    },
    "graph-rag": {
      "prerequisiteReasons": {
        "retrieval-augmented-generation": "The stated objective requires Retrieval-Augmented Generation: the complete explanation applies it in this mechanism: GraphRAG extends retrieval-augmented-generation for global questions over a corpus. It extracts entities and relationships into a knowledge-graph, groups related entities, prepares summaries of those communities, and combines community-level partial answers for a query.",
        "knowledge-graph": "The stated objective requires Knowledge Graph: the complete explanation applies it in this mechanism: GraphRAG extends retrieval-augmented-generation for global questions over a corpus. It extracts entities and relationships into a knowledge-graph, groups related entities, prepares summaries of those communities, and combines community-level partial answers for a query."
      }
    },
    "hard-link": {
      "prerequisiteReasons": {
        "inode": "The stated objective requires Inode: the complete explanation applies it in this mechanism: A hard link is an additional name for a file that already exists — a dentry (a name-to-number pair) pointing at an existing inode. Because a name and the inode it names are separate things, one inode can be reached through several names at once. Two hard links are therefore two",
        "dentry": "The stated objective requires Dentry: the complete explanation applies it in this mechanism: A hard link is an additional name for a file that already exists — a dentry (a name-to-number pair) pointing at an existing inode. Because a name and the inode it names are separate things, one inode can be reached through several names at once. Two hard links are therefore two"
      }
    },
    "hash-map": {
      "prerequisiteReasons": {
        "key-value": "The stated objective requires Key–Value Mapping: the complete explanation applies it in this mechanism: The object and the problem it solves. A hash map is one implementation of the key-value mapping — a collection of key → value pairs. A key is whatever you look things up by (a word, an identifier, a number); the value is the data attached to that key. The two core operations are",
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: The central trick: compute the location instead of searching for it. The map keeps a backing array — a fixed-length row of numbered slots, called buckets — of some size m (say m = 8, so the slots are numbered 0 through 7). To store a pair, the map first runs the key through a"
      }
    },
    "hash-set": {
      "prerequisiteReasons": {
        "hash-map": "The stated objective requires Hash Map: the complete explanation applies it in this mechanism: A hash set is a hash-map stripped down to its keys: it stores a collection of items with no value attached to each one, so it answers exactly one question, and answers it fast — is this item present? Adding an item, removing it, and testing whether it is in the set each cost"
      }
    },
    "heap": {
      "prerequisiteReasons": {
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: The clever trick: store the tree as a flat array, with no pointers. You might expect a tree to need explicit links — each node remembering where its children live in memory. A heap needs none. Because the tree is complete, you can lay its nodes into a plain array (an indexed"
      }
    },
    "hessian": {
      "prerequisiteReasons": {
        "jacobian": "The stated objective requires Jacobian (J): the complete explanation applies it in this mechanism: It is the jacobian of the gradient $\\nabla f$:",
        "gradient": "The stated objective requires Gradient: the complete explanation applies it in this mechanism: It is the jacobian of the gradient $\\nabla f$:",
        "partial-derivative": "The stated objective requires Partial derivative: the complete explanation applies it in this mechanism: Each entry is a second partial-derivative — differentiate $f$ once with respect to $xj$, then again with respect to $xi$."
      }
    },
    "hierarchical-bayes": {
      "prerequisiteReasons": {
        "bayes-rule": "The stated objective requires Bayes' Rule: the complete explanation applies it in this mechanism: Plain bayes-rule turns a prior belief about an unknown θ into a posterior by reweighting it with the data — but it assumes you already fixed the prior. Often the prior itself has knobs (its hyper-parameters) you are unsure about. The tempting fix — peek at the data, tune those",
        "beta-distribution": "The stated objective requires Beta Distribution: the complete explanation applies it in this mechanism: A prior is rarely a single curve; it is usually a named shape with adjustable parameters. Take the running case: θ is the bias p of a coin — the probability it lands heads, a number in (0,1). A natural prior over a probability is the beta-distribution, written Beta(α, β). It is",
        "likelihood": "The stated objective requires Likelihood: the complete explanation applies it in this mechanism: Now build one enlarged unknown out of all the things you do not know — here (p, α, β) — and apply bayes-rule to that whole bundle at once. The likelihood P(D | p) (how probable the observed flips are given the coin's bias) and the two stacked prior factors combine:"
      }
    },
    "http": {
      "prerequisiteReasons": {
        "tcp": "The stated objective requires TCP: the complete explanation applies it in this mechanism: HTTP (HyperText Transfer Protocol) is the application protocol of the web. It is a request–response protocol: a client sends one self-contained message asking a server to do something to a named resource — \"GET me /index.html\", \"POST this form to /orders\" — and the server sends",
        "dns": "The stated objective requires DNS: the complete explanation applies it in this mechanism: 1. Name → address. The URL names the server by hostname, so the client first asks dns to resolve example.com → 93.184.216.34. 2. Connect. The client opens a tcp connection to that address, port 80 (the HTTP default). 3. Request. It writes the request above — method GET, path"
      }
    },
    "huber-loss": {
      "prerequisiteReasons": {
        "loss-function": "The stated objective requires Loss function: the complete explanation applies it in this mechanism: As a loss-function, Huber loss changes how strongly each residual affects fitting. Its derivative with respect to $r$ is",
        "derivative": "The stated objective requires Derivative: the complete explanation applies it in this mechanism: As a loss-function, Huber loss changes how strongly each residual affects fitting. Its derivative with respect to $r$ is",
        "convexity": "The stated objective requires Convexity: the complete explanation applies it in this mechanism: For $\\delta=2$, residuals $1$ and $5$ have losses $0.5$ and $2(5-1)=8$. Squared loss would give $0.5$ and $12.5$. Both Huber pieces meet at $|r|=2$ with the same value and slope. The derivative never decreases as $r$ increases, so the objective has convexity and is continuously"
      }
    },
    "hybrid-lexical-semantic-retrieval": {
      "prerequisiteReasons": {
        "lexical-retrieval": "The stated objective requires Lexical Retrieval: the complete explanation applies it in this mechanism: Hybrid lexical–semantic retrieval combines lexical-retrieval with embedding-based nearest-neighbor-search, then uses a fusion rule such as reciprocal-rank-fusion to produce one ranking.",
        "nearest-neighbor-search": "The stated objective requires Nearest-Neighbor Search: the complete explanation applies it in this mechanism: Hybrid lexical–semantic retrieval combines lexical-retrieval with embedding-based nearest-neighbor-search, then uses a fusion rule such as reciprocal-rank-fusion to produce one ranking.",
        "reciprocal-rank-fusion": "The stated objective requires Reciprocal Rank Fusion: the complete explanation applies it in this mechanism: Hybrid lexical–semantic retrieval combines lexical-retrieval with embedding-based nearest-neighbor-search, then uses a fusion rule such as reciprocal-rank-fusion to produce one ranking."
      }
    },
    "hypervisor": {
      "prerequisiteReasons": {
        "kernel": "The stated objective requires Kernel: the complete explanation applies it in this mechanism: A hypervisor (also called a virtual machine monitor) is software that slices one physical computer into several virtual machines (VMs). Each VM is a make-believe computer: it boots and runs its own complete operating system, including its own kernel — the privileged resident"
      }
    },
    "image-preference-modeling": {
      "prerequisiteReasons": {
        "text-image-attribute-binding": "The stated objective requires Text–Image Attribute Binding: the complete explanation applies it in this mechanism: Preference conflates dimensions unless the protocol separates them. A polished image may ignore the prompt, while an accurate image may have weak aesthetics. Measure prompt adherence—including text-image-attribute-binding—aesthetics, artifacts, diversity, safety, and pairwise",
        "model-calibration": "The stated objective requires Model Calibration: the complete explanation applies it in this mechanism: Use blinded randomized comparisons, explicit ties, multiple raters, agreement statistics, held-out prompt and style slices, and confidence intervals. Audit model-calibration and ranking consistency under transformations that should not change preference. Recheck with people"
      }
    },
    "inference-cost-break-even": {
      "prerequisiteReasons": {
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: Let $CL$ be the local system's monthly fixed cost: hardware rental or depreciation, power, storage, networking, and attributable operations. Let $r$ be its measured active throughput in accepted tokens per second, $T$ the seconds in the billing period, and $u$ the fraction of",
        "continuous-batching": "The stated objective requires Continuous Batching: the complete explanation applies it in this mechanism: Throughput $r$ is not a hardware nameplate value. It depends on the workload and serving policy. continuous-batching can increase it by replacing completed sequences with queued work so expensive weight loads serve more live tokens. prefix-caching can avoid repeated prefill for",
        "prefix-caching": "The stated objective requires Prefix Caching: the complete explanation applies it in this mechanism: Throughput $r$ is not a hardware nameplate value. It depends on the workload and serving policy. continuous-batching can increase it by replacing completed sequences with queued work so expensive weight loads serve more live tokens. prefix-caching can avoid repeated prefill for",
        "latency-percentile": "The stated objective requires Latency Percentiles (Tail Latency): the complete explanation applies it in this mechanism: A metered inference API charges for each input, cached-input, and output token, while a self-hosted server costs money even when no request is running. Inference cost break-even is the sustained fraction of time that the local system must do useful work before its fixed monthly"
      }
    },
    "init-process": {
      "prerequisiteReasons": {
        "process": "The stated objective requires Process: the complete explanation applies it in this mechanism: The init process is the first process that runs in userspace — the very first ordinary program the kernel launches at the end of boot — and it is given the identifier PID 1. (\"Init\" is short for initialization; \"PID\" is the unique number that names a process.) The kernel creates"
      }
    },
    "inode": {
      "prerequisiteReasons": {
        "vfs": "The stated objective requires Virtual File System: the complete explanation applies it in this mechanism: An inode is the single object — living both on disk and, while in use, cached in kernel memory — that holds everything about a file except its name. That \"everything\" is of two kinds. First, the file's metadata: its type (regular file, directory, device), its permission bits,"
      }
    },
    "intermediate-representation": {
      "prerequisiteReasons": {
        "graph": "The stated objective requires Graph (Adjacency List): the complete explanation applies it in this mechanism: Many IRs describe structure as a graph. Vertices record operations or objects, edges record relationships or value flow, and metadata fixes details that names alone cannot express. The IR is not necessarily executable by itself. Its purpose is to preserve meaning across the"
      }
    },
    "interrupt": {
      "prerequisiteReasons": {
        "kernel": "The stated objective requires Kernel: the complete explanation applies it in this mechanism: An interrupt is an electrical signal a hardware device sends to the CPU that preempts whatever the CPU is currently doing — that is, it forces the CPU to stop in the middle of the instruction stream it was running and immediately jump elsewhere. Where it jumps is into the"
      }
    },
    "inverted-index": {
      "prerequisiteReasons": {
        "hash-map": "The stated objective requires Hash Map: the complete explanation applies it in this mechanism: The term dictionary can use a hash-map from a normalized term to its postings. A posting records a document identifier and may also store term frequency and positions. Query work then grows with the matching postings rather than with every document in the collection."
      }
    },
    "io-interconnect": {
      "prerequisiteReasons": {
        "device-driver": "The stated objective requires Device Driver: the complete explanation applies it in this mechanism: An I/O interconnect connects processors to peripheral devices through links and optional switches, carrying addressed requests, completions, and data between a host and the hardware managed by a device-driver."
      }
    },
    "ip-routing": {
      "prerequisiteReasons": {
        "network-stack": "The stated objective requires Network Stack: the complete explanation applies it in this mechanism: IP routing is the decision the network layer of the network-stack makes on every outgoing packet: out which interface, and to which next device, do I send this so it gets one step closer to its final host? The network layer already stamps each packet with a destination host"
      }
    },
    "ito-integral": {
      "prerequisiteReasons": {
        "definite-integral": "The stated objective requires Definite Integral: the complete explanation applies it in this mechanism: This resembles a definite-integral, but interval width is replaced by a random Brownian increment. The left endpoint prevents $H{ti}$ from peeking at the future increment. Refining the partition defines the limit in mean square, measured with expectation.",
        "brownian-motion": "The stated objective requires Brownian Motion: the complete explanation applies it in this mechanism: The Itô integral $\\int0^T Ht\\,dWt$ accumulates a process $Ht$ against increments of brownian-motion, with each coefficient chosen using information available before the corresponding increment.",
        "expectation": "The stated objective requires Expectation: the complete explanation applies it in this mechanism: This resembles a definite-integral, but interval width is replaced by a random Brownian increment. The left endpoint prevents $H{ti}$ from peeking at the future increment. Refining the partition defines the limit in mean square, measured with expectation."
      }
    },
    "jacobian": {
      "prerequisiteReasons": {
        "partial-derivative": "The stated objective requires Partial derivative: the complete explanation applies it in this mechanism: The Jacobian of a vector-field is the square grid holding all of the field's first partial-derivatives — its full derivative, the single matrix that says, to first order, how the field stretches, spins, and shears the space around a point. Rows index the field's output",
        "matrix-trace": "The stated objective requires Matrix Trace: the complete explanation applies it in this mechanism: The Jacobian of a vector-field is the square grid holding all of the field's first partial-derivatives — its full derivative, the single matrix that says, to first order, how the field stretches, spins, and shears the space around a point. Rows index the field's output",
        "symmetric-antisymmetric-decomposition": "The stated objective requires Symmetric-Antisymmetric Decomposition: the complete explanation applies it in this mechanism: The Jacobian of a vector-field is the square grid holding all of the field's first partial-derivatives — its full derivative, the single matrix that says, to first order, how the field stretches, spins, and shears the space around a point. Rows index the field's output",
        "vector-field": "The stated objective requires Vector Field: the complete explanation applies it in this mechanism: The Jacobian of a vector-field is the square grid holding all of the field's first partial-derivatives — its full derivative, the single matrix that says, to first order, how the field stretches, spins, and shears the space around a point. Rows index the field's output",
        "differential": "The stated objective requires Differential (Total Derivative): the complete explanation applies it in this mechanism: So rows are indexed by output components, columns by input variables. That layout is the whole point: it is exactly the shape needed for $J$ to act as the best linear approximation of $F$ near a point — the multi-output generalization of the scalar differential, which is a"
      }
    },
    "jit-vs-aot-compilation": {
      "prerequisiteReasons": {
        "cuda-kernel": "The stated objective requires CUDA Kernel: the complete explanation applies it in this mechanism: A cuda-kernel is written by a human, but the GPU cannot run that source text — it can only run machine code for the exact chip in front of it. Somewhere between writing the kernel and the GPU executing it, a compiler must turn the source into that machine code. JIT vs AOT is the",
        "ptx": "The stated objective requires PTX (Parallel Thread Execution): the complete explanation applies it in this mechanism: They coexist — AOT with a JIT fallback. The cleanest demonstration that this is an axis, not a binary choice, is that one shipped program commonly does both at once. A program can pre-build finished machine code for the GPU generations it expects (AOT, instant for those) and",
        "cubin": "The stated objective requires Cubin and Fatbinary: the complete explanation applies it in this mechanism: - Fast, predictable startup, no warm-up. Because the machine code already exists, the very first launch is as fast as the thousandth. There is no compile pause anywhere — the cost was paid once, long ago, by the builder, and never reappears for the user. - You must pre-target"
      }
    },
    "joint-attention": {
      "prerequisiteReasons": {
        "transformer-attention": "The stated objective requires Transformer attention: the complete explanation applies it in this mechanism: - transformer-attention - multi-head-attention - text-image-attribute-binding - softmax",
        "multi-head-attention": "The stated objective requires Multi-Head Attention: the complete explanation applies it in this mechanism: - transformer-attention - multi-head-attention - text-image-attribute-binding - softmax",
        "text-image-attribute-binding": "The stated objective requires Text–Image Attribute Binding: the complete explanation applies it in this mechanism: Joint interaction may help text-image-attribute-binding, typography, or prompt comprehension, but it does not guarantee them. Longer combined sequences increase attention memory and operations, and observed gains may come from capacity, data, training objectives, or other",
        "softmax": "The stated objective requires Softmax: the complete explanation applies it in this mechanism: The softmax converts each query's scores into attention weights. Its attention matrix contains text–text, text–image, image–text, and image–image blocks unless masks remove some interactions. In cross-attention from image to text, only image queries receive the cross-modal"
      }
    },
    "k-means-clustering": {
      "prerequisiteReasons": {
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: - arithmetic"
      }
    },
    "k6": {
      "prerequisiteReasons": {
        "load-testing": "The stated objective requires Load Testing: the complete explanation applies it in this mechanism: k6 is an open-source load-testing tool built by Grafana Labs. Its design splits the two halves of a load test between two languages: the workload — what one simulated client does — is a small JavaScript file, while the load engine that runs thousands of copies of it concurrently",
        "http": "The stated objective requires HTTP: the complete explanation applies it in this mechanism: k6 is an open-source load-testing tool built by Grafana Labs. Its design splits the two halves of a load test between two languages: the workload — what one simulated client does — is a small JavaScript file, while the load engine that runs thousands of copies of it concurrently",
        "latency-percentile": "The stated objective requires Latency Percentiles (Tail Latency): the complete explanation applies it in this mechanism: k6 is an open-source load-testing tool built by Grafana Labs. Its design splits the two halves of a load test between two languages: the workload — what one simulated client does — is a small JavaScript file, while the load engine that runs thousands of copies of it concurrently",
        "thread": "The stated objective requires Thread: the complete explanation applies it in this mechanism: Why VUs are cheap — green threads, not OS threads. The classic thread-per-user design (each simulated user = one OS thread) hits a wall fast: each thread costs a megabyte-scale stack and a kernel context-switch to schedule, so thousands of users means gigabytes of stacks and a",
        "interpreter": "The stated objective requires Interpreter: the complete explanation applies it in this mechanism: Why JavaScript for the workload. A workload is behavior — log in, parse the response, pick a random item, post — and that wants a real programming language, familiar to the web developers whose services are under test. k6 embeds its own JavaScript interpreter, written in Go (the",
        "context-switch": "The stated objective requires Context Switch: the complete explanation applies it in this mechanism: Why VUs are cheap — green threads, not OS threads. The classic thread-per-user design (each simulated user = one OS thread) hits a wall fast: each thread costs a megabyte-scale stack and a kernel context-switch to schedule, so thousands of users means gigabytes of stacks and a"
      }
    },
    "kernel": {
      "prerequisiteReasons": {
        "memory-hierarchy": "The stated objective requires Memory Hierarchy: the complete explanation applies it in this mechanism: The kernel is the privileged core program of an operating system. A program is just a list of machine instructions plus its data; a process is one running program with its own private slice of memory; the CPU is the chip that executes instructions one after another. The kernel"
      }
    },
    "kernel-module": {
      "prerequisiteReasons": {
        "kernel": "The stated objective requires Kernel: the complete explanation applies it in this mechanism: A kernel module is a chunk of kernel code, packaged as a single file (by convention ending in .ko, for kernel object), that can be loaded into the already-running kernel at runtime and unloaded again without rebooting the machine. You add one with a loader command (insmod loads"
      }
    },
    "kernel-thread": {
      "prerequisiteReasons": {
        "process": "The stated objective requires Process: the complete explanation applies it in this mechanism: A kernel thread (kthread) is a schedulable task — it has a numeric identity and is run by the scheduler exactly like a process — but it lives entirely inside the operating-system kernel and owns no user memory. It never loads or executes a user program; it runs only the kernel's"
      }
    },
    "key-value": {
      "prerequisiteReasons": {
        "set": "The stated objective requires Set (membership): the complete explanation applies it in this mechanism: The object: a set of pairs with distinct keys. A key–value mapping is a set of ordered pairs (k, v) — the key k bound to its value v — subject to one constraint that makes it a mapping rather than just any set of pairs: no two pairs share a key. Because the keys are distinct,"
      }
    },
    "key-value-store": {
      "prerequisiteReasons": {
        "key-value": "The stated objective requires Key–Value Mapping: the complete explanation applies it in this mechanism: A key–value store (or key–value database) is a database whose data model is the key-value mapping: you store a value under a unique key and later fetch, overwrite, or delete it by naming that key — nothing else. What lifts it from the in-process mapping (a language's dict) to a",
        "atomic-operation": "The stated objective requires Atomic Operation: the complete explanation applies it in this mechanism: Staying correct under concurrency — atomicity per key. Many clients reach the same server and may touch the same key at the same instant: two clients both incrementing a counter stored under \"visits\", say. If their read-modify-write sequences interleave, one increment is lost. A"
      }
    },
    "kl-divergence": {
      "prerequisiteReasons": {
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: Kullback–Leibler divergence $D{KL}(P\\|Q)$ measures how differently two probability-distributions weight the same outcomes by taking, under $P$, the expectation of the log ratio $p/q$.",
        "expectation": "The stated objective requires Expectation: the complete explanation applies it in this mechanism: Kullback–Leibler divergence $D{KL}(P\\|Q)$ measures how differently two probability-distributions weight the same outcomes by taking, under $P$, the expectation of the log ratio $p/q$."
      }
    },
    "knowledge-distillation": {
      "prerequisiteReasons": {
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: A trained teacher neural-network maps an input to a probability-distribution over classes. Ordinary supervised training supplies one target class, but a teacher's complete distribution also says which wrong alternatives it considers similar. Distillation makes that distribution",
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: A trained teacher neural-network maps an input to a probability-distribution over classes. Ordinary supervised training supplies one target class, but a teacher's complete distribution also says which wrong alternatives it considers similar. Distillation makes that distribution",
        "loss-function": "The stated objective requires Loss function: the complete explanation applies it in this mechanism: A trained teacher neural-network maps an input to a probability-distribution over classes. Ordinary supervised training supplies one target class, but a teacher's complete distribution also says which wrong alternatives it considers similar. Distillation makes that distribution"
      }
    },
    "knowledge-graph": {
      "prerequisiteReasons": {
        "graph": "The stated objective requires Graph (Adjacency List): the complete explanation applies it in this mechanism: A knowledge graph is a graph put to a particular use: its vertices are entities — specific, identifiable real-world things (the person Ada Lovelace, the company Acme, the city Seoul) — and its edges are typed, directed relations that say how two entities are connected (worksAt,"
      }
    },
    "kubernetes-pod": {
      "prerequisiteReasons": {
        "container": "The stated objective requires Container: the complete explanation applies it in this mechanism: A Kubernetes Pod is the smallest unit Kubernetes schedules onto a machine. A Pod contains one or more tightly coupled containers that must run together. Those containers share one network identity supplied by container-networking: one IP address, one set of ports, and localhost",
        "container-networking": "The stated objective requires Container Networking: the complete explanation applies it in this mechanism: A Kubernetes Pod is the smallest unit Kubernetes schedules onto a machine. A Pod contains one or more tightly coupled containers that must run together. Those containers share one network identity supplied by container-networking: one IP address, one set of ports, and localhost"
      }
    },
    "kubernetes-service": {
      "prerequisiteReasons": {
        "kubernetes-pod": "The stated objective requires Kubernetes Pod: the complete explanation applies it in this mechanism: A Kubernetes Service gives a changing set of backend Pods one stable network identity. Clients use the Service's name and address instead of any Pod address. Kubernetes continuously records which Pods currently belong to the Service, and the cluster's traffic mechanism applies",
        "load-balancing": "The stated objective requires Load Balancing: the complete explanation applies it in this mechanism: A Kubernetes Service gives a changing set of backend Pods one stable network identity. Clients use the Service's name and address instead of any Pod address. Kubernetes continuously records which Pods currently belong to the Service, and the cluster's traffic mechanism applies",
        "dns": "The stated objective requires DNS: the complete explanation applies it in this mechanism: A client resolves the Service name through dns and connects to 10.96.0.20:80. The traffic mechanism chooses checkout-b and redirects that connection to 10.42.1.9:8080."
      }
    },
    "kv-cache": {
      "prerequisiteReasons": {
        "transformer-attention": "The stated objective requires Transformer attention: the complete explanation applies it in this mechanism: When a transformer generates text one token at a time, every step's transformer-attention needs the keys and values of all earlier tokens. Those don't change once computed, so recomputing them each step is pure waste. The KV-cache stores each token's K and V the first time"
      }
    },
    "kv-quantization": {
      "prerequisiteReasons": {
        "quantization": "The stated objective requires Quantization: the complete explanation applies it in this mechanism: The kv-cache — the stored keys and values of every past token — is normally held in 16-bit floats (FP16), and for long context or large batch it is the biggest thing in memory and the thing the model must re-read from memory on every decode step. KV-cache quantization shrinks it",
        "kv-cache": "The stated objective requires KV-cache: the complete explanation applies it in this mechanism: The kv-cache — the stored keys and values of every past token — is normally held in 16-bit floats (FP16), and for long context or large batch it is the biggest thing in memory and the thing the model must re-read from memory on every decode step. KV-cache quantization shrinks it"
      }
    },
    "label-smoothing": {
      "prerequisiteReasons": {
        "regularization": "The stated objective requires Regularization: the complete explanation applies it in this mechanism: Uniform smoothing sets $u(k)=1/K$. Training then evaluates the classification loss-function against $q$ rather than the one-hot target. The softmax model is discouraged from assigning all mass to the observed label, making label smoothing a form of target-side regularization.",
        "softmax": "The stated objective requires Softmax: the complete explanation applies it in this mechanism: Uniform smoothing sets $u(k)=1/K$. Training then evaluates the classification loss-function against $q$ rather than the one-hot target. The softmax model is discouraged from assigning all mass to the observed label, making label smoothing a form of target-side regularization.",
        "loss-function": "The stated objective requires Loss function: the complete explanation applies it in this mechanism: Uniform smoothing sets $u(k)=1/K$. Training then evaluates the classification loss-function against $q$ rather than the one-hot target. The softmax model is discouraged from assigning all mass to the observed label, making label smoothing a form of target-side regularization.",
        "model-calibration": "The stated objective requires Model Calibration: the complete explanation applies it in this mechanism: Smoothing can improve optimization or generalization, but it changes the meaning of learned scores. It does not universally worsen model-calibration; empirical work has found improved calibration in some settings while also finding reduced information useful for teacher–student",
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: Uniform smoothing encodes a uniform reference probability-distribution, which may be inappropriate under structured or cost-sensitive labels. Alternatives include class-dependent priors, data augmentation, weight decay, focal objectives, and post-hoc calibration; they solve"
      }
    },
    "laplacian": {
      "prerequisiteReasons": {
        "divergence": "The stated objective requires Divergence (∇·F): the complete explanation applies it in this mechanism: Take the gradient of $f$, then its divergence:",
        "gradient": "The stated objective requires Gradient: the complete explanation applies it in this mechanism: Take the gradient of $f$, then its divergence:"
      }
    },
    "late-interaction-retrieval": {
      "prerequisiteReasons": {
        "embedding": "The stated objective requires Embedding: the complete explanation applies it in this mechanism: Late-interaction retrieval encodes a query and a document independently into multiple embedding vectors, then delays their interaction until ranking. It retains token-level matching signals without running one joint model over every query-document pair.",
        "vector-dot-product": "The stated objective requires Vector dot product: the complete explanation applies it in this mechanism: Here $qi \\cdot dj$ is the vector-dot-product similarity between one query token and one document token. For each query token, the score keeps its strongest document-token match; summing those maxima rewards a document that covers multiple parts of the query. The document vectors",
        "nearest-neighbor-search": "The stated objective requires Nearest-Neighbor Search: the complete explanation applies it in this mechanism: A single-vector retriever compresses each document into one embedding before search. That makes nearest-neighbor-search efficient, but distinct clues inside a long document must share one vector. A fully joint reranker preserves detailed query-document interactions, but it must"
      }
    },
    "latency-percentile": {
      "prerequisiteReasons": {
        "cumulative-distribution-function": "The stated objective requires Cumulative Distribution Function: the complete explanation applies it in this mechanism: Measure the response time of every request a service handles and you get not one number but a distribution. A latency percentile summarizes it: pN (the N-th percentile) is the time under which N% of requests completed — p50 = 20 ms means half the requests finished within 20 ms;"
      }
    },
    "latent-variable-model": {
      "prerequisiteReasons": {
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: A latent-variable model introduces an unobserved random variable $Z$ that helps generate observed data $X$, defining their joint probability-distribution and obtaining the observed-data probability by marginalizing $Z$.",
        "likelihood": "The stated objective requires Likelihood: the complete explanation applies it in this mechanism: where $p(z)$ is a prior and $p\\theta(x\\mid z)$ is the likelihood of an observation given the latent value. The observed-data density is"
      }
    },
    "layer-normalization": {
      "prerequisiteReasons": {
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: Layer normalization normalizes one activation vector at a time: it centers that vector's features around their own mean and scales them by their own spread. Learned scale and offset parameters then let a neural-network keep whatever feature-wise range is useful."
      }
    },
    "leader-election": {
      "prerequisiteReasons": {
        "replication": "The stated objective requires Replication: the complete explanation applies it in this mechanism: Leader-based replication hinges on there being exactly one primary: one node sequences all writes, and that single sequence is what keeps replicas identical. So the moment the primary's machine dies, the system faces a question it must answer by itself: who is the new primary?"
      }
    },
    "lexical-retrieval": {
      "prerequisiteReasons": {
        "inverted-index": "The stated objective requires Inverted Index: the complete explanation applies it in this mechanism: Lexical retrieval returns documents based on explicit overlap between normalized query terms and indexed document terms, using an inverted-index for candidates and a ranking function such as bm25 for relevance.",
        "bm25": "The stated objective requires BM25: the complete explanation applies it in this mechanism: Lexical retrieval returns documents based on explicit overlap between normalized query terms and indexed document terms, using an inverted-index for candidates and a ranking function such as bm25 for relevance."
      }
    },
    "likelihood": {
      "prerequisiteReasons": {
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: A probability-distribution $P(D \\mid \\theta)$ answers \"given the setting $\\theta$, how probable is each possible data outcome $D$?\" — here $\\theta$ (Greek \"theta\") is a parameter, a number (or set of numbers) that pins down which distribution we mean, and $D$ stands for the"
      }
    },
    "linear-independence": {
      "prerequisiteReasons": {
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: Vectors $v1,\\dots,vk$ are linearly independent if the only way to combine them (scalar-multiply and add — i.e. arithmetic) to reach zero is the trivial one:"
      }
    },
    "linear-least-squares": {
      "prerequisiteReasons": {
        "matrix-multiplication": "The stated objective requires Matrix Multiplication: the complete explanation applies it in this mechanism: For $A\\in\\mathbb R^{m\\times n}$ and $b\\in\\mathbb R^m$, the residual is $r=b-Ax$, where $Ax$ is matrix-multiplication. Expanding the squared Euclidean norm gives $r^Tr$, a vector-dot-product. If the columns of $A$ have full matrix-rank, the minimizing coefficient vector is",
        "vector-dot-product": "The stated objective requires Vector dot product: the complete explanation applies it in this mechanism: For $A\\in\\mathbb R^{m\\times n}$ and $b\\in\\mathbb R^m$, the residual is $r=b-Ax$, where $Ax$ is matrix-multiplication. Expanding the squared Euclidean norm gives $r^Tr$, a vector-dot-product. If the columns of $A$ have full matrix-rank, the minimizing coefficient vector is",
        "matrix-rank": "The stated objective requires Matrix rank: the complete explanation applies it in this mechanism: For $A\\in\\mathbb R^{m\\times n}$ and $b\\in\\mathbb R^m$, the residual is $r=b-Ax$, where $Ax$ is matrix-multiplication. Expanding the squared Euclidean norm gives $r^Tr$, a vector-dot-product. If the columns of $A$ have full matrix-rank, the minimizing coefficient vector is",
        "linear-independence": "The stated objective requires Linear independence: the complete explanation applies it in this mechanism: - matrix-multiplication - vector-dot-product - matrix-rank - linear-independence"
      }
    },
    "linear-programming": {
      "prerequisiteReasons": {
        "linear-transformation": "The stated objective requires Linear Transformation: the complete explanation applies it in this mechanism: The vector $c$ assigns a marginal cost to each decision. The products $A{ub}x$ and $A{eq}x$ use matrix-multiplication to express capacity limits, resource balances, or other coupled constraints. A row of an equality constraint is a linear-transformation of the decision vector",
        "matrix-multiplication": "The stated objective requires Matrix Multiplication: the complete explanation applies it in this mechanism: The vector $c$ assigns a marginal cost to each decision. The products $A{ub}x$ and $A{eq}x$ use matrix-multiplication to express capacity limits, resource balances, or other coupled constraints. A row of an equality constraint is a linear-transformation of the decision vector"
      }
    },
    "linear-transformation": {
      "prerequisiteReasons": {
        "matrix-multiplication": "The stated objective requires Matrix Multiplication: the complete explanation applies it in this mechanism: Composing maps corresponds to matrix-multiplication. If $TA(x)=Ax$ and $TB(x)=Bx$, then applying $B$ first and $A$ second gives $TA(TB(x))=(AB)x$. The order therefore matters.",
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: - matrix-multiplication - arithmetic"
      }
    },
    "linked-list": {
      "prerequisiteReasons": {
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: Why this is worth contrasting. The natural alternative is to lay the values out in one continuous run of equally sized slots — slot 0, then slot 1 right after it, then slot 2, and so on (a contiguous array). Because the slots are the same size and packed back-to-back, you can"
      }
    },
    "llm-as-a-judge": {
      "prerequisiteReasons": {
        "transformer-attention": "The stated objective requires Transformer attention: the complete explanation applies it in this mechanism: A judging record should contain the task input, candidate output or pair, rubric, reference answer when available, judge model and version, prompt, decoding settings, and structured verdict. The judge processes these tokens through transformer-attention and may return a scalar",
        "measurement": "The stated objective requires Measurement: the complete explanation applies it in this mechanism: LLM-as-a-judge evaluation uses one language model to score, rank, or critique outputs from another system. It scales qualitative evaluation beyond manual review, but its verdict is a proxy measurement, not ground truth.",
        "model-calibration": "The stated objective requires Model Calibration: the complete explanation applies it in this mechanism: Calibrate the judge against blinded human labels on the target task. Report agreement and confusion by rubric dimension, model family, length, language, and difficulty; model-calibration matters when numeric judge confidence is used as a probability. Keep a human-reviewed"
      }
    },
    "load-balancing": {
      "prerequisiteReasons": {
        "ip-routing": "The stated objective requires IP Routing: the complete explanation applies it in this mechanism: Ordinary ip-routing answers, for each packet, which next network interface and next device lead toward the packet's destination address. If every client addresses one server directly, that address also fixes which server receives the work. Adding a second server does not help"
      }
    },
    "load-testing": {
      "prerequisiteReasons": {
        "http": "The stated objective requires HTTP: the complete explanation applies it in this mechanism: Load testing is measuring a service's performance under controlled synthetic demand: instead of waiting for real users, a test program plays the part of many concurrent clients — issuing http requests against the real system — while recording two curves as the load rises:",
        "latency-percentile": "The stated objective requires Latency Percentiles (Tail Latency): the complete explanation applies it in this mechanism: Load testing is measuring a service's performance under controlled synthetic demand: instead of waiting for real users, a test program plays the part of many concurrent clients — issuing http requests against the real system — while recording two curves as the load rises:"
      }
    },
    "local-feature-attribution": {
      "prerequisiteReasons": {
        "expectation": "The stated objective requires Expectation: the complete explanation applies it in this mechanism: For real models, “feature missing” is not literal. A method may replace absent features using an expectation over background data. Different background populations or assumptions about dependence can therefore change the attributions. Correlated inputs are especially delicate:",
        "set": "The stated objective requires Set (membership): the complete explanation applies it in this mechanism: Global importance averages behavior across many examples; local attribution concerns one $x$. SHAP constructs local additive attributions from Shapley values. For a feature $i$, it averages the change in a value function when $i$ is added to every possible subset of the other",
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: Global importance averages behavior across many examples; local attribution concerns one $x$. SHAP constructs local additive attributions from Shapley values. For a feature $i$, it averages the change in a value function when $i$ is added to every possible subset of the other"
      }
    },
    "local-llm-inference": {
      "prerequisiteReasons": {
        "post-training-quantization": "The stated objective requires Post-Training Quantization: the complete explanation applies it in this mechanism: Feasibility begins with the full memory budget: weights, key–value cache, runtime buffers, and operating-system headroom. post-training-quantization can reduce weight memory, while context length, batch size, cache precision, and concurrent requests still determine substantial",
        "memory-mapped-io": "The stated objective requires Memory-Mapped I/O: the complete explanation applies it in this mechanism: Feasibility begins with the full memory budget: weights, key–value cache, runtime buffers, and operating-system headroom. post-training-quantization can reduce weight memory, while context length, batch size, cache precision, and concurrent requests still determine substantial",
        "inference-cost-break-even": "The stated objective requires Inference Cost Break-Even: the complete explanation applies it in this mechanism: Benchmark prompt processing and token generation separately, plus first-token and tail latency, throughput under concurrency, energy, memory pressure, quality at each compression setting, and failure recovery. Compare CLI, desktop, and serving runtimes by required interface and"
      }
    },
    "log-normal-distribution": {
      "prerequisiteReasons": {
        "change-of-variables": "The stated objective requires Change of Variables: the complete explanation applies it in this mechanism: A positive random quantity $X$ is log-normal when its logarithm is normal: $Y = \\ln X$ follows a normal-distribution with mean $\\mu$ and standard deviation $\\sigma$, or equivalently $X = e^{Y}$ is a normal-distribution passed through the exponential. It is the natural model for",
        "normal-distribution": "The stated objective requires Normal Distribution: the complete explanation applies it in this mechanism: A positive random quantity $X$ is log-normal when its logarithm is normal: $Y = \\ln X$ follows a normal-distribution with mean $\\mu$ and standard deviation $\\sigma$, or equivalently $X = e^{Y}$ is a normal-distribution passed through the exponential. It is the natural model for",
        "expectation": "The stated objective requires Expectation: the complete explanation applies it in this mechanism: The three \"centers\" are exactly the trio that skewness compares to read off a distribution's asymmetry: the mode (the density peak), the median (the value with half the probability on each side), and the mean (the expectation, the value-weighted balance point). A symmetric",
        "convexity": "The stated objective requires Convexity: the complete explanation applies it in this mechanism: A positive random quantity $X$ is log-normal when its logarithm is normal: $Y = \\ln X$ follows a normal-distribution with mean $\\mu$ and standard deviation $\\sigma$, or equivalently $X = e^{Y}$ is a normal-distribution passed through the exponential. It is the natural model for",
        "moment-generating-function": "The stated objective requires Moment Generating Function: the complete explanation applies it in this mechanism: A positive random quantity $X$ is log-normal when its logarithm is normal: $Y = \\ln X$ follows a normal-distribution with mean $\\mu$ and standard deviation $\\sigma$, or equivalently $X = e^{Y}$ is a normal-distribution passed through the exponential. It is the natural model for",
        "skewness": "The stated objective requires Skewness: the complete explanation applies it in this mechanism: A positive random quantity $X$ is log-normal when its logarithm is normal: $Y = \\ln X$ follows a normal-distribution with mean $\\mu$ and standard deviation $\\sigma$, or equivalently $X = e^{Y}$ is a normal-distribution passed through the exponential. It is the natural model for",
        "exponential-function": "The stated objective requires Exponential function: the complete explanation applies it in this mechanism: $X$ is called log-normal precisely because its log is normal. The name describes the relationship, not $X$ itself: it is the variable whose logarithm is the normal one. Because $e^{(\\cdot)}$ — the exponential-function — is always strictly positive, $X$ ranges only over the",
        "moment-skewness": "The stated objective requires Moment Skewness: the complete explanation applies it in this mechanism: But skewness is careful to call that ordering a rule of thumb, not a theorem — under the weak measures of skew (the third moment (moment-skewness), or the nonparametric gap $(\\text{mean}-\\text{median})/\\sigma$) the mode is unreliable and the order can scramble for multimodal or"
      }
    },
    "log-record": {
      "prerequisiteReasons": {
        "event": "The stated objective requires Event: the complete explanation applies it in this mechanism: A log record captures an event at a time. Its structured form combines well-known fields—timestamp, severity, body, and correlation IDs—with arbitrary key-value attributes that describe the source and occurrence.",
        "key-value": "The stated objective requires Key–Value Mapping: the complete explanation applies it in this mechanism: A log record captures an event at a time. Its structured form combines well-known fields—timestamp, severity, body, and correlation IDs—with arbitrary key-value attributes that describe the source and occurrence."
      }
    },
    "lora": {
      "prerequisiteReasons": {
        "low-rank-factorization": "The stated objective requires Low-rank factorization: the complete explanation applies it in this mechanism: The whole idea rests on low-rank-factorization: instead of updating all d² entries of W, LoRA assumes the change needed to adapt the model is itself low-rank, so it represents ΔW as B·A with a tiny r. Training touches only 2dr parameters while W stays fixed.",
        "fine-tuning": "The stated objective requires Fine-tuning: the complete explanation applies it in this mechanism: Where these weight matrices live inside the model is now grounded in transformer-attention — LoRA injects its low-rank update into the query and value projections ($Wq$, $Wv$). What is being adapted — fine-tuning — is the one prerequisite of LoRA still on the frontier.",
        "transformer-attention": "The stated objective requires Transformer attention: the complete explanation applies it in this mechanism: Where these weight matrices live inside the model is now grounded in transformer-attention — LoRA injects its low-rank update into the query and value projections ($Wq$, $Wv$). What is being adapted — fine-tuning — is the one prerequisite of LoRA still on the frontier."
      }
    },
    "loss-function": {
      "prerequisiteReasons": {
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: It maps the model's parameters $\\theta$ to a non-negative error computed from predictions versus targets (sums, differences, products — arithmetic):",
        "maximum-likelihood-estimation": "The stated objective requires Maximum Likelihood Estimation: the complete explanation applies it in this mechanism: Where the loss comes from. A principled choice of $\\ell$ falls out of maximum-likelihood-estimation: the cross-entropy loss is the negative log-likelihood of the targets under the model, so minimizing it is exactly maximizing the likelihood of the data — $\\mathcal{L}(\\theta) =",
        "likelihood": "The stated objective requires Likelihood: the complete explanation applies it in this mechanism: Where the loss comes from. A principled choice of $\\ell$ falls out of maximum-likelihood-estimation: the cross-entropy loss is the negative log-likelihood of the targets under the model, so minimizing it is exactly maximizing the likelihood of the data — $\\mathcal{L}(\\theta) ="
      }
    },
    "lost-in-the-middle": {
      "prerequisiteReasons": {
        "retrieval-augmented-generation": "The stated objective requires Retrieval-Augmented Generation: the complete explanation applies it in this mechanism: Retrieval-augmented-generation must fit selected evidence into a finite context-window. It is tempting to treat that window as a neutral budget: retrieve more passages, concatenate them, and expect relevant evidence to remain equally usable wherever it lands. Position-sensitive",
        "context-window": "The stated objective requires Context Window: the complete explanation applies it in this mechanism: Retrieval-augmented-generation must fit selected evidence into a finite context-window. It is tempting to treat that window as a neutral budget: retrieve more passages, concatenate them, and expect relevant evidence to remain equally usable wherever it lands. Position-sensitive"
      }
    },
    "low-rank-factorization": {
      "prerequisiteReasons": {
        "matrix-rank": "The stated objective requires Matrix rank: the complete explanation applies it in this mechanism: By matrix-rank, a product like B·A cannot have more independent directions than its thinnest dimension r, so choosing a small r deliberately constrains the result to low rank. We accept some error in exchange for storing 2dr parameters instead of d² — a big saving when d is"
      }
    },
    "lua": {
      "prerequisiteReasons": {
        "bytecode-vm": "The stated objective requires Bytecode Virtual Machine: the complete explanation applies it in this mechanism: Lua is a small, fast scripting language designed above all to be embedded inside a larger host program — game engines, web servers, and text editors — as its configuration and scripting layer. Everything about it serves that goal: a tiny implementation, and a handful of concepts",
        "garbage-collection": "The stated objective requires Garbage Collection: the complete explanation applies it in this mechanism: Lua is a small, fast scripting language designed above all to be embedded inside a larger host program — game engines, web servers, and text editors — as its configuration and scripting layer. Everything about it serves that goal: a tiny implementation, and a handful of concepts",
        "dynamic-typing": "The stated objective requires Dynamic Typing: the complete explanation applies it in this mechanism: Lua is a small, fast scripting language designed above all to be embedded inside a larger host program — game engines, web servers, and text editors — as its configuration and scripting layer. Everything about it serves that goal: a tiny implementation, and a handful of concepts",
        "closure": "The stated objective requires Closure: the complete explanation applies it in this mechanism: Lua is a small, fast scripting language designed above all to be embedded inside a larger host program — game engines, web servers, and text editors — as its configuration and scripting layer. Everything about it serves that goal: a tiny implementation, and a handful of concepts",
        "coroutine": "The stated objective requires Coroutine: the complete explanation applies it in this mechanism: Lua is a small, fast scripting language designed above all to be embedded inside a larger host program — game engines, web servers, and text editors — as its configuration and scripting layer. Everything about it serves that goal: a tiny implementation, and a handful of concepts",
        "metatable": "The stated objective requires Metatable: the complete explanation applies it in this mechanism: Lua is a small, fast scripting language designed above all to be embedded inside a larger host program — game engines, web servers, and text editors — as its configuration and scripting layer. Everything about it serves that goal: a tiny implementation, and a handful of concepts",
        "hash-map": "The stated objective requires Hash Map: the complete explanation applies it in this mechanism: Lua is a small, fast scripting language designed above all to be embedded inside a larger host program — game engines, web servers, and text editors — as its configuration and scripting layer. Everything about it serves that goal: a tiny implementation, and a handful of concepts"
      }
    },
    "mac-vs-ip": {
      "prerequisiteReasons": {
        "network-stack": "The stated objective requires Network Stack: the complete explanation applies it in this mechanism: A single message crossing the network-stack is addressed twice, with two completely different kinds of address that answer two different questions, and the gap between them is how packets actually find their way across the world. The IP address (the network-layer / host address)"
      }
    },
    "mamba": {
      "prerequisiteReasons": {
        "state-space-model": "The stated objective requires State Space Model: the complete explanation applies it in this mechanism: Mamba is a selective state-space-model: it keeps the SSM's linear recurrence and its fixed-size state, but makes the input map $B$, the output map $C$, and the per-step \"step size\" $\\Delta$ functions of the current input $xt$ instead of fixed numbers. A plain SSM uses the same"
      }
    },
    "mapreduce": {
      "prerequisiteReasons": {
        "hash-map": "The stated objective requires Hash Map: the complete explanation applies it in this mechanism: The grouping key is the essential contract. A hash-map is a familiar single-machine analogy: it associates each key with the values accumulated for that key. MapReduce makes that association distributed, so the implementation must transfer intermediate partitions between workers"
      }
    },
    "markov-decision-process": {
      "prerequisiteReasons": {
        "probability": "The stated objective requires Probability: the complete explanation applies it in this mechanism: An MDP packages a sequential decision problem as the tuple $(S, A, P, R, \\gamma)$. $S$ is a set of states and $A$ is a set of actions. At time $t$, the agent observes state $St$, selects $At$, then the environment draws a next state $S{t+1}$ and reward $R{t+1}$. The transition",
        "set": "The stated objective requires Set (membership): the complete explanation applies it in this mechanism: An MDP packages a sequential decision problem as the tuple $(S, A, P, R, \\gamma)$. $S$ is a set of states and $A$ is a set of actions. At time $t$, the agent observes state $St$, selects $At$, then the environment draws a next state $S{t+1}$ and reward $R{t+1}$. The transition"
      }
    },
    "matrix-multiplication": {
      "prerequisiteReasons": {
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: - arithmetic - vector-dot-product",
        "vector-dot-product": "The stated objective requires Vector dot product: the complete explanation applies it in this mechanism: Each entry is the vector-dot-product of row $i$ of $A$ and column $j$ of $B$."
      }
    },
    "matrix-rank": {
      "prerequisiteReasons": {
        "matrix-multiplication": "The stated objective requires Matrix Multiplication: the complete explanation applies it in this mechanism: When we form products via matrix-multiplication, some rows or columns may be exact combinations of others and add no new direction. Rank counts the rows (or columns) that are not such combinations. A precise definition needs linear-independence, which is on the frontier; until",
        "linear-independence": "The stated objective requires Linear independence: the complete explanation applies it in this mechanism: When we form products via matrix-multiplication, some rows or columns may be exact combinations of others and add no new direction. Rank counts the rows (or columns) that are not such combinations. A precise definition needs linear-independence, which is on the frontier; until"
      }
    },
    "matrix-trace": {
      "prerequisiteReasons": {
        "matrix-multiplication": "The stated objective requires Matrix Multiplication: the complete explanation applies it in this mechanism: It is cyclic: tr(AB) = tr(BA). This one is not obvious, because the matrix products AB and BA are themselves usually different matrices. Recall from matrix-multiplication that the [i][i] entry of the product AB is the sum A[i][1]·B[1][i] + A[i][2]·B[2][i] + …, i.e. row i of A"
      }
    },
    "maximum-a-posteriori": {
      "prerequisiteReasons": {
        "maximum-likelihood-estimation": "The stated objective requires Maximum Likelihood Estimation: the complete explanation applies it in this mechanism: Maximum a posteriori (MAP) estimation is maximum-likelihood-estimation with one addition: a prior belief about the parameter, folded in before you look at the data. Recall that maximum-likelihood-estimation reports the parameter $\\theta$ that makes the observed data $D$ most",
        "bayes-rule": "The stated objective requires Bayes' Rule: the complete explanation applies it in this mechanism: Maximum a posteriori (MAP) estimation is maximum-likelihood-estimation with one addition: a prior belief about the parameter, folded in before you look at the data. Recall that maximum-likelihood-estimation reports the parameter $\\theta$ that makes the observed data $D$ most",
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: - $P(D \\mid \\theta)$ is the likelihood — the very thing MLE maximizes. For each candidate $\\theta$, how probable the data would be if that $\\theta$ held. - $P(\\theta)$ is the prior — the probability-distribution over $\\theta$ before seeing any data, the formal home for our",
        "likelihood": "The stated objective requires Likelihood: the complete explanation applies it in this mechanism: - $P(D \\mid \\theta)$ is the likelihood — the very thing MLE maximizes. For each candidate $\\theta$, how probable the data would be if that $\\theta$ held. - $P(\\theta)$ is the prior — the probability-distribution over $\\theta$ before seeing any data, the formal home for our"
      }
    },
    "maximum-likelihood-estimation": {
      "prerequisiteReasons": {
        "likelihood": "The stated objective requires Likelihood: the complete explanation applies it in this mechanism: Maximum likelihood estimation (MLE) is a recipe for turning observed data into a single best guess for an unknown parameter. The idea is a one-line principle: of all the parameter settings you could have used, pick the one under which the data you actually saw would have been",
        "derivative": "The stated objective requires Derivative: the complete explanation applies it in this mechanism: Maximum likelihood estimation (MLE) is a recipe for turning observed data into a single best guess for an unknown parameter. The idea is a one-line principle: of all the parameter settings you could have used, pick the one under which the data you actually saw would have been",
        "bernoulli-distribution": "The stated objective requires Bernoulli Distribution: the complete explanation applies it in this mechanism: Maximum likelihood estimation (MLE) is a recipe for turning observed data into a single best guess for an unknown parameter. The idea is a one-line principle: of all the parameter settings you could have used, pick the one under which the data you actually saw would have been",
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: 1. Write the likelihood $L(\\theta)$ as the product of the per-observation probabilities the model assigns — each factor $P(di \\mid \\theta)$ is read off the model's probability-distribution evaluated at the observed value $di$. 2. Take logs to get the log-likelihood"
      }
    },
    "measurement": {
      "prerequisiteReasons": {
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: The value makes observations comparable. Once three requests have durations of 120 ms, 150 ms, and 230 ms, ordinary arithmetic can combine them: the count is 3, the total is 500 ms, and the mean is $500 / 3 \\approx 166.7$ ms. The unit is load-bearing because adding 120"
      }
    },
    "memory-coalescing": {
      "prerequisiteReasons": {
        "warp": "The stated objective requires Warp: the complete explanation applies it in this mechanism: Memory coalescing is about the pattern in which the 32 threads of a warp read or write global memory — the large, slow off-chip space from gpu-memory-spaces. The hardware never services those 32 accesses one at a time; it fetches global memory only in fixed-width chunks called",
        "gpu-memory-spaces": "The stated objective requires GPU Memory Spaces: the complete explanation applies it in this mechanism: Memory coalescing is about the pattern in which the 32 threads of a warp read or write global memory — the large, slow off-chip space from gpu-memory-spaces. The hardware never services those 32 accesses one at a time; it fetches global memory only in fixed-width chunks called"
      }
    },
    "memory-mapped-io": {
      "prerequisiteReasons": {
        "virtual-memory": "The stated objective requires Virtual Memory: the complete explanation applies it in this mechanism: A device-driver first obtains a device resource range and maps it into the kernel's virtual-memory. Reading an address in that mapping asks the device for a register value; writing it may configure a queue, acknowledge an event, or ring a command doorbell.",
        "device-driver": "The stated objective requires Device Driver: the complete explanation applies it in this mechanism: A device-driver first obtains a device resource range and maps it into the kernel's virtual-memory. Reading an address in that mapping asks the device for a register value; writing it may configure a queue, acknowledge an event, or ring a command doorbell."
      }
    },
    "message-passing": {
      "prerequisiteReasons": {
        "parallel-process": "The stated objective requires Parallel process: the complete explanation applies it in this mechanism: A parallel-process has private memory: no other process can reach into it and read or write its variables. So if process A computes a value that process B needs, the value cannot simply \"appear\" in B — it has to be transmitted. Message passing is the discipline for doing that:"
      }
    },
    "metamorphic-testing": {
      "prerequisiteReasons": {
        "set": "The stated objective requires Set (membership): the complete explanation applies it in this mechanism: The acceptable transformations form a set chosen from domain knowledge. For a rotation-invariant image task, one test may require $|f(x)-f(T\\theta(x))|\\le\\epsilon$ for specified angles $\\theta$. For a price estimator, increasing only floor area might instead require a",
        "measurement": "The stated objective requires Measurement: the complete explanation applies it in this mechanism: A deterministic matrix makes failures reproducible: fixed examples crossed with named transformations, severities, and seeds. Report paired deltas and worst-case measurements, not only the average. A model can preserve mean accuracy while failing every example in one clinically"
      }
    },
    "metatable": {
      "prerequisiteReasons": {
        "hash-map": "The stated objective requires Hash Map: the complete explanation applies it in this mechanism: In a language whose one data structure is the table — a hash-map holding key-value pairs — a metatable is a second table hung off a first one to change what operations on the first table mean. Its entries are keyed by special event names (like index for \"a key was missing\" or",
        "key-value": "The stated objective requires Key–Value Mapping: the complete explanation applies it in this mechanism: In a language whose one data structure is the table — a hash-map holding key-value pairs — a metatable is a second table hung off a first one to change what operations on the first table mean. Its entries are keyed by special event names (like index for \"a key was missing\" or"
      }
    },
    "minimum-cost-flow": {
      "prerequisiteReasons": {
        "flow-network": "The stated objective requires Flow Network: the complete explanation applies it in this mechanism: Minimum-cost flow routes required supply through a flow-network while respecting capacities and conservation, and among all feasible routings chooses the one with the smallest sum of flow times unit cost.",
        "residual-network": "The stated objective requires Residual Network: the complete explanation applies it in this mechanism: A successive-shortest-path method starts with zero flow, views editable choices through the residual-network, and repeatedly sends as much as possible along a minimum-cost shortest-path from an unsatisfied supply to an unsatisfied demand. Reverse residual edges carry the",
        "shortest-path": "The stated objective requires Shortest Path: the complete explanation applies it in this mechanism: A successive-shortest-path method starts with zero flow, views editable choices through the residual-network, and repeatedly sends as much as possible along a minimum-cost shortest-path from an unsatisfied supply to an unsatisfied demand. Reverse residual edges carry the"
      }
    },
    "mixed-precision-training": {
      "prerequisiteReasons": {
        "gradient-descent": "The stated objective requires Gradient descent: the complete explanation applies it in this mechanism: Gradient scaling protects an update from underflow. Before backpropagation, multiply the loss by a scale $s$; the resulting gradients are multiplied by the same $s$. Before the optimizer applies gradient-descent, divide those gradients by $s$ to restore the intended update. If",
        "tensor": "The stated objective requires Tensor: the complete explanation applies it in this mechanism: Mixed-precision training combines lower-precision computation with selected higher-precision tensor state so hardware can reduce memory traffic and accelerate eligible operations without treating every numerical quantity as equally safe to narrow."
      }
    },
    "mixture-of-experts": {
      "prerequisiteReasons": {
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: A Mixture-of-Experts (MoE) layer replaces the transformer's single feed-forward block with many of them — each a small neural-network FFN with its own weights, called an expert — plus a router that, for every token, picks only the top-k experts to actually run. This decouples",
        "softmax": "The stated objective requires Softmax: the complete explanation applies it in this mechanism: 1. Route (the gate). A tiny linear layer scores the token's hidden vector $h$ against the experts and normalizes with softmax: $g = \\mathrm{softmax}(Wr\\,h) \\in \\mathbb{R}^{E}$ — a probability over experts. 2. Select top-k (sparsity). Keep only the $k$ largest gates (typically"
      }
    },
    "ml-system-freshness": {
      "prerequisiteReasons": {
        "concept-drift": "The stated objective requires Concept Drift: the complete explanation applies it in this mechanism: Freshness differs from concept-drift. Drift changes the relationship the model must learn; stale serving can fail even with unchanged model parameters because a user, item, or inventory state changed after precomputation. Retraining more often cannot repair an old cache",
        "measurement": "The stated objective requires Measurement: the complete explanation applies it in this mechanism: For a served artifact produced at time $ta$ and used at $ts$, define age $A=ts-ta$. A freshness service-level objective can require, for example, that 99% of recommendation candidate sets have $A\\le 6$ hours. This is a measurement of age, not a claim that six hours is"
      }
    },
    "mmu": {
      "prerequisiteReasons": {
        "page-table": "The stated objective requires Page Table: the complete explanation applies it in this mechanism: The memory management unit (MMU) is the piece of hardware inside the CPU that turns every virtual address a program uses — the addresses it computes and dereferences — into the physical address of an actual byte in RAM, and it does this on every single memory access. The",
        "virtual-memory": "The stated objective requires Virtual Memory: the complete explanation applies it in this mechanism: The memory management unit (MMU) is the piece of hardware inside the CPU that turns every virtual address a program uses — the addresses it computes and dereferences — into the physical address of an actual byte in RAM, and it does this on every single memory access. The",
        "page-fault": "The stated objective requires Page Fault: the complete explanation applies it in this mechanism: The memory management unit (MMU) is the piece of hardware inside the CPU that turns every virtual address a program uses — the addresses it computes and dereferences — into the physical address of an actual byte in RAM, and it does this on every single memory access. The"
      }
    },
    "model-calibration": {
      "prerequisiteReasons": {
        "conditional-probability": "The stated objective requires Conditional Probability: the complete explanation applies it in this mechanism: for every score value $p$ that the model emits. This is a conditional-probability statement: among cases whose score is $p$, the positive-event frequency must equal $p$."
      }
    },
    "model-free-learning": {
      "prerequisiteReasons": {
        "markov-decision-process": "The stated objective requires Markov Decision Process: the complete explanation applies it in this mechanism: Model-free learning uses experience—observed states, actions, rewards, and next states—to estimate which actions have high long-run return, without first learning the complete transition-and-reward model of a markov-decision-process. It can therefore learn from interaction when",
        "bellman-equation": "The stated objective requires Bellman Equation: the complete explanation applies it in this mechanism: The bellman-equation supplies the one-step target. If an estimate says that the next state is valuable, then a transition that reaches it with reward r should raise the current action’s estimated value toward r + γ × next-value; if the observed reward or continuation is worse,"
      }
    },
    "modularity-resolution-limit": {
      "prerequisiteReasons": {
        "graph-rag": "The stated objective requires GraphRAG: the complete explanation applies it in this mechanism: Graph-rag uses communities to make corpus-wide questions tractable, but a community is an index structure rather than ground truth. A common way to construct such groups is to optimize modularity, a score that prefers more edges inside a group than a randomized graph with the",
        "graph": "The stated objective requires Graph (Adjacency List): the complete explanation applies it in this mechanism: - graph-rag - graph"
      }
    },
    "moe-routing": {
      "prerequisiteReasons": {
        "mixture-of-experts": "The stated objective requires Mixture-of-Experts (MoE): the complete explanation applies it in this mechanism: Routing is the decision mechanism inside a mixture-of-experts layer that chooses which experts each token is sent to. A small learned matrix — the router (or gate) — scores every expert for the token, a softmax turns those scores into weights, and top-K selection sends the token",
        "softmax": "The stated objective requires Softmax: the complete explanation applies it in this mechanism: The router (gate). Each token enters the layer as a hidden vector $h$: the list of numbers the model currently holds for that token. The router is a single small learned matrix, written $W{\\text{gate}}$, with one row per expert. Multiplying it by the token gives one raw score"
      }
    },
    "moment-generating-function": {
      "prerequisiteReasons": {
        "expectation": "The stated objective requires Expectation: the complete explanation applies it in this mechanism: The moment generating function (MGF) of a random variable $X$ is a single function $MX(t) = E[e^{tX}]$ — for each real number $t$, it is the expectation of the exponential $e^{tX}$. It packs the entire shape of $X$ into one function of the dummy variable $t$. The name announces",
        "random-variable": "The stated objective requires Random Variable: the complete explanation applies it in this mechanism: Start from expectation: for a random-variable $X$, $E[g(X)]$ is the probability-weighted average of the transformed value $g(X)$, computed by averaging $g$ over $X$'s own distribution (the law of the unconscious statistician). The moment generating function picks one particular"
      }
    },
    "moment-skewness": {
      "prerequisiteReasons": {
        "skewness": "The stated objective requires Skewness: the complete explanation applies it in this mechanism: Moment skewness is the standard quantitative measure of skewness — it turns the lopsidedness of a distribution into a single signed number $\\gamma$ (Greek \"gamma\"). Where skewness is described qualitatively by comparing the three centers (mean, median, mode), moment skewness",
        "expectation": "The stated objective requires Expectation: the complete explanation applies it in this mechanism: Moment skewness is the standard quantitative measure of skewness — it turns the lopsidedness of a distribution into a single signed number $\\gamma$ (Greek \"gamma\"). Where skewness is described qualitatively by comparing the three centers (mean, median, mode), moment skewness"
      }
    },
    "momentum": {
      "prerequisiteReasons": {
        "gradient-descent": "The stated objective requires Gradient descent: the complete explanation applies it in this mechanism: Momentum changes gradient-descent so that an update remembers a decaying fraction of earlier updates. When successive gradients point in a consistent direction, the remembered velocity accumulates and moves faster; when gradients alternate across a narrow valley, their opposing"
      }
    },
    "mongodb": {
      "prerequisiteReasons": {
        "document-model": "The stated objective requires Document Data Model: the complete explanation applies it in this mechanism: MongoDB is a distributed document database — and like PostgreSQL, the point of treating it as its own concept is not any single subsystem, all of which already exist as the prerequisite nodes below. The point is the composition. Its data model is the document-model: records are",
        "b-tree": "The stated objective requires B-Tree / B+-Tree: the complete explanation applies it in this mechanism: MongoDB is a distributed document database — and like PostgreSQL, the point of treating it as its own concept is not any single subsystem, all of which already exist as the prerequisite nodes below. The point is the composition. Its data model is the document-model: records are",
        "database-index": "The stated objective requires Database Index: the complete explanation applies it in this mechanism: MongoDB is a distributed document database — and like PostgreSQL, the point of treating it as its own concept is not any single subsystem, all of which already exist as the prerequisite nodes below. The point is the composition. Its data model is the document-model: records are",
        "mvcc": "The stated objective requires Multi-Version Concurrency Control: the complete explanation applies it in this mechanism: MongoDB is a distributed document database — and like PostgreSQL, the point of treating it as its own concept is not any single subsystem, all of which already exist as the prerequisite nodes below. The point is the composition. Its data model is the document-model: records are",
        "write-ahead-logging": "The stated objective requires Write-Ahead Logging (WAL): the complete explanation applies it in this mechanism: MongoDB is a distributed document database — and like PostgreSQL, the point of treating it as its own concept is not any single subsystem, all of which already exist as the prerequisite nodes below. The point is the composition. Its data model is the document-model: records are",
        "transaction": "The stated objective requires Transaction (ACID): the complete explanation applies it in this mechanism: MongoDB is a distributed document database — and like PostgreSQL, the point of treating it as its own concept is not any single subsystem, all of which already exist as the prerequisite nodes below. The point is the composition. Its data model is the document-model: records are",
        "replication": "The stated objective requires Replication: the complete explanation applies it in this mechanism: MongoDB is a distributed document database — and like PostgreSQL, the point of treating it as its own concept is not any single subsystem, all of which already exist as the prerequisite nodes below. The point is the composition. Its data model is the document-model: records are",
        "leader-election": "The stated objective requires Leader Election: the complete explanation applies it in this mechanism: MongoDB is a distributed document database — and like PostgreSQL, the point of treating it as its own concept is not any single subsystem, all of which already exist as the prerequisite nodes below. The point is the composition. Its data model is the document-model: records are",
        "sharding": "The stated objective requires Sharding (Horizontal Partitioning): the complete explanation applies it in this mechanism: MongoDB is a distributed document database — and like PostgreSQL, the point of treating it as its own concept is not any single subsystem, all of which already exist as the prerequisite nodes below. The point is the composition. Its data model is the document-model: records are"
      }
    },
    "mpi-datatype": {
      "prerequisiteReasons": {
        "message-passing": "The stated objective requires Message passing: the complete explanation applies it in this mechanism: An MPI datatype is the part of a message that says what each element is. In message-passing one process hands some data to another, but the bytes alone do not say how to read them. MPI fixes this by describing every transfer as a triple: a buffer (a starting address in the"
      }
    },
    "multi-agent-orchestration": {
      "prerequisiteReasons": {
        "transformer-attention": "The stated objective requires Transformer attention: the complete explanation applies it in this mechanism: - transformer-attention - structured-output - measurement",
        "structured-output": "The stated objective requires Structured Output: the complete explanation applies it in this mechanism: An orchestrated system needs more than role labels. Specify each worker’s input and output contract, allowed tools, private and shared state, routing rules, stopping condition, retry budget, and authority. structured-output can make handoffs machine-checkable instead of relying",
        "measurement": "The stated objective requires Measurement: the complete explanation applies it in this mechanism: Compare orchestration with a single-worker baseline at equal model and tool budgets. measurement should include task success, independently verified correctness, tool failures, handoff loss, latency, token and external-service cost, retries, and safety violations. Keep multiple"
      }
    },
    "multi-armed-bandit": {
      "prerequisiteReasons": {
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: For $K$ actions with reward probability-distributions, a policy selects $At$ and observes reward $Rt$. In a stationary stochastic setting, expected cumulative regret through round $T$ is",
        "expectation": "The stated objective requires Expectation: the complete explanation applies it in this mechanism: where $\\mu^$ is the best action’s expectation. Regret measures opportunity loss relative to repeatedly choosing the best fixed action in hindsight or under the assumed model.",
        "measurement": "The stated objective requires Measurement: the complete explanation applies it in this mechanism: - probability-distribution - expectation - measurement"
      }
    },
    "multi-head-attention": {
      "prerequisiteReasons": {
        "transformer-attention": "The stated objective requires Transformer attention: the complete explanation applies it in this mechanism: One transformer-attention produces a single weighting of the tokens — so it can emphasize only one kind of relationship at a time. Multi-head attention runs $h$ attentions in parallel, each on its own learned slice of the representation, then glues the results back together.",
        "matrix-multiplication": "The stated objective requires Matrix Multiplication: the complete explanation applies it in this mechanism: What it is. Each head $i$ first projects the input into its own $d\\text{head}$-dimensional subspace via matrix-multiplication: $X Wq^i$, $X Wk^i$, $X Wv^i$. It then runs an ordinary transformer-attention there to get $\\text{head}i$ (shape $n\\times d\\text{head}$), and then the",
        "softmax": "The stated objective requires Softmax: the complete explanation applies it in this mechanism: - Head 1 (dims 1–2): query $q = [1,0]$; keys $k1 = [1,0],\\, k2 = [0,1]$; values $v1 = [2,0],\\, v2 = [0,2]$. Scores $q\\!\\cdot\\!k1 = 1,\\ q\\!\\cdot\\!k2 = 0$; scale by $\\sqrt{d\\text{head}} = \\sqrt2$ to get $[0.71,\\,0]$; softmax $\\to [0.67,\\,0.33]$. So $\\text{head}1 = 0.67\\,[2,0] +"
      }
    },
    "multi-head-latent-attention": {
      "prerequisiteReasons": {
        "multi-head-attention": "The stated objective requires Multi-Head Attention: the complete explanation applies it in this mechanism: The kv-cache is the memory bottleneck of generation, and in multi-head-attention it stores the keys and values of every head for every past token. Multi-head latent attention (MLA) shrinks that cache with a low-rank-factorization: instead of caching all the heads' K and V, it",
        "kv-cache": "The stated objective requires KV-cache: the complete explanation applies it in this mechanism: The kv-cache is the memory bottleneck of generation, and in multi-head-attention it stores the keys and values of every head for every past token. Multi-head latent attention (MLA) shrinks that cache with a low-rank-factorization: instead of caching all the heads' K and V, it",
        "low-rank-factorization": "The stated objective requires Low-rank factorization: the complete explanation applies it in this mechanism: The kv-cache is the memory bottleneck of generation, and in multi-head-attention it stores the keys and values of every head for every past token. Multi-head latent attention (MLA) shrinks that cache with a low-rank-factorization: instead of caching all the heads' K and V, it"
      }
    },
    "multimodal-rag": {
      "prerequisiteReasons": {
        "retrieval-augmented-generation": "The stated objective requires Retrieval-Augmented Generation: the complete explanation applies it in this mechanism: Multimodal retrieval-augmented generation extends retrieval-augmented-generation so indexed and retrieved evidence can include document-page images, figures, layouts, tables, audio, or other non-text representations.",
        "embedding": "The stated objective requires Embedding: the complete explanation applies it in this mechanism: A visually rich document can lose information when flattened into extracted text. One alternative renders each page as an image, computes one or more embedding vectors with a vision-language encoder, stores them in a vector-database, and retrieves pages for a query. A multimodal",
        "vector-database": "The stated objective requires Vector Database: the complete explanation applies it in this mechanism: A visually rich document can lose information when flattened into extracted text. One alternative renders each page as an image, computes one or more embedding vectors with a vision-language encoder, stores them in a vector-database, and retrieves pages for a query. A multimodal",
        "late-interaction-retrieval": "The stated objective requires Late-Interaction Retrieval: the complete explanation applies it in this mechanism: ColPali is one concrete design: it embeds document-page images into multiple vectors and uses late-interaction-retrieval between query and page vectors. That preserves token- or patch-level matching signals without requiring one pooled vector to represent the whole page. It is"
      }
    },
    "mvcc": {
      "prerequisiteReasons": {
        "concurrency-control": "The stated objective requires Concurrency Control: the complete explanation applies it in this mechanism: MVCC is the multi-version member of concurrency-control's optimistic family — the scheme PostgreSQL is built on. Its one defining idea is that an UPDATE never overwrites a row in place: it writes a new version of the row and leaves the old version intact. Each row version",
        "transaction-isolation": "The stated objective requires Transaction Isolation: the complete explanation applies it in this mechanism: MVCC is the multi-version member of concurrency-control's optimistic family — the scheme PostgreSQL is built on. Its one defining idea is that an UPDATE never overwrites a row in place: it writes a new version of the row and leaves the old version intact. Each row version"
      }
    },
    "namespace": {
      "prerequisiteReasons": {
        "process": "The stated objective requires Process: the complete explanation applies it in this mechanism: A namespace is a kernel feature that gives a process (and the children it creates) an isolated view of one class of system resource — so the process sees only its own slice of that resource and is blind to everything else of the same kind on the machine. The kernel keeps one"
      }
    },
    "nccl": {
      "prerequisiteReasons": {
        "all-reduce": "The stated objective requires All-reduce: the complete explanation applies it in this mechanism: NCCL (NVIDIA Collective Communications Library) is the software library that implements multi-GPU collective-operations — above all all-reduce, where every GPU contributes a value (its gradient), the values are summed, and the identical total is handed back to every GPU. NCCL is",
        "nvlink": "The stated objective requires NVLink: the complete explanation applies it in this mechanism: NCCL (NVIDIA Collective Communications Library) is the software library that implements multi-GPU collective-operations — above all all-reduce, where every GPU contributes a value (its gradient), the values are summed, and the identical total is handed back to every GPU. NCCL is",
        "collective-operation": "The stated objective requires Collective operation: the complete explanation applies it in this mechanism: NCCL (NVIDIA Collective Communications Library) is the software library that implements multi-GPU collective-operations — above all all-reduce, where every GPU contributes a value (its gradient), the values are summed, and the identical total is handed back to every GPU. NCCL is",
        "reduce-scatter": "The stated objective requires Reduce-scatter: the complete explanation applies it in this mechanism: Phase 1 — reduce-scatter. This phase ends with each GPU holding the complete sum of one chunk (a different chunk per GPU), rather than the whole result. It takes N−1 steps. At each step, every GPU simultaneously sends one chunk to its successor and receives a chunk from its",
        "all-gather": "The stated objective requires All-gather: the complete explanation applies it in this mechanism: Phase 2 — all-gather. Now the fully-summed chunks are scattered one-per-GPU; everyone needs all of them. This is the broadcast half of all-reduce, done as another N−1 steps around the same ring. Each GPU sends the finished chunk it owns to its successor, which stores it and"
      }
    },
    "nearest-neighbor-search": {
      "prerequisiteReasons": {
        "cosine-similarity": "The stated objective requires Cosine Similarity: the complete explanation applies it in this mechanism: Nearest-neighbor search answers one question over a stored collection: given a query, which stored items are most similar to it? Each item is held as an embedding — a vector whose position encodes meaning — and \"most similar\" is scored by cosine-similarity, how nearly two",
        "embedding": "The stated objective requires Embedding: the complete explanation applies it in this mechanism: Nearest-neighbor search answers one question over a stored collection: given a query, which stored items are most similar to it? Each item is held as an embedding — a vector whose position encodes meaning — and \"most similar\" is scored by cosine-similarity, how nearly two"
      }
    },
    "netfilter": {
      "prerequisiteReasons": {
        "network-stack": "The stated objective requires Network Stack: the complete explanation applies it in this mechanism: Netfilter is the part of the kernel that lets you intercept and act on packets while they move through the network-stack. Recall that a packet, on its way in or out, climbs or descends a fixed ladder of layers; netfilter plants a handful of hooks — checkpoints — at fixed points"
      }
    },
    "network-stack": {
      "prerequisiteReasons": {
        "socket": "The stated objective requires Socket: the complete explanation applies it in this mechanism: The network stack is the layered path that bytes written to a socket travel down, inside the kernel and out the wire, to reach a program on another machine — and the mirror-image path they climb back up at the far end. Going down, the data is wrapped layer by layer: each layer",
        "device-driver": "The stated objective requires Device Driver: the complete explanation applies it in this mechanism: 5. The physical layer is the network card (NIC) itself — driven by a device-driver, the kernel code that speaks the card's private register dialect — turning the frame's bits into a physical signal on the medium: changing voltage on copper wires, pulsing light in a glass fiber,",
        "file-descriptor": "The stated objective requires File Descriptor: the complete explanation applies it in this mechanism: Recall from socket that a program holds a network connection as a file-descriptor — a small integer ticket — and pushes bytes into it with send (or plain write) and pulls them out with recv (or read), exactly as if it were a file. That node deliberately stopped at the",
        "interrupt": "The stated objective requires Interrupt: the complete explanation applies it in this mechanism: 1. Physical / NIC: the card senses the voltage, light, or radio; its device-driver reassembles the bits into the frame, and (because the CPU was doing other things) raises an interrupt — a hardware nudge meaning \"a packet arrived, come handle it.\" 2. Link layer: reads the link"
      }
    },
    "neural-network": {
      "prerequisiteReasons": {
        "matrix-multiplication": "The stated objective requires Matrix Multiplication: the complete explanation applies it in this mechanism: Each layer multiplies its input by a weight matrix (matrix-multiplication), adds a bias, and applies a nonlinearity $\\sigma$:",
        "softmax": "The stated objective requires Softmax: the complete explanation applies it in this mechanism: Stacking layers composes these maps; the output layer often uses softmax to produce probabilities. The weight matrices $W^{(l)}$ are the parameters learned during training — and they are exactly what transformer-attention is built from and what lora adapts."
      }
    },
    "nonparametric-skew": {
      "prerequisiteReasons": {
        "skewness": "The stated objective requires Skewness: the complete explanation applies it in this mechanism: The nonparametric skew is the simplest robust number for the asymmetry studied in skewness: take the gap between the mean and the median, and divide it by the standard deviation,"
      }
    },
    "normal-distribution": {
      "prerequisiteReasons": {
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: The normal distribution (or Gaussian) is the most important continuous probability-distribution: a smooth, symmetric bell-shaped density curve. It is described by two numbers — the mean $\\mu$ (the center the bell sits over) and the standard deviation $\\sigma$ (how wide the bell",
        "exponential-function": "The stated objective requires Exponential function: the complete explanation applies it in this mechanism: The normal distribution (or Gaussian) is the most important continuous probability-distribution: a smooth, symmetric bell-shaped density curve. It is described by two numbers — the mean $\\mu$ (the center the bell sits over) and the standard deviation $\\sigma$ (how wide the bell"
      }
    },
    "numa-architecture": {
      "prerequisiteReasons": {
        "memory-hierarchy": "The stated objective requires Memory Hierarchy: the complete explanation applies it in this mechanism: A uniform shared-memory machine presents one memory cost. NUMA preserves one addressable memory space while adding distance. A processor reaches its local memory controller directly; reaching another node's memory crosses a socket-to-socket interconnect. NUMA is therefore an"
      }
    },
    "numa-locality": {
      "prerequisiteReasons": {
        "numa-memory-policy": "The stated objective requires NUMA Memory Policy: the complete explanation applies it in this mechanism: NUMA locality is the coordinated placement of execution and data on the same NUMA node: processor-affinity keeps work near a processor set, and numa-memory-policy places its pages near that set.",
        "processor-affinity": "The stated objective requires Processor Affinity: the complete explanation applies it in this mechanism: NUMA locality is the coordinated placement of execution and data on the same NUMA node: processor-affinity keeps work near a processor set, and numa-memory-policy places its pages near that set."
      }
    },
    "numa-memory-policy": {
      "prerequisiteReasons": {
        "numa-architecture": "The stated objective requires NUMA Architecture: the complete explanation applies it in this mechanism: On a numa-architecture, choosing where code runs is only half of locality; the operating system must also choose where newly allocated physical pages reside. Policies commonly express four intentions: use the local node, prefer one node but fall back, bind allocations to a set,",
        "page": "The stated objective requires Page: the complete explanation applies it in this mechanism: A NUMA memory policy tells the operating system which NUMA node or node set should supply the physical pages that back a task or region of virtual-memory.",
        "virtual-memory": "The stated objective requires Virtual Memory: the complete explanation applies it in this mechanism: A NUMA memory policy tells the operating system which NUMA node or node set should supply the physical pages that back a task or region of virtual-memory.",
        "page-fault": "The stated objective requires Page Fault: the complete explanation applies it in this mechanism: Policy applies when a physical page is allocated, often at the first page-fault. Existing pages normally stay where they are unless explicitly migrated. A virtual-memory range can therefore span pages on several NUMA nodes even though its addresses look contiguous to the process."
      }
    },
    "numeric-precision-formats": {
      "prerequisiteReasons": {
        "quantization": "The stated objective requires Quantization: the complete explanation applies it in this mechanism: A numeric precision format is the fixed recipe for laying out one number inside a small, fixed number of bits. It is the target a number is mapped into: where quantization is the procedure for rounding values onto a coarse grid and remembering a scale, a precision format is the"
      }
    },
    "nvlink": {
      "prerequisiteReasons": {
        "cpu-vs-gpu": "The stated objective requires CPU vs GPU: the complete explanation applies it in this mechanism: NVLink is a dedicated, high-bandwidth, low-latency wire that connects GPUs directly to each other (and, in some packages, a GPU directly to a CPU), so that data living in one GPU's memory can move into another's without detouring through the slow general-purpose bus that"
      }
    },
    "object-oriented-inheritance": {
      "prerequisiteReasons": {
        "namespace": "The stated objective requires Namespace: the complete explanation applies it in this mechanism: Test inherited behavior through the base contract, overrides, super() chains, initialization order, and multiple-inheritance diamonds. Inspect the actual method resolution order when behavior is ambiguous, and avoid relying on undocumented base-class internals or nominally",
        "python-descriptor": "The stated objective requires Python Descriptor: the complete explanation applies it in this mechanism: In Python, class Child(Base): ... records Base in the class hierarchy. Attribute lookup searches the derived class and then its method resolution order. A method call binds the selected function through the python-descriptor protocol. An override replaces lookup at the derived"
      }
    },
    "observability": {
      "prerequisiteReasons": {
        "telemetry-signal": "The stated objective requires Telemetry Signal: the complete explanation applies it in this mechanism: Observability is the ability to understand a running system's internal state from its externally available telemetry-signal outputs. It is not merely having data; the signals must let an operator form and test explanations for behavior that was not fully predicted in advance."
      }
    },
    "observability-instrumentation": {
      "prerequisiteReasons": {
        "telemetry-signal": "The stated objective requires Telemetry Signal: the complete explanation applies it in this mechanism: Observability instrumentation is the code or runtime integration that watches application activity and emits a telemetry-signal. It is the bridge between work occurring inside a program and evidence available outside it, giving the system observability.",
        "observability": "The stated objective requires Observability: the complete explanation applies it in this mechanism: Observability instrumentation is the code or runtime integration that watches application activity and emits a telemetry-signal. It is the bridge between work occurring inside a program and evidence available outside it, giving the system observability."
      }
    },
    "occupancy": {
      "prerequisiteReasons": {
        "streaming-multiprocessor": "The stated objective requires Streaming Multiprocessor: the complete explanation applies it in this mechanism: Occupancy is the ratio of warps actually resident on a streaming-multiprocessor (loaded and ready, in that node's sense) to the maximum number that SM is built to hold. An SM has fixed resources — a register file (fast on-chip storage holding every resident thread's working",
        "warp": "The stated objective requires Warp: the complete explanation applies it in this mechanism: Occupancy is the ratio of warps actually resident on a streaming-multiprocessor (loaded and ready, in that node's sense) to the maximum number that SM is built to hold. An SM has fixed resources — a register file (fast on-chip storage holding every resident thread's working",
        "shared-memory": "The stated objective requires Shared Memory: the complete explanation applies it in this mechanism: - A hard cap on resident slots. The SM has a fixed maximum number of resident warps (and blocks) it can track at all, regardless of how cheap each one is — 64 warps per SM on an H100. Even infinitely small warps cannot exceed this. - The register file. Every resident thread"
      }
    },
    "onnx": {
      "prerequisiteReasons": {
        "intermediate-representation": "The stated objective requires Intermediate representation: the complete explanation applies it in this mechanism: ONNX (Open Neural Network Exchange) is an open, runtime-agnostic intermediate-representation for exchanging machine-learning models. It packages a typed computation-graph with versioned operator-set imports so a model exported by one tool can be validated and executed or",
        "computation-graph": "The stated objective requires Computation graph: the complete explanation applies it in this mechanism: ONNX (Open Neural Network Exchange) is an open, runtime-agnostic intermediate-representation for exchanging machine-learning models. It packages a typed computation-graph with versioned operator-set imports so a model exported by one tool can be validated and executed or",
        "operator-set": "The stated objective requires Operator set: the complete explanation applies it in this mechanism: ONNX (Open Neural Network Exchange) is an open, runtime-agnostic intermediate-representation for exchanging machine-learning models. It packages a typed computation-graph with versioned operator-set imports so a model exported by one tool can be validated and executed or",
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: A trained neural-network has parameters and a forward calculation, but every training framework may store them differently. ONNX defines the common contract in the middle. A model records an IR version, imported operator-set domains and versions, metadata, and a",
        "graph": "The stated objective requires Graph (Adjacency List): the complete explanation applies it in this mechanism: A trained neural-network has parameters and a forward calculation, but every training framework may store them differently. ONNX defines the common contract in the middle. A model records an IR version, imported operator-set domains and versions, metadata, and a"
      }
    },
    "onnx-runtime": {
      "prerequisiteReasons": {
        "onnx": "The stated objective requires ONNX (Open Neural Network Exchange): the complete explanation applies it in this mechanism: ONNX Runtime is a cross-platform model accelerator that loads an onnx model, applies graph-optimization, partitions supported work among execution providers, and runs the resulting plan through one consistent API.",
        "graph-optimization": "The stated objective requires Graph optimization: the complete explanation applies it in this mechanism: ONNX Runtime is a cross-platform model accelerator that loads an onnx model, applies graph-optimization, partitions supported work among execution providers, and runs the resulting plan through one consistent API.",
        "execution-provider": "The stated objective requires Execution provider: the complete explanation applies it in this mechanism: ONNX Runtime is a cross-platform model accelerator that loads an onnx model, applies graph-optimization, partitions supported work among execution providers, and runs the resulting plan through one consistent API.",
        "graph": "The stated objective requires Graph (Adjacency List): the complete explanation applies it in this mechanism: ONNX specifies a portable model, but a specification does not itself allocate memory, choose hardware, or execute operations. ONNX Runtime supplies that execution layer. When an application creates a session, the runtime validates the onnx contract, builds an in-memory graph,"
      }
    },
    "oom-killer": {
      "prerequisiteReasons": {
        "demand-paging": "The stated objective requires Demand Paging: the complete explanation applies it in this mechanism: The OOM (out-of-memory) killer is the kernel's last resort for the one situation demand-paging cannot bluff its way out of: a page-fault demands a physical frame, every frame is already in use, and there is no way to free one — not by reclaiming caches, not by writing some page",
        "page-fault": "The stated objective requires Page Fault: the complete explanation applies it in this mechanism: The OOM (out-of-memory) killer is the kernel's last resort for the one situation demand-paging cannot bluff its way out of: a page-fault demands a physical frame, every frame is already in use, and there is no way to free one — not by reclaiming caches, not by writing some page",
        "swap": "The stated objective requires Swap: the complete explanation applies it in this mechanism: The OOM (out-of-memory) killer is the kernel's last resort for the one situation demand-paging cannot bluff its way out of: a page-fault demands a physical frame, every frame is already in use, and there is no way to free one — not by reclaiming caches, not by writing some page"
      }
    },
    "opentelemetry": {
      "prerequisiteReasons": {
        "observability": "The stated objective requires Observability: the complete explanation applies it in this mechanism: OpenTelemetry therefore enables observability but is not itself an observability backend. It creates and moves evidence; another system stores, queries, alerts on, and visualizes that evidence.",
        "telemetry-signal": "The stated objective requires Telemetry Signal: the complete explanation applies it in this mechanism: The flow begins with observability-instrumentation. Manual API calls or automatic library hooks turn application activity into telemetry-signal data. The main views are distributed-tracing paths built from spans, telemetry-metric aggregations, and log-record occurrences. A",
        "observability-instrumentation": "The stated objective requires Observability Instrumentation: the complete explanation applies it in this mechanism: OpenTelemetry (OTel) is a vendor-neutral specification and toolkit for producing, correlating, collecting, and exporting observability data. It standardizes the path from observability-instrumentation to backend delivery, but intentionally does not provide the backend that",
        "distributed-tracing": "The stated objective requires Distributed Tracing: the complete explanation applies it in this mechanism: The flow begins with observability-instrumentation. Manual API calls or automatic library hooks turn application activity into telemetry-signal data. The main views are distributed-tracing paths built from spans, telemetry-metric aggregations, and log-record occurrences. A",
        "telemetry-metric": "The stated objective requires Telemetry Metric: the complete explanation applies it in this mechanism: The flow begins with observability-instrumentation. Manual API calls or automatic library hooks turn application activity into telemetry-signal data. The main views are distributed-tracing paths built from spans, telemetry-metric aggregations, and log-record occurrences. A",
        "log-record": "The stated objective requires Log Record: the complete explanation applies it in this mechanism: The flow begins with observability-instrumentation. Manual API calls or automatic library hooks turn application activity into telemetry-signal data. The main views are distributed-tracing paths built from spans, telemetry-metric aggregations, and log-record occurrences. A",
        "telemetry-context": "The stated objective requires Telemetry Context: the complete explanation applies it in this mechanism: The flow begins with observability-instrumentation. Manual API calls or automatic library hooks turn application activity into telemetry-signal data. The main views are distributed-tracing paths built from spans, telemetry-metric aggregations, and log-record occurrences. A",
        "context-propagation": "The stated objective requires Context Propagation: the complete explanation applies it in this mechanism: The flow begins with observability-instrumentation. Manual API calls or automatic library hooks turn application activity into telemetry-signal data. The main views are distributed-tracing paths built from spans, telemetry-metric aggregations, and log-record occurrences. A",
        "opentelemetry-collector": "The stated objective requires OpenTelemetry Collector: the complete explanation applies it in this mechanism: SDK exporters can send directly to a backend, but production systems commonly send OTLP to an opentelemetry-collector. Its receivers accept the signals, processors batch or sanitize them, and exporters route them to one or several backends. Replacing the backend changes"
      }
    },
    "opentelemetry-collector": {
      "prerequisiteReasons": {
        "telemetry-signal": "The stated objective requires Telemetry Signal: the complete explanation applies it in this mechanism: The OpenTelemetry Collector is a vendor-neutral service that receives, processes, and exports telemetry-signal data. It separates applications from backend-specific protocols and operational concerns such as batching, retries, filtering, and redaction."
      }
    },
    "operator-set": {
      "prerequisiteReasons": {
        "tensor": "The stated objective requires Tensor: the complete explanation applies it in this mechanism: An operation name alone is ambiguous. To execute Add, a consumer must know how many inputs it accepts, which tensor element types are legal, how shapes combine, and what output values must result. An operator schema supplies that contract. An operator set groups such schemas"
      }
    },
    "out-of-core-processing": {
      "prerequisiteReasons": {
        "memory-hierarchy": "The stated objective requires Memory Hierarchy: the complete explanation applies it in this mechanism: The memory-hierarchy trades fast limited memory against larger slower storage. Read a partition, compute its contribution, release it, and continue. A reduction-operation with mergeable state lets partition results combine without collecting all rows.",
        "reduction-operation": "The stated objective requires Reduction operation: the complete explanation applies it in this mechanism: The memory-hierarchy trades fast limited memory against larger slower storage. Read a partition, compute its contribution, release it, and continue. A reduction-operation with mergeable state lets partition results combine without collecting all rows."
      }
    },
    "overlayfs": {
      "prerequisiteReasons": {
        "vfs": "The stated objective requires Virtual File System: the complete explanation applies it in this mechanism: OverlayFS is a union filesystem — a filesystem that, instead of owning bytes on a disk of its own, builds one merged view by stacking other directory trees on top of each other. It plugs into the kernel through the vfs interface, so a program reading or writing through it issues",
        "copy-on-write": "The stated objective requires Copy-on-Write: the complete explanation applies it in this mechanism: 2. Modifying a file that exists only in a lower layer. This is the non-obvious, \"magic-looking\" step, and it has a name: copy-up. When a program opens a lower-layer file for writing, OverlayFS first copies the whole file up into the upper layer, then redirects the write to that"
      }
    },
    "page": {
      "prerequisiteReasons": {
        "virtual-memory": "The stated objective requires Virtual Memory: the complete explanation applies it in this mechanism: A page is the fixed-size block — almost always 4 KB (4096 bytes) — in which memory is managed. virtual-memory gives each process a private range of virtual addresses that the kernel must connect to real spots in physical RAM; rather than tracking that connection one byte at a"
      }
    },
    "page-cache": {
      "prerequisiteReasons": {
        "page": "The stated objective requires Page: the complete explanation applies it in this mechanism: The page cache is the kernel's use of otherwise-idle RAM to hold recently or frequently used file data, kept in page-sized units (4 KB blocks), so that file I/O issued through the vfs mostly lands in fast RAM instead of on slow disk. The motive is a raw speed gap: reading from",
        "vfs": "The stated objective requires Virtual File System: the complete explanation applies it in this mechanism: The page cache is the kernel's use of otherwise-idle RAM to hold recently or frequently used file data, kept in page-sized units (4 KB blocks), so that file I/O issued through the vfs mostly lands in fast RAM instead of on slow disk. The motive is a raw speed gap: reading from",
        "inode": "The stated objective requires Inode: the complete explanation applies it in this mechanism: Step 1 — first read (all misses). The program opens the file (VFS resolves the path to its inode and gives back a file-descriptor, fd = 3) and reads all 1 MB. Every one of the 256 pages is absent, so every page is a cache miss: the kernel allocates 256 page-cache pages, pulls",
        "system-call": "The stated objective requires System Call: the complete explanation applies it in this mechanism: A program issues a read system-call on a file-descriptor. VFS routes it to the disk filesystem's read routine, which now asks one question: are the requested pages of this file already in the page cache? Two cases, and they must both be enumerated because each exercises a"
      }
    },
    "page-fault": {
      "prerequisiteReasons": {
        "page-table": "The stated objective requires Page Table: the complete explanation applies it in this mechanism: A page fault is the hardware trap the CPU raises the instant a memory access cannot be satisfied by the page-table — either the looked-up entry maps nothing valid, or the entry exists but the access violates its permission bits (for example, writing a page the entry marks",
        "interrupt": "The stated objective requires Interrupt: the complete explanation applies it in this mechanism: The word \"fault\" is misleading if read as \"malfunction.\" In CPU terminology a fault is a restartable exception: the hardware hits something it cannot resolve on its own, pauses the current instruction before it completes, transfers control to a kernel routine registered for this"
      }
    },
    "page-table": {
      "prerequisiteReasons": {
        "page": "The stated objective requires Page: the complete explanation applies it in this mechanism: A page table is the per-process lookup structure that records, for each virtual page, which physical frame currently holds it — the \"translation book\" the CPU's address hardware consults on every memory access to turn a virtual address into a physical one. A page already split"
      }
    },
    "paged-attention": {
      "prerequisiteReasons": {
        "kv-cache": "The stated objective requires KV-cache: the complete explanation applies it in this mechanism: The kv-cache grows one token at a time, but a sequence's final length is unknown, so the naive implementation reserves one big contiguous block sized for the maximum length — wasting most of it (internal fragmentation) and blocking sharing. Paged attention (the idea behind vLLM)",
        "copy-on-write": "The stated objective requires Copy-on-Write: the complete explanation applies it in this mechanism: - Fixed-size blocks. The kv-cache is split into uniform blocks holding a few tokens' K,V each. The physical blocks live anywhere in a shared pool — not contiguous. - Block table (the page-table). Each sequence keeps a small table: logical block i → physical block address.",
        "virtual-memory": "The stated objective requires Virtual Memory: the complete explanation applies it in this mechanism: The kv-cache grows one token at a time, but a sequence's final length is unknown, so the naive implementation reserves one big contiguous block sized for the maximum length — wasting most of it (internal fragmentation) and blocking sharing. Paged attention (the idea behind vLLM)",
        "page-table": "The stated objective requires Page Table: the complete explanation applies it in this mechanism: - Fixed-size blocks. The kv-cache is split into uniform blocks holding a few tokens' K,V each. The physical blocks live anywhere in a shared pool — not contiguous. - Block table (the page-table). Each sequence keeps a small table: logical block i → physical block address."
      }
    },
    "parallel-process": {
      "prerequisiteReasons": {
        "process": "The stated objective requires Process: the complete explanation applies it in this mechanism: Parallel processes are two or more processes that run simultaneously — each making progress on its own stream of instructions at the same time as the others, with no guaranteed relative order between them. The key property that makes them parallel rather than merely concurrent",
        "thread": "The stated objective requires Thread: the complete explanation applies it in this mechanism: Parallel processes are two or more processes that run simultaneously — each making progress on its own stream of instructions at the same time as the others, with no guaranteed relative order between them. The key property that makes them parallel rather than merely concurrent",
        "scheduler": "The stated objective requires Scheduler: the complete explanation applies it in this mechanism: Parallel processes are two or more processes that run simultaneously — each making progress on its own stream of instructions at the same time as the others, with no guaranteed relative order between them. The key property that makes them parallel rather than merely concurrent",
        "context-switch": "The stated objective requires Context Switch: the complete explanation applies it in this mechanism: Parallel processes are two or more processes that run simultaneously — each making progress on its own stream of instructions at the same time as the others, with no guaranteed relative order between them. The key property that makes them parallel rather than merely concurrent",
        "address-space-layout": "The stated objective requires Address Space Layout: the complete explanation applies it in this mechanism: Parallel processes are two or more processes that run simultaneously — each making progress on its own stream of instructions at the same time as the others, with no guaranteed relative order between them. The key property that makes them parallel rather than merely concurrent",
        "page-table": "The stated objective requires Page Table: the complete explanation applies it in this mechanism: The structure of that address space — how code, data, heap, and stack are arranged inside it — is described by address-space-layout. What matters for parallel processes is the consequence: because each process has its own separate layout with its own page table, two processes"
      }
    },
    "partial-derivative": {
      "prerequisiteReasons": {
        "derivative": "The stated objective requires Derivative: the complete explanation applies it in this mechanism: It is just a derivative taken with respect to one variable while the rest are treated as constants:"
      }
    },
    "path-environment-variable": {
      "prerequisiteReasons": {
        "process": "The stated objective requires Process: the complete explanation applies it in this mechanism: On POSIX systems, an environment is inherited by a new process as name–value strings. When a shell-like interpreter receives a command name without a slash, it searches the directories listed in PATH according to its command-resolution rules. The first acceptable match",
        "interpreter": "The stated objective requires Interpreter: the complete explanation applies it in this mechanism: On POSIX systems, an environment is inherited by a new process as name–value strings. When a shell-like interpreter receives a command name without a slash, it searches the directories listed in PATH according to its command-resolution rules. The first acceptable match"
      }
    },
    "pci-express": {
      "prerequisiteReasons": {
        "io-interconnect": "The stated objective requires I/O Interconnect: the complete explanation applies it in this mechanism: PCI Express (PCIe) is a switched, point-to-point serial io-interconnect that connects a host root complex to endpoints such as GPUs, network cards, and storage controllers using packetized transactions over one or more lanes.",
        "memory-mapped-io": "The stated objective requires Memory-Mapped I/O: the complete explanation applies it in this mechanism: Software discovers an endpoint and assigns address ranges. Control accesses use memory-mapped-io: a host load or store becomes a transaction-layer packet (TLP) carrying an address, operation, requester identity, and optional payload. Reads return completion TLPs; posted writes",
        "dma": "The stated objective requires Direct Memory Access: the complete explanation applies it in this mechanism: For bulk movement, the host gives the device buffer addresses and the endpoint uses dma to read or write host memory across PCIe. MMIO usually rings the doorbell; DMA carries the large payload."
      }
    },
    "permutation-feature-importance": {
      "prerequisiteReasons": {
        "measurement": "The stated objective requires Measurement: the complete explanation applies it in this mechanism: Let $s(D)$ be a chosen performance measurement for a fitted model on dataset $D$. For feature $j$, independently shuffle its observed values across rows to create $D{j,k}$ on repetition $k$. The importance estimate is",
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: Shuffling preserves the feature’s marginal probability-distribution but destroys its row-level association. If the fitted model depends on that association, its score falls and $Ij$ is positive. Repetitions expose variability from the random permutations."
      }
    },
    "pgai-vectorizer": {
      "prerequisiteReasons": {
        "database-trigger": "The stated objective requires Database Trigger: the complete explanation applies it in this mechanism: The pgai Vectorizer is a declarative auto-embedding pipeline for Postgres. You declare once that a given source table's text column should be embedded — which model, how to chunk the text — and the Vectorizer then keeps the embeddings in sync with the source data automatically,",
        "embedding": "The stated objective requires Embedding: the complete explanation applies it in this mechanism: The pgai Vectorizer is a declarative auto-embedding pipeline for Postgres. You declare once that a given source table's text column should be embedded — which model, how to chunk the text — and the Vectorizer then keeps the embeddings in sync with the source data automatically,",
        "pgvector": "The stated objective requires pgvector: the complete explanation applies it in this mechanism: The pgai Vectorizer is a declarative auto-embedding pipeline for Postgres. You declare once that a given source table's text column should be embedded — which model, how to chunk the text — and the Vectorizer then keeps the embeddings in sync with the source data automatically,"
      }
    },
    "pgvector": {
      "prerequisiteReasons": {
        "postgresql": "The stated objective requires PostgreSQL: the complete explanation applies it in this mechanism: pgvector is an extension for postgresql that teaches it to store and search vectors. It adds three things to the database: a first-class vector column type (a fixed-length list of real numbers), a set of distance operators that score how close two vectors are, and",
        "embedding": "The stated objective requires Embedding: the complete explanation applies it in this mechanism: pgvector is an extension for postgresql that teaches it to store and search vectors. It adds three things to the database: a first-class vector column type (a fixed-length list of real numbers), a set of distance operators that score how close two vectors are, and",
        "nearest-neighbor-search": "The stated objective requires Nearest-Neighbor Search: the complete explanation applies it in this mechanism: pgvector is an extension for postgresql that teaches it to store and search vectors. It adds three things to the database: a first-class vector column type (a fixed-length list of real numbers), a set of distance operators that score how close two vectors are, and",
        "cosine-similarity": "The stated objective requires Cosine Similarity: the complete explanation applies it in this mechanism: Recall the retrieval stack this builds on. An embedding is a dense vector whose position encodes meaning; cosine-similarity scores how nearly two such vectors point the same way; and nearest-neighbor-search is the operation \"given a query vector, return the k stored vectors most",
        "database-index": "The stated objective requires Database Index: the complete explanation applies it in this mechanism: 3. ANN index types. pgvector supplies index types — HNSW (a navigable-graph index) and IVFFlat (a clustering index) — that are pgvector's realization of the two ANN families nearest-neighbor-search describes. Each is a specialized database-index: like a B-tree index it is an"
      }
    },
    "physics-informed-neural-network": {
      "prerequisiteReasons": {
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: Let $u\\theta(x,t)$ be the output of a neural-network with trainable parameters $\\theta$, spatial coordinate $x$, and time $t$. Suppose the desired field must satisfy the differential-equation",
        "differential-equation": "The stated objective requires Differential Equation: the complete explanation applies it in this mechanism: Let $u\\theta(x,t)$ be the output of a neural-network with trainable parameters $\\theta$, spatial coordinate $x$, and time $t$. Suppose the desired field must satisfy the differential-equation",
        "gradient-descent": "The stated objective requires Gradient descent: the complete explanation applies it in this mechanism: Here $(xi,ti,yi)$ are $Nu$ known values, $(\\tilde xj,\\tilde tj)$ are $Nr$ residual coordinates, and $\\lambda$ balances the two requirements. gradient-descent changes $\\theta$ to reduce this joint objective. The decisive idea is that a coordinate needs no measured label to",
        "loss-function": "The stated objective requires Loss function: the complete explanation applies it in this mechanism: The derivatives are computed from the network's operations, so $r\\theta$ remains a function of $\\theta$. A typical loss-function combines observed or boundary values with residual samples:"
      }
    },
    "pipeline-bottleneck-localization": {
      "prerequisiteReasons": {
        "measurement": "The stated objective requires Measurement: the complete explanation applies it in this mechanism: Start with end-to-end measurement under representative shapes, worker counts, cache state, and steady load. Record CPU operators, device kernels, transfers, buffering delays, and memory consumption on one aligned timeline. Distinguish self time from inclusive time, and",
        "latency-percentile": "The stated objective requires Latency Percentiles (Tail Latency): the complete explanation applies it in this mechanism: Change one stage at a time and compare warm steady-state throughput plus latency-percentiles. Track buffered-work depth, accelerator idle gaps, host utilization, transfer bandwidth, peak memory, and output equivalence. Reprofile after every optimization because removing one",
        "memory-hierarchy": "The stated objective requires Memory Hierarchy: the complete explanation applies it in this mechanism: Caching can introduce a new memory-hierarchy bottleneck, alter randomness or augmentation, and change preprocessing fidelity. Measure cache construction separately, verify identical stage outputs or acceptable semantics, and ensure the cache fits without paging or evicting other"
      }
    },
    "pipeline-parallelism": {
      "prerequisiteReasons": {
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: Pipeline parallelism splits a neural-network by layers — each GPU holds a contiguous block of layers (a stage) — and pushes the data through the stages like an assembly line. To keep every stage busy, the batch is cut into micro-batches that flow through the pipeline one after",
        "message-passing": "The stated objective requires Message passing: the complete explanation applies it in this mechanism: Pipeline parallelism splits a neural-network by layers — each GPU holds a contiguous block of layers (a stage) — and pushes the data through the stages like an assembly line. To keep every stage busy, the batch is cut into micro-batches that flow through the pipeline one after",
        "gradient": "The stated objective requires Gradient: the complete explanation applies it in this mechanism: 1. Layers → stages. Cut the neural-network's layers into P contiguous blocks, one per GPU. Forward activations flow stage 0 → 1 → 2 → 3; the backward pass sends gradients the other way, 3 → 2 → 1 → 0 (chain rule). 2. The dependency problem. Stage i+1 cannot start until stage i"
      }
    },
    "positional-encoding": {
      "prerequisiteReasons": {
        "embedding": "The stated objective requires Embedding: the complete explanation applies it in this mechanism: An embedding maps a token identity to a vector, but the same token receives the same vector wherever it appears. Moreover, transformer-attention compares queries with keys and takes weighted sums of values; without a position-dependent signal, reordering all input token vectors",
        "transformer-attention": "The stated objective requires Transformer attention: the complete explanation applies it in this mechanism: An embedding maps a token identity to a vector, but the same token receives the same vector wherever it appears. Moreover, transformer-attention compares queries with keys and takes weighted sums of values; without a position-dependent signal, reordering all input token vectors"
      }
    },
    "post-training-quantization": {
      "prerequisiteReasons": {
        "quantization": "The stated objective requires Quantization: the complete explanation applies it in this mechanism: Post-training quantization (PTQ) converts an already trained neural-network to lower-bit weights, activations, or both without updating its learned parameters. It applies quantization after training, then accepts the conversion only if the measured task quality remains within an",
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: Post-training quantization (PTQ) converts an already trained neural-network to lower-bit weights, activations, or both without updating its learned parameters. It applies quantization after training, then accepts the conversion only if the measured task quality remains within an",
        "loss-function": "The stated objective requires Loss function: the complete explanation applies it in this mechanism: Post-training quantization (PTQ) converts an already trained neural-network to lower-bit weights, activations, or both without updating its learned parameters. It applies quantization after training, then accepts the conversion only if the measured task quality remains within an",
        "tensor": "The stated objective requires Tensor: the complete explanation applies it in this mechanism: PTQ is an application of quantization to selected tensors in the trained network. For one group of real values $x$, choose a scale $s$, map each value to an integer code,"
      }
    },
    "posterior-consistency": {
      "prerequisiteReasons": {
        "bayes-rule": "The stated objective requires Bayes' Rule: the complete explanation applies it in this mechanism: Posterior consistency is the property that, as the amount of data grows without bound, the bayes-rule posterior collapses onto the true value of the unknown quantity — no matter where the prior started, as long as the prior was not dogmatic (did not rule that value out in",
        "likelihood": "The stated objective requires Likelihood: the complete explanation applies it in this mechanism: Recall the inference setup from bayes-rule. There is an unknown quantity θ we want to learn (a \"parameter\" — a number, fixed but unknown, that indexes the state of the world; for a coin, θ is p, the unknown probability of heads). We hold a prior distribution P(θ) over its"
      }
    },
    "postgresql": {
      "prerequisiteReasons": {
        "relational-model": "The stated objective requires Relational Model: the complete explanation applies it in this mechanism: PostgreSQL is an open-source relational database management system (RDBMS) — but the point of treating it as its own concept is not any single subsystem, all of which already exist as the prerequisite nodes below. The point is the composition: a specific, battle-tested set of",
        "sql": "The stated objective requires SQL (Structured Query Language): the complete explanation applies it in this mechanism: PostgreSQL is an open-source relational database management system (RDBMS) — but the point of treating it as its own concept is not any single subsystem, all of which already exist as the prerequisite nodes below. The point is the composition: a specific, battle-tested set of",
        "transaction": "The stated objective requires Transaction (ACID): the complete explanation applies it in this mechanism: PostgreSQL is an open-source relational database management system (RDBMS) — but the point of treating it as its own concept is not any single subsystem, all of which already exist as the prerequisite nodes below. The point is the composition: a specific, battle-tested set of",
        "mvcc": "The stated objective requires Multi-Version Concurrency Control: the complete explanation applies it in this mechanism: PostgreSQL is an open-source relational database management system (RDBMS) — but the point of treating it as its own concept is not any single subsystem, all of which already exist as the prerequisite nodes below. The point is the composition: a specific, battle-tested set of",
        "write-ahead-logging": "The stated objective requires Write-Ahead Logging (WAL): the complete explanation applies it in this mechanism: PostgreSQL is an open-source relational database management system (RDBMS) — but the point of treating it as its own concept is not any single subsystem, all of which already exist as the prerequisite nodes below. The point is the composition: a specific, battle-tested set of",
        "b-tree": "The stated objective requires B-Tree / B+-Tree: the complete explanation applies it in this mechanism: PostgreSQL is an open-source relational database management system (RDBMS) — but the point of treating it as its own concept is not any single subsystem, all of which already exist as the prerequisite nodes below. The point is the composition: a specific, battle-tested set of",
        "query-planning": "The stated objective requires Query Planning & Optimization: the complete explanation applies it in this mechanism: PostgreSQL is an open-source relational database management system (RDBMS) — but the point of treating it as its own concept is not any single subsystem, all of which already exist as the prerequisite nodes below. The point is the composition: a specific, battle-tested set of",
        "concurrency-control": "The stated objective requires Concurrency Control: the complete explanation applies it in this mechanism: PostgreSQL is an open-source relational database management system (RDBMS) — but the point of treating it as its own concept is not any single subsystem, all of which already exist as the prerequisite nodes below. The point is the composition: a specific, battle-tested set of"
      }
    },
    "precision-recall": {
      "prerequisiteReasons": {
        "conditional-probability": "The stated objective requires Conditional Probability: the complete explanation applies it in this mechanism: Let $Y=1$ denote a true positive-class case and $A=1$ denote an alert. These metrics are conditional-probability statements: precision is $P(Y=1\\mid A=1)$ and recall is $P(A=1\\mid Y=1)$."
      }
    },
    "predecessor-query": {
      "prerequisiteReasons": {
        "binary-search": "The stated objective requires Binary Search: the complete explanation applies it in this mechanism: A predecessor query asks one question of a collection of stored numbers (called keys): given a query value T, return the largest stored key that is strictly less than T — the key that comes just before T, with T itself excluded. The motivating example is a log store keyed by",
        "binary-search-tree": "The stated objective requires Binary Search Tree: the complete explanation applies it in this mechanism: A predecessor query asks one question of a collection of stored numbers (called keys): given a query value T, return the largest stored key that is strictly less than T — the key that comes just before T, with T itself excluded. The motivating example is a log store keyed by"
      }
    },
    "prefill-decode-disaggregation": {
      "prerequisiteReasons": {
        "prefill-vs-decode": "The stated objective requires Prefill vs Decode: the complete explanation applies it in this mechanism: Prefill-decode disaggregation runs the two phases of an LLM request on separate worker pools. A prefill worker processes the prompt and builds its kv-cache; that cache is then transferred to a decode worker, which generates the output tokens. The split follows the opposite",
        "kv-cache": "The stated objective requires KV-cache: the complete explanation applies it in this mechanism: Prefill-decode disaggregation runs the two phases of an LLM request on separate worker pools. A prefill worker processes the prompt and builds its kv-cache; that cache is then transferred to a decode worker, which generates the output tokens. The split follows the opposite"
      }
    },
    "prefill-vs-decode": {
      "prerequisiteReasons": {
        "kv-cache": "The stated objective requires KV-cache: the complete explanation applies it in this mechanism: Serving a large language model splits into two phases with opposite cost profiles. In prefill the whole prompt is pushed through the model in one forward pass to populate the kv-cache — all prompt tokens travel through each weight together, so every weight loaded from memory is",
        "roofline-model": "The stated objective requires Roofline Model: the complete explanation applies it in this mechanism: Serving a large language model splits into two phases with opposite cost profiles. In prefill the whole prompt is pushed through the model in one forward pass to populate the kv-cache — all prompt tokens travel through each weight together, so every weight loaded from memory is"
      }
    },
    "probability": {
      "prerequisiteReasons": {
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: Probability is a number between 0 and 1 attached to an event — a yes/no question about the result of some uncertain situation, like \"the die shows an even number.\" It measures how strongly we expect that event to happen: 0 means never, 1 means always, and a value in between"
      }
    },
    "probability-distribution": {
      "prerequisiteReasons": {
        "random-variable": "The stated objective requires Random Variable: the complete explanation applies it in this mechanism: A random-variable is a function that assigns numbers to outcomes, with each value carrying a probability. A probability distribution is the full bookkeeping of how that probability is spread across the values — the complete answer to \"how likely is each number, or each range of"
      }
    },
    "process": {
      "prerequisiteReasons": {
        "kernel": "The stated objective requires Kernel: the complete explanation applies it in this mechanism: A process is a running instance of a program. A program is passive: a file of machine instructions sitting on disk, doing nothing. A process is that program in execution — loaded into memory with live state and actively making progress. The kernel is what turns the one into the"
      }
    },
    "processor-affinity": {
      "prerequisiteReasons": {
        "thread": "The stated objective requires Thread: the complete explanation applies it in this mechanism: Processor affinity is the allowed set of CPUs on which a thread may execute. Restricting that set can preserve cache and hardware locality, while an overly narrow set can prevent the scheduler from balancing load.",
        "scheduler": "The stated objective requires Scheduler: the complete explanation applies it in this mechanism: Processor affinity is the allowed set of CPUs on which a thread may execute. Restricting that set can preserve cache and hardware locality, while an overly narrow set can prevent the scheduler from balancing load."
      }
    },
    "progressive-diffusion-distillation": {
      "prerequisiteReasons": {
        "denoising-diffusion-probabilistic-model": "The stated objective requires Denoising Diffusion Probabilistic Model: the complete explanation applies it in this mechanism: - denoising-diffusion-probabilistic-model - inference-cost-break-even",
        "inference-cost-break-even": "The stated objective requires Inference Cost Break-Even: the complete explanation applies it in this mechanism: Compare teacher and students at every stage using end-to-end latency, number of evaluations, device utilization, peak memory, cost, quality, diversity, and prompt adherence. Include equal-latency baselines with better solvers or fewer undistilled steps, and account for"
      }
    },
    "progressive-model-rollout": {
      "prerequisiteReasons": {
        "multi-armed-bandit": "The stated objective requires Multi-Armed Bandit: the complete explanation applies it in this mechanism: - Shadow evaluation duplicates requests to the candidate but suppresses candidate responses. It reveals compatibility, latency, and candidate outputs without changing user-visible predictions. It can still consume resources, expose data to another path, and trigger side effects",
        "load-balancing": "The stated objective requires Load Balancing: the complete explanation applies it in this mechanism: Route by user or another persistent unit when repeated interactions could cross-contaminate behavior. load-balancing alone is not experimental randomization. Define primary outcomes, guardrails, minimum duration, ramp stages, stop conditions, and rollback before exposure.",
        "concept-drift": "The stated objective requires Concept Drift: the complete explanation applies it in this mechanism: Route by user or another persistent unit when repeated interactions could cross-contaminate behavior. load-balancing alone is not experimental randomization. Define primary outcomes, guardrails, minimum duration, ramp stages, stop conditions, and rollback before exposure."
      }
    },
    "prompt-rewriting": {
      "prerequisiteReasons": {
        "text-image-attribute-binding": "The stated objective requires Text–Image Attribute Binding: the complete explanation applies it in this mechanism: The rewrite must preserve hard constraints: entities, counts, relations, negation, requested style, and exclusions. Those constraints include text-image-attribute-binding. Added details should be distinguishable from the user's words, controllable, and reversible. Otherwise a",
        "image-preference-modeling": "The stated objective requires Image Preference Modeling: the complete explanation applies it in this mechanism: Evaluate on representative anonymized prompts across lengths, languages, and domains. Use blinded human judgments of intent preservation, prompt adherence, aesthetics, diversity, and unwanted additions; report rewrite latency and failure rate. A learned preference model can"
      }
    },
    "ptx": {
      "prerequisiteReasons": {
        "cuda-kernel": "The stated objective requires CUDA Kernel: the complete explanation applies it in this mechanism: PTX (Parallel Thread Execution) is the intermediate, virtual instruction set that a cuda-kernel's GPU code is first compiled into, on its way from high-level source to the actual machine code a GPU executes. It is an assembly-like text representation, but it is not the real"
      }
    },
    "python-assertion": {
      "prerequisiteReasons": {
        "interpreter": "The stated objective requires Interpreter: the complete explanation applies it in this mechanism: The interpreter evaluates the condition. When it is false, assert condition, message behaves like raising AssertionError(message); without a message, it raises AssertionError. This makes an assertion useful for checking an invariant after a local computation, such as a parser"
      }
    },
    "python-break-statement": {
      "prerequisiteReasons": {
        "python-loop": "The stated objective requires Python Loop: the complete explanation applies it in this mechanism: Python’s break statement terminates the nearest syntactically enclosing python-loop. Execution continues after that loop, not after every enclosing loop or function."
      }
    },
    "python-copying": {
      "prerequisiteReasons": {
        "dynamic-typing": "The stated objective requires Dynamic Typing: the complete explanation applies it in this mechanism: A deep copy recursively constructs copies of contained objects, so mutating a nested mutable object in the result normally does not affect the original. It does not blindly duplicate every reference. Python's copy module keeps a memo of objects already copied, which both"
      }
    },
    "python-descriptor": {
      "prerequisiteReasons": {
        "dynamic-typing": "The stated objective requires Dynamic Typing: the complete explanation applies it in this mechanism: Ordinary instance state lives in a dictionary-like mapping from attribute names to values. A descriptor changes the lookup path: instead of simply returning or replacing an entry, the class-level object participates in the operation. This is a protocol of dynamic-typing—behavior",
        "hash-map": "The stated objective requires Hash Map: the complete explanation applies it in this mechanism: When Python creates Rectangle, setname tells each descriptor which class attribute owns it, so width and height choose distinct private names. Constructing Rectangle(3, 4) routes both assignments through set; constructing Rectangle(3, -1) raises before the invalid height is"
      }
    },
    "python-governance": {
      "prerequisiteReasons": {
        "interpreter": "The stated objective requires Interpreter: the complete explanation applies it in this mechanism: The council's mandate includes language and interpreter quality and stability, sustainable contribution, the core-team relationship with the Python Software Foundation, PEP decision processes, and final appeal when other mechanisms fail."
      }
    },
    "python-loop": {
      "prerequisiteReasons": {
        "interpreter": "The stated objective requires Interpreter: the complete explanation applies it in this mechanism: The interpreter evaluates condition before the first iteration and again after every completed iteration. The body can therefore run zero times. Correctness requires that the body or external state eventually falsify the condition unless nontermination is intentional."
      }
    },
    "python-membership-test": {
      "prerequisiteReasons": {
        "interpreter": "The stated objective requires Interpreter: the complete explanation applies it in this mechanism: - interpreter - python-loop",
        "python-loop": "The stated objective requires Python Loop: the complete explanation applies it in this mechanism: Built-in sequences test elements, mappings test keys rather than values, and strings or bytes test substrings. The empty string is a substring of every string. These distinctions make explicit tests clearer than manually writing a python-loop."
      }
    },
    "python-profiling": {
      "prerequisiteReasons": {
        "interpreter": "The stated objective requires Interpreter: the complete explanation applies it in this mechanism: - interpreter - measurement",
        "measurement": "The stated objective requires Measurement: the complete explanation applies it in this mechanism: Python profiling is a runtime measurement that records execution statistics such as call counts, self time, and cumulative time to locate bottlenecks before changing code."
      }
    },
    "python-special-method": {
      "prerequisiteReasons": {
        "dynamic-typing": "The stated objective requires Dynamic Typing: the complete explanation applies it in this mechanism: Python syntax is connected to protocol methods. For example, len(x) can invoke type(x).len(x), for item in x uses the iteration protocol, and x[key] uses the subscription protocol. A class therefore need not inherit from one common interface to support an operation: it can"
      }
    },
    "python-underscore-conventions": {
      "prerequisiteReasons": {
        "namespace": "The stated objective requires Namespace: the complete explanation applies it in this mechanism: - name conventionally marks a non-public name. At module level, it is omitted from from module import  unless explicitly exported. It remains directly accessible; this is not access control. - name avoids collision with a keyword or another preferred name. It has no special",
        "interpreter": "The stated objective requires Interpreter: the complete explanation applies it in this mechanism: - name conventionally marks a non-public name. At module level, it is omitted from from module import  unless explicitly exported. It remains directly accessible; this is not access control. - name avoids collision with a keyword or another preferred name. It has no special"
      }
    },
    "pytorch-module-call": {
      "prerequisiteReasons": {
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: A model built from a neural-network is often implemented as a class derived from torch.nn.Module. That derived-class relationship is an instance of object-oriented-inheritance: the model supplies its own forward method while retaining behavior supplied by the base class.",
        "object-oriented-inheritance": "The stated objective requires Object-Oriented Inheritance: the complete explanation applies it in this mechanism: A model built from a neural-network is often implemented as a class derived from torch.nn.Module. That derived-class relationship is an instance of object-oriented-inheritance: the model supplies its own forward method while retaining behavior supplied by the base class."
      }
    },
    "quantile-quantile-plot": {
      "prerequisiteReasons": {
        "cumulative-distribution-function": "The stated objective requires Cumulative Distribution Function: the complete explanation applies it in this mechanism: The cumulative-distribution-function (CDF) maps a value to the fraction of observations at or below it. Reading that map backward gives a quantile: for a chosen fraction $q$ between 0 and 1, it is the value below which approximately fraction $q$ of observations lie."
      }
    },
    "quantile-regression": {
      "prerequisiteReasons": {
        "loss-function": "The stated objective requires Loss function: the complete explanation applies it in this mechanism: An ordinary squared-error regression target makes over-predictions and under-predictions equally costly, so its optimum estimates a conditional mean. Quantile regression instead uses an asymmetric loss-function for a selected level $\\tau$ between 0 and 1. For residual $r = y -"
      }
    },
    "query-planning": {
      "prerequisiteReasons": {
        "sql": "The stated objective requires SQL (Structured Query Language): the complete explanation applies it in this mechanism: Query planning (query optimization) is the engine step that turns a declarative sql query into a concrete execution plan — a specific procedure for computing the answer. Because sql says what result you want and not how to compute it, there are many physically-different",
        "database-index": "The stated objective requires Database Index: the complete explanation applies it in this mechanism: Query planning (query optimization) is the engine step that turns a declarative sql query into a concrete execution plan — a specific procedure for computing the answer. Because sql says what result you want and not how to compute it, there are many physically-different",
        "b-tree": "The stated objective requires B-Tree / B+-Tree: the complete explanation applies it in this mechanism: Query planning (query optimization) is the engine step that turns a declarative sql query into a concrete execution plan — a specific procedure for computing the answer. Because sql says what result you want and not how to compute it, there are many physically-different"
      }
    },
    "queue": {
      "prerequisiteReasons": {
        "linked-list": "The stated objective requires Linked List: the complete explanation applies it in this mechanism: The insight: back it with a structure whose ends are both cheap. This is precisely the operation a linked-list makes cheap. Recall its trade: a linked-list gives up instant jump-to-the-i-th-value but gains O(1) splicing — inserting or removing a node, given that node, costs only",
        "dynamic-array": "The stated objective requires Dynamic Array: the complete explanation applies it in this mechanism: Why \"cheaply at both ends\" is the hard part. The obvious way to store a sequence is a contiguous run of equally sized slots — slot 0, then slot 1 packed right after it, and so on (a dynamic-array; in Python, a list). Appending a new value at the back of such a run is fine: you"
      }
    },
    "random-variable": {
      "prerequisiteReasons": {
        "probability": "The stated objective requires Probability: the complete explanation applies it in this mechanism: A random variable is a rule that attaches a number to each possible outcome of a random experiment. A probability model on its own only tells us how likely each outcome is, but the outcomes themselves may be things we cannot add, average, or compare numerically (heads/tails, a"
      }
    },
    "rank": {
      "prerequisiteReasons": {
        "communicator": "The stated objective requires Communicator: the complete explanation applies it in this mechanism: A rank is the integer id that a communicator assigns to each of its member processes — 0, 1, …, n-1 for a group of n. It is how a process is addressed: within the communicator, \"rank 2\" unambiguously names one specific process. A process can also ask \"what is my rank?\" to find"
      }
    },
    "reactive-user-interface": {
      "prerequisiteReasons": {
        "event": "The stated objective requires Event: the complete explanation applies it in this mechanism: Suppose a view is $V=f(S)$ for application state $S$. An input event changes $S$; the runtime identifies dependent views and schedules $f$ again. This separates state transitions from rendering and can coalesce several changes into one refresh.",
        "python-descriptor": "The stated objective requires Python Descriptor: the complete explanation applies it in this mechanism: Textual applies this pattern to terminal interfaces. Its reactive attributes use the python-descriptor protocol, can validate or watch assignments, and automatically refresh a widget when a value changes. Multiple modifications can be combined into one refresh. State that should"
      }
    },
    "read-eval-print-loop": {
      "prerequisiteReasons": {
        "interpreter": "The stated objective requires Interpreter: the complete explanation applies it in this mechanism: A read–eval–print loop (REPL) is an interactive mode of an interpreter: read one input unit, evaluate it in the current environment, print a representation of the result, and repeat."
      }
    },
    "real-time-os": {
      "prerequisiteReasons": {
        "scheduler": "The stated objective requires Scheduler: the complete explanation applies it in this mechanism: A real-time operating system (RTOS) is an operating system that guarantees a task will run within a fixed, predictable amount of time after the event that needs it — every single time, not on average. The defining word is deadline: a result that is correct but arrives late is",
        "interrupt": "The stated objective requires Interrupt: the complete explanation applies it in this mechanism: 2. The kernel disables interrupts in places. To protect its own shared data while updating it, the kernel sometimes briefly turns off the very interrupts that would signal an urgent external event. During such a window the event sits unprocessed. In a large general-purpose"
      }
    },
    "receptive-field": {
      "prerequisiteReasons": {
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: - neural-network - transformer-attention",
        "transformer-attention": "The stated objective requires Transformer attention: the complete explanation applies it in this mechanism: A larger theoretical field therefore does not guarantee strong long-range use, but neither does it prove distant information becomes unusable. transformer-attention offers short interaction paths between tokens, while convolutional hierarchies provide locality and favorable"
      }
    },
    "reciprocal-rank-fusion": {
      "prerequisiteReasons": {
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: Rank starts at 1. The arithmetic uses only order, so a cosine score of 0.8 need not be placed on the same scale as a lexical score of 12. The constant $k$ softens the gap between early and later positions."
      }
    },
    "redis": {
      "prerequisiteReasons": {
        "key-value-store": "The stated objective requires Key–Value Store (key-value database): the complete explanation applies it in this mechanism: Redis (REmote DIctionary Server) is a key-value-store: at its core sits one giant hash-map — the keyspace — mapping a string key to a value, with the usual O(1)-average get / set / delete. Two design choices make it more than a dictionary. First, the whole keyspace lives in",
        "hash-map": "The stated objective requires Hash Map: the complete explanation applies it in this mechanism: Redis (REmote DIctionary Server) is a key-value-store: at its core sits one giant hash-map — the keyspace — mapping a string key to a value, with the usual O(1)-average get / set / delete. Two design choices make it more than a dictionary. First, the whole keyspace lives in",
        "memory-hierarchy": "The stated objective requires Memory Hierarchy: the complete explanation applies it in this mechanism: Redis (REmote DIctionary Server) is a key-value-store: at its core sits one giant hash-map — the keyspace — mapping a string key to a value, with the usual O(1)-average get / set / delete. Two design choices make it more than a dictionary. First, the whole keyspace lives in",
        "linked-list": "The stated objective requires Linked List: the complete explanation applies it in this mechanism: Redis (REmote DIctionary Server) is a key-value-store: at its core sits one giant hash-map — the keyspace — mapping a string key to a value, with the usual O(1)-average get / set / delete. Two design choices make it more than a dictionary. First, the whole keyspace lives in",
        "hash-set": "The stated objective requires Hash Set: the complete explanation applies it in this mechanism: Redis (REmote DIctionary Server) is a key-value-store: at its core sits one giant hash-map — the keyspace — mapping a string key to a value, with the usual O(1)-average get / set / delete. Two design choices make it more than a dictionary. First, the whole keyspace lives in",
        "atomic-operation": "The stated objective requires Atomic Operation: the complete explanation applies it in this mechanism: Redis (REmote DIctionary Server) is a key-value-store: at its core sits one giant hash-map — the keyspace — mapping a string key to a value, with the usual O(1)-average get / set / delete. Two design choices make it more than a dictionary. First, the whole keyspace lives in"
      }
    },
    "reduce": {
      "prerequisiteReasons": {
        "collective-operation": "The stated objective requires Collective operation: the complete explanation applies it in this mechanism: Reduce is the collective-operation in which every process contributes a value and they are all combined by a reduction-operation (e.g. SUM) into a single result stored at the root-process. It has gather's all → one shape, but where gather collects the pieces side by side, reduce",
        "root-process": "The stated objective requires Root process: the complete explanation applies it in this mechanism: Reduce is the collective-operation in which every process contributes a value and they are all combined by a reduction-operation (e.g. SUM) into a single result stored at the root-process. It has gather's all → one shape, but where gather collects the pieces side by side, reduce",
        "reduction-operation": "The stated objective requires Reduction operation: the complete explanation applies it in this mechanism: Reduce is the collective-operation in which every process contributes a value and they are all combined by a reduction-operation (e.g. SUM) into a single result stored at the root-process. It has gather's all → one shape, but where gather collects the pieces side by side, reduce"
      }
    },
    "reduce-scatter": {
      "prerequisiteReasons": {
        "reduce": "The stated objective requires Reduce: the complete explanation applies it in this mechanism: Reduce-scatter is reduce followed by scatter: every process holds a vector of the same length; the vectors are reduced element-wise (slice j of every process is combined into one value), and then those reduced slices are scattered so each process keeps one of them. Process j",
        "scatter": "The stated objective requires Scatter: the complete explanation applies it in this mechanism: Reduce-scatter is reduce followed by scatter: every process holds a vector of the same length; the vectors are reduced element-wise (slice j of every process is combined into one value), and then those reduced slices are scattered so each process keeps one of them. Process j",
        "reduction-operation": "The stated objective requires Reduction operation: the complete explanation applies it in this mechanism: 1. The reduce part — element-wise, per slice. Line the processes' vectors up as a matrix (row = process, column = slice). For each column j, combine its values with the reduction-operation, just like reduce but done once per column. In the figure column 0 gives 1+5+2+4 = 12,"
      }
    },
    "reduction-operation": {
      "prerequisiteReasons": {
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: A reduction operation is a binary arithmetic operation that combines two values into one and is associative — e.g. sum, product, max, min. Because it is associative, applying it across a whole list folds many values down to a single result, and the order in which the combining"
      }
    },
    "reflow": {
      "prerequisiteReasons": {
        "flow-matching": "The stated objective requires Flow Matching: the complete explanation applies it in this mechanism: Reflow is a recursive rectification procedure for flow-matching. It uses a fitted ordinary differential-equation to induce a new coupling between source and target samples, then fits another flow to straighter interpolation paths under that coupling.",
        "differential-equation": "The stated objective requires Differential Equation: the complete explanation applies it in this mechanism: Reflow is a recursive rectification procedure for flow-matching. It uses a fitted ordinary differential-equation to induce a new coupling between source and target samples, then fits another flow to straighter interpolation paths under that coupling.",
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: Start with paired endpoints $(X0,X1)$ from a coupling of source and target probability-distributions. A rectified flow learns a velocity field along the linear interpolation"
      }
    },
    "register-pressure": {
      "prerequisiteReasons": {
        "occupancy": "The stated objective requires Occupancy: the complete explanation applies it in this mechanism: Register pressure is the number of registers each thread uses — where a register is the fastest storage the hardware has, a tiny on-chip slot that holds one of a thread's live working values and is read or written in a single cycle. Every thread that is resident on the chip",
        "warp": "The stated objective requires Warp: the complete explanation applies it in this mechanism: Register pressure is the number of registers each thread uses — where a register is the fastest storage the hardware has, a tiny on-chip slot that holds one of a thread's live working values and is read or written in a single cycle. Every thread that is resident on the chip"
      }
    },
    "regularization": {
      "prerequisiteReasons": {
        "loss-function": "The stated objective requires Loss function: the complete explanation applies it in this mechanism: Let $w$ denote all trainable parameters, let $L{\\mathrm{data}}(w)$ be the ordinary loss-function measured on the training examples, let $R(w)$ measure an undesirable property of a solution, and let $\\lambda\\ge 0$ control how strongly that property is discouraged. Penalty-based",
        "gradient-descent": "The stated objective requires Gradient descent: the complete explanation applies it in this mechanism: This works through the update itself. gradient-descent follows the gradient of the total objective,"
      }
    },
    "relational-model": {
      "prerequisiteReasons": {
        "set": "The stated objective requires Set (membership): the complete explanation applies it in this mechanism: The relational model is the data model in which all data is held as relations — what everyday usage calls tables. The model's one structural idea is precise: a relation is a set of tuples. A tuple (a row) is a fixed list of values, one per attribute (a named, typed column); a"
      }
    },
    "reparameterization-trick": {
      "prerequisiteReasons": {
        "change-of-variables": "The stated objective requires Change of Variables: the complete explanation applies it in this mechanism: This is a change-of-variables: randomness lives in $\\epsilon$, while $z$ is differentiable with respect to $\\mu$ and $\\sigma$ for the sampled value.",
        "normal-distribution": "The stated objective requires Normal Distribution: the complete explanation applies it in this mechanism: For a normal-distribution with mean $\\mu$ and standard deviation $\\sigma$,"
      }
    },
    "replication": {
      "prerequisiteReasons": {
        "write-ahead-logging": "The stated objective requires Write-Ahead Logging (WAL): the complete explanation applies it in this mechanism: Replication keeps a full copy of the same data on several machines (replicas). It exists for two reasons: availability — one machine can die, burn, or reboot and the data is still served — and read scaling — reads can be answered by any replica, multiplying read throughput"
      }
    },
    "representation-alignment": {
      "prerequisiteReasons": {
        "embedding": "The stated objective requires Embedding: the complete explanation applies it in this mechanism: - embedding - neural-network - regularization - diffusion-transformer",
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: - embedding - neural-network - regularization - diffusion-transformer",
        "regularization": "The stated objective requires Regularization: the complete explanation applies it in this mechanism: where $D$ compares normalized vectors or corresponding spatial tokens. This is a form of regularization: the generation target remains, while the auxiliary loss biases internal learning toward a teacher's representation.",
        "diffusion-transformer": "The stated objective requires Diffusion Transformer: the complete explanation applies it in this mechanism: Representation alignment adds an auxiliary objective that makes an internal feature of a student network resemble a representation produced by a pretrained teacher. REPA applies this idea to a diffusion-transformer using clean-image features from an external visual encoder."
      }
    },
    "reservoir-sampling": {
      "prerequisiteReasons": {
        "probability": "The stated objective requires Probability: the complete explanation applies it in this mechanism: Store records in a dynamic-array capped at $k$ slots. Fill those slots first. For arrival $ik$, draw $j$: if $j\\leq k$, replace slot $j$; otherwise discard the arrival. All $i$ draw outcomes must have equal probability. The new record enters with chance $k/i$. Processing $n$",
        "dynamic-array": "The stated objective requires Dynamic Array: the complete explanation applies it in this mechanism: Store records in a dynamic-array capped at $k$ slots. Fill those slots first. For arrival $ik$, draw $j$: if $j\\leq k$, replace slot $j$; otherwise discard the arrival. All $i$ draw outcomes must have equal probability. The new record enters with chance $k/i$. Processing $n$"
      }
    },
    "residual-network": {
      "prerequisiteReasons": {
        "flow-network": "The stated objective requires Flow Network: the complete explanation applies it in this mechanism: A residual network is the editable view of a current flow-network: a forward residual edge shows unused capacity, while a reverse residual edge shows flow that can be cancelled and rerouted."
      }
    },
    "retrieval-augmented-generation": {
      "prerequisiteReasons": {
        "vector-database": "The stated objective requires Vector Database: the complete explanation applies it in this mechanism: Lewis et al. introduced RAG formulations that combined a pretrained sequence-to-sequence model with a dense Wikipedia index. One formulation conditioned the whole generated sequence on the same retrieved documents; another allowed the latent retrieved document to vary per",
        "context-window": "The stated objective requires Context Window: the complete explanation applies it in this mechanism: 1. Indexing: split source documents into addressable units, attach provenance, compute an embedding or lexical representation, and store searchable records. 2. Query time: transform the question into a retrieval query, rank candidate records, select evidence that fits the",
        "embedding": "The stated objective requires Embedding: the complete explanation applies it in this mechanism: 1. Indexing: split source documents into addressable units, attach provenance, compute an embedding or lexical representation, and store searchable records. 2. Query time: transform the question into a retrieval query, rank candidate records, select evidence that fits the"
      }
    },
    "reward-hacking": {
      "prerequisiteReasons": {
        "image-preference-modeling": "The stated objective requires Image Preference Modeling: the complete explanation applies it in this mechanism: The kl-divergence term limits distribution shift from a known reference; it does not make the proxy correct. InstructGPT used a per-token KL penalty to mitigate over-optimization of its reward model and evaluated with held-out human judgments. The useful lesson extends to",
        "kl-divergence": "The stated objective requires Kullback–Leibler Divergence: the complete explanation applies it in this mechanism: The kl-divergence term limits distribution shift from a known reference; it does not make the proxy correct. InstructGPT used a per-token KL penalty to mitigate over-optimization of its reward model and evaluated with held-out human judgments. The useful lesson extends to",
        "model-calibration": "The stated objective requires Model Calibration: the complete explanation applies it in this mechanism: Track reward–human correlation over training, proxy uncertainty, distance from the reference, diversity, artifacts, and subgroup disagreement. Keep evaluation prompts and raters independent from reward training, use confidence intervals, and stop or roll back when proxy and"
      }
    },
    "ring-attention": {
      "prerequisiteReasons": {
        "flash-attention": "The stated objective requires FlashAttention: the complete explanation applies it in this mechanism: Ring Attention computes the exact same attention output as flash-attention, but for a sequence so long that its keys and values cannot fit on a single device. It splits the sequence across $D$ devices, so each device permanently holds only the queries for its own block plus, at",
        "all-gather": "The stated objective requires All-gather: the complete explanation applies it in this mechanism: Ring Attention computes the exact same attention output as flash-attention, but for a sequence so long that its keys and values cannot fit on a single device. It splits the sequence across $D$ devices, so each device permanently holds only the queries for its own block plus, at",
        "softmax": "The stated objective requires Softmax: the complete explanation applies it in this mechanism: Ring Attention computes the exact same attention output as flash-attention, but for a sequence so long that its keys and values cannot fit on a single device. It splits the sequence across $D$ devices, so each device permanently holds only the queries for its own block plus, at"
      }
    },
    "rmsnorm": {
      "prerequisiteReasons": {
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: RMSNorm (root-mean-square normalization) is a small layer placed between the layers of a neural-network that rescales each activation vector to a stable size: it divides the vector by its root mean square and then multiplies each coordinate by a learned gain. Holding activations"
      }
    },
    "rnnt": {
      "prerequisiteReasons": {
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: An RNN-T has three neural-network parts. An encoder maps each acoustic frame to a representation of the audio observed so far. A prediction network maps the already emitted token prefix to a representation of linguistic history. A joint network combines the two representations",
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: An RNN-T has three neural-network parts. An encoder maps each acoustic frame to a representation of the audio observed so far. A prediction network maps the already emitted token prefix to a representation of linguistic history. A joint network combines the two representations",
        "softmax": "The stated objective requires Softmax: the complete explanation applies it in this mechanism: An RNN-T has three neural-network parts. An encoder maps each acoustic frame to a representation of the audio observed so far. A prediction network maps the already emitted token prefix to a representation of linguistic history. A joint network combines the two representations"
      }
    },
    "roc-curve": {
      "prerequisiteReasons": {
        "confusion-matrix": "The stated objective requires Confusion Matrix: the complete explanation applies it in this mechanism: For every candidate threshold, classify examples at or above the threshold as positive and form a confusion-matrix. The true-positive rate is $TP/(TP+FN)$ and the false-positive rate is $FP/(FP+TN)$. Moving the threshold downward normally labels more examples positive,"
      }
    },
    "roofline-model": {
      "prerequisiteReasons": {
        "arithmetic-intensity": "The stated objective requires Arithmetic Intensity: the complete explanation applies it in this mechanism: The roofline model is a single picture that predicts the fastest a computation could possibly run on a given chip, and — more usefully — which limit is holding it back. From memory-hierarchy and arithmetic-intensity we already have the two numbers that matter: a computation's",
        "memory-hierarchy": "The stated objective requires Memory Hierarchy: the complete explanation applies it in this mechanism: The roofline model is a single picture that predicts the fastest a computation could possibly run on a given chip, and — more usefully — which limit is holding it back. From memory-hierarchy and arithmetic-intensity we already have the two numbers that matter: a computation's"
      }
    },
    "root-process": {
      "prerequisiteReasons": {
        "rank": "The stated objective requires Rank: the complete explanation applies it in this mechanism: The root is the single process singled out to play a special role in a collective that is not symmetric — the one rank that is either the source of the data (it has the value everyone else will get) or the sink (it ends up holding the combined or collected result). It is named"
      }
    },
    "sass": {
      "prerequisiteReasons": {
        "ptx": "The stated objective requires PTX (Parallel Thread Execution): the complete explanation applies it in this mechanism: SASS (Streaming ASSembler) is the actual machine code a GPU runs — the real, binary instructions that the streaming-multiprocessors (the GPU's arithmetic cores, abbreviated SMs) decode and execute directly. It is the final, concrete form that the portable virtual instruction set",
        "streaming-multiprocessor": "The stated objective requires Streaming Multiprocessor: the complete explanation applies it in this mechanism: SASS (Streaming ASSembler) is the actual machine code a GPU runs — the real, binary instructions that the streaming-multiprocessors (the GPU's arithmetic cores, abbreviated SMs) decode and execute directly. It is the final, concrete form that the portable virtual instruction set",
        "compute-capability": "The stated objective requires Compute Capability: the complete explanation applies it in this mechanism: SASS (Streaming ASSembler) is the actual machine code a GPU runs — the real, binary instructions that the streaming-multiprocessors (the GPU's arithmetic cores, abbreviated SMs) decode and execute directly. It is the final, concrete form that the portable virtual instruction set",
        "fma": "The stated objective requires Fused Multiply-Add: the complete explanation applies it in this mechanism: What appears at the SASS level that ptx hides. Because SASS is the real instruction stream, it exposes things ptx abstracts away. The most important are the actual hardware instructions — the named operations the chip provides for one architecture — and the actual register"
      }
    },
    "scalar-field": {
      "prerequisiteReasons": {
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: A scalar field is a rule that attaches a single number to every point of a space. Feed it the location of a point — for example the three coordinates of a spot in a room — and it hands back one ordinary number, like the temperature there. The word scalar is just the name for"
      }
    },
    "scan": {
      "prerequisiteReasons": {
        "collective-operation": "The stated objective requires Collective operation: the complete explanation applies it in this mechanism: Scan (inclusive prefix scan) is the collective-operation in which every process contributes a value and process i receives the reduction-operation of values 0 … i — a running total. Where reduce produces one final answer, scan produces every partial answer along the way: each",
        "reduction-operation": "The stated objective requires Reduction operation: the complete explanation applies it in this mechanism: Scan (inclusive prefix scan) is the collective-operation in which every process contributes a value and process i receives the reduction-operation of values 0 … i — a running total. Where reduce produces one final answer, scan produces every partial answer along the way: each"
      }
    },
    "scatter": {
      "prerequisiteReasons": {
        "collective-operation": "The stated objective requires Collective operation: the complete explanation applies it in this mechanism: Scatter is the collective-operation in which the root-process holds an array split into one chunk per process and sends a different chunk to each. Like broadcast it is one → all from the root, but where broadcast copies the same value to everyone, scatter distributes distinct",
        "root-process": "The stated objective requires Root process: the complete explanation applies it in this mechanism: Scatter is the collective-operation in which the root-process holds an array split into one chunk per process and sends a different chunk to each. Like broadcast it is one → all from the root, but where broadcast copies the same value to everyone, scatter distributes distinct"
      }
    },
    "scheduler": {
      "prerequisiteReasons": {
        "context-switch": "The stated objective requires Context Switch: the complete explanation applies it in this mechanism: A scheduler is the part of the kernel that answers one question, over and over, thousands of times a second: of all the tasks that are ready to run right now, which one should this CPU run next, and for how long? It does not itself swap tasks — that is the job of a",
        "interrupt": "The stated objective requires Interrupt: the complete explanation applies it in this mechanism: A scheduler is the part of the kernel that answers one question, over and over, thousands of times a second: of all the tasks that are ready to run right now, which one should this CPU run next, and for how long? It does not itself swap tasks — that is the job of a"
      }
    },
    "score-based-generative-model": {
      "prerequisiteReasons": {
        "score-function": "The stated objective requires Score Function: the complete explanation applies it in this mechanism: A score-based generative model learns the time-dependent score-function of progressively perturbed data, then solves reverse dynamics from a simple prior distribution to the data distribution.",
        "stochastic-differential-equation": "The stated objective requires Stochastic Differential Equation: the complete explanation applies it in this mechanism: Consider a forward stochastic-differential-equation",
        "differential-equation": "The stated objective requires Differential Equation: the complete explanation applies it in this mechanism: Generation begins with a random draw from the terminal prior, not a fixed data-space point. A numerical solver then follows either the reverse-time SDE or a corresponding deterministic probability-flow differential-equation. These are structured, time-dependent dynamics; direct",
        "differential": "The stated objective requires Differential (Total Derivative): the complete explanation applies it in this mechanism: - score-function - stochastic-differential-equation - differential-equation - differential - denoising-diffusion-probabilistic-model - neural-network",
        "denoising-diffusion-probabilistic-model": "The stated objective requires Denoising Diffusion Probabilistic Model: the complete explanation applies it in this mechanism: This framework includes connections to the discrete denoising-diffusion-probabilistic-model, but discretization, prediction target, solver, and noise schedule remain design choices. More injected noise is not automatically better: stochasticity, corrector steps, and solver",
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: that gradually changes data into a tractable terminal distribution. Under regularity conditions, its reverse-time SDE depends on the score $\\nablax\\log pt(x)$. A neural-network estimates that score from noisy training samples."
      }
    },
    "score-function": {
      "prerequisiteReasons": {
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: It describes the local geometry of a probability-distribution; it is not synonymous with every vector field used to move samples.",
        "gradient": "The stated objective requires Gradient: the complete explanation applies it in this mechanism: - probability-distribution - gradient - vector-field - expectation",
        "vector-field": "The stated objective requires Vector Field: the complete explanation applies it in this mechanism: A general vector-field may encode particle velocity, force, or another dynamics. The score is constrained to be a spatial log-density gradient. A transport velocity can be related to a score through a particular generative construction, but coefficients, drift, and time",
        "expectation": "The stated objective requires Expectation: the complete explanation applies it in this mechanism: where $p(x)0$. The score points in a direction where local log density increases. Its expectation under suitable boundary conditions is zero, so it is not a global direction toward one universal mode."
      }
    },
    "seccomp": {
      "prerequisiteReasons": {
        "system-call": "The stated objective requires System Call: the complete explanation applies it in this mechanism: seccomp (secure computing mode) is a kernel facility that lets a process install a filter restricting which system-calls it — and every child it later spawns — is allowed to make. The filter is a tiny program the kernel runs on each system-call, inspecting the syscall number"
      }
    },
    "sharding": {
      "prerequisiteReasons": {
        "key-value": "The stated objective requires Key–Value Mapping: the complete explanation applies it in this mechanism: replication copies the whole dataset onto every machine, so it never lets the data outgrow one machine. Sharding (horizontal partitioning) is the complementary move: split the dataset so that each machine — each shard — owns only a subset of the records, and the union of the",
        "hash-map": "The stated objective requires Hash Map: the complete explanation applies it in this mechanism: replication copies the whole dataset onto every machine, so it never lets the data outgrow one machine. Sharding (horizontal partitioning) is the complementary move: split the dataset so that each machine — each shard — owns only a subset of the records, and the union of the",
        "replication": "The stated objective requires Replication: the complete explanation applies it in this mechanism: replication copies the whole dataset onto every machine, so it never lets the data outgrow one machine. Sharding (horizontal partitioning) is the complementary move: split the dataset so that each machine — each shard — owns only a subset of the records, and the union of the"
      }
    },
    "shared-memory": {
      "prerequisiteReasons": {
        "gpu-memory-spaces": "The stated objective requires GPU Memory Spaces: the complete explanation applies it in this mechanism: Shared memory is one of the gpu-memory-spaces — specifically the fast, on-chip region whose scope is exactly one block of the cuda-thread-hierarchy. Recall the two facts those prerequisites established: shared memory lives on the fast on-chip rung (roughly as quick as a thread's",
        "cuda-thread-hierarchy": "The stated objective requires CUDA Thread Hierarchy: the complete explanation applies it in this mechanism: Shared memory is one of the gpu-memory-spaces — specifically the fast, on-chip region whose scope is exactly one block of the cuda-thread-hierarchy. Recall the two facts those prerequisites established: shared memory lives on the fast on-chip rung (roughly as quick as a thread's",
        "barrier": "The stated objective requires Barrier: the complete explanation applies it in this mechanism: Shared memory is one of the gpu-memory-spaces — specifically the fast, on-chip region whose scope is exactly one block of the cuda-thread-hierarchy. Recall the two facts those prerequisites established: shared memory lives on the fast on-chip rung (roughly as quick as a thread's"
      }
    },
    "shortest-path": {
      "prerequisiteReasons": {
        "graph": "The stated objective requires Graph (Adjacency List): the complete explanation applies it in this mechanism: A shortest path in a weighted graph is a route whose edge weights add to the smallest possible total between two vertices.",
        "heap": "The stated objective requires Heap: the complete explanation applies it in this mechanism: When all edge weights are nonnegative, Dijkstra's method maintains a tentative distance for each vertex. A heap repeatedly returns the unsettled vertex with the smallest tentative distance. Once that vertex is removed, no later route can improve it: every alternative must first"
      }
    },
    "simt": {
      "prerequisiteReasons": {
        "warp": "The stated objective requires Warp: the complete explanation applies it in this mechanism: SIMT stands for Single Instruction, Multiple Threads, and it is the name for the GPU's execution model — the one the warp node described mechanically but never had to name. The model is exactly this: the hardware issues one instruction to a whole warp of 32 threads, and all 32"
      }
    },
    "sk-buff": {
      "prerequisiteReasons": {
        "network-stack": "The stated objective requires Network Stack: the complete explanation applies it in this mechanism: The socket buffer, written skbuff in the kernel, is the data structure that represents one packet the whole time it is inside the network-stack — the single object every layer holds, reads, and edits as the packet travels down to the wire or up from it. Its defining trick is how"
      }
    },
    "skewness": {
      "prerequisiteReasons": {
        "expectation": "The stated objective requires Expectation: the complete explanation applies it in this mechanism: Skewness is the asymmetry of a distribution — whether its probability leans symmetrically about a center or trails off into one long tail. The cleanest way to see it is to compare a distribution's three \"centers.\" The mean is the expectation $E[X]$, the probability-weighted",
        "cumulative-distribution-function": "The stated objective requires Cumulative Distribution Function: the complete explanation applies it in this mechanism: Skewness is the asymmetry of a distribution — whether its probability leans symmetrically about a center or trails off into one long tail. The cleanest way to see it is to compare a distribution's three \"centers.\" The mean is the expectation $E[X]$, the probability-weighted"
      }
    },
    "sliding-window-attention": {
      "prerequisiteReasons": {
        "transformer-attention": "The stated objective requires Transformer attention: the complete explanation applies it in this mechanism: In ordinary transformer-attention every query attends to all $n$ keys, so the work scales like $n^2$. Sliding-window attention restricts each query to a fixed local window — only the most recent $w$ keys — dropping the cost to $n\\cdot w$. A single layer then sees only $w$ tokens",
        "softmax": "The stated objective requires Softmax: the complete explanation applies it in this mechanism: i.e. itself and the $w-1$ keys immediately before it (we use the causal version — look back, never forward — which is the common one for language models). Every key outside that band is masked out: its score is discarded before the softmax, so it contributes zero weight. The"
      }
    },
    "socket": {
      "prerequisiteReasons": {
        "vfs": "The stated objective requires Virtual File System: the complete explanation applies it in this mechanism: A socket is a communication endpoint for the network that the kernel hands to a program as a file-descriptor — the same small per-process integer, and the same coat-check ticket, that a program gets when it opens a file. Because the endpoint is a descriptor, the program talks to",
        "file-descriptor": "The stated objective requires File Descriptor: the complete explanation applies it in this mechanism: A socket is a communication endpoint for the network that the kernel hands to a program as a file-descriptor — the same small per-process integer, and the same coat-check ticket, that a program gets when it opens a file. Because the endpoint is a descriptor, the program talks to",
        "system-call": "The stated objective requires System Call: the complete explanation applies it in this mechanism: A socket is a communication endpoint for the network that the kernel hands to a program as a file-descriptor — the same small per-process integer, and the same coat-check ticket, that a program gets when it opens a file. Because the endpoint is a descriptor, the program talks to"
      }
    },
    "speculative-decoding": {
      "prerequisiteReasons": {
        "transformer-attention": "The stated objective requires Transformer attention: the complete explanation applies it in this mechanism: The setup and notation. We have two models over the same vocabulary. The target model $T$ is the one we actually want to sample from; running its forward pass is expensive. The draft model $D$ is small and fast (e.g. a model with far fewer layers). Both are ordinary",
        "kv-cache": "The stated objective requires KV-cache: the complete explanation applies it in this mechanism: Generating text one token at a time runs the big \"target\" model once per token, and that pass is memory-bound — most of the time goes to streaming the model's weights and the kv-cache through memory, not to arithmetic, so scoring one token costs almost as much as scoring",
        "softmax": "The stated objective requires Softmax: the complete explanation applies it in this mechanism: The setup and notation. We have two models over the same vocabulary. The target model $T$ is the one we actually want to sample from; running its forward pass is expensive. The draft model $D$ is small and fast (e.g. a model with far fewer layers). Both are ordinary",
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: The setup and notation. We have two models over the same vocabulary. The target model $T$ is the one we actually want to sample from; running its forward pass is expensive. The draft model $D$ is small and fast (e.g. a model with far fewer layers). Both are ordinary"
      }
    },
    "sql": {
      "prerequisiteReasons": {
        "relational-model": "The stated objective requires Relational Model: the complete explanation applies it in this mechanism: SQL is the declarative query language over the relational-model. Its defining commitment is in the word declarative: you write down what result you want — which columns, drawn from which relations, joined on which key, kept by which filter, summarized by which aggregate — and"
      }
    },
    "stack": {
      "prerequisiteReasons": {
        "dynamic-array": "The stated objective requires Dynamic Array: the complete explanation applies it in this mechanism: A stack is a collection of items that you can only touch at one end, called the top. You add an item by pushing it onto the top, and you remove an item by popping the one currently on top — so the item you get back is always the most recently added one still present. This"
      }
    },
    "state-space-model": {
      "prerequisiteReasons": {
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: A neural-network maps one input to one output: feed it $x$, it returns $f(x)$. It has no built-in notion of sequence or carried memory — show it the tenth word of a sentence and it has forgotten the first nine. A state space model adds exactly that missing piece: a state $h$"
      }
    },
    "static-type-checking": {
      "prerequisiteReasons": {
        "dynamic-typing": "The stated objective requires Dynamic Typing: the complete explanation applies it in this mechanism: Static type checking analyzes expressions before they run and reports code whose values cannot satisfy the operations or interfaces declared for them. It complements dynamic-typing: the checker reasons from annotations and inference, while the runtime still executes the original"
      }
    },
    "stochastic-differential-equation": {
      "prerequisiteReasons": {
        "differential-equation": "The stated objective requires Differential Equation: the complete explanation applies it in this mechanism: A stochastic differential equation (SDE) augments a differential-equation with random forcing, usually written as a drift term times $dt$ plus a diffusion term times a brownian-motion increment.",
        "ito-integral": "The stated objective requires Itô Integral: the complete explanation applies it in this mechanism: where the second term is an ito-integral. This interpretation is necessary because Brownian motion has no ordinary derivative.",
        "brownian-motion": "The stated objective requires Brownian Motion: the complete explanation applies it in this mechanism: A stochastic differential equation (SDE) augments a differential-equation with random forcing, usually written as a drift term times $dt$ plus a diffusion term times a brownian-motion increment."
      }
    },
    "stochastic-process": {
      "prerequisiteReasons": {
        "random-variable": "The stated objective requires Random Variable: the complete explanation applies it in this mechanism: A stochastic process is a family $\\{Xt:t\\in T\\}$ of random-variables indexed by time or another coordinate; one outcome of the experiment produces a complete path $t\\mapsto Xt$.",
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: For each fixed time $t$, $Xt$ has a probability-distribution. Across times, the variables also have a joint distribution that describes dependence. Knowing every one-time distribution is not enough: two processes can have identical marginals but very different path behavior."
      }
    },
    "streaming-multiprocessor": {
      "prerequisiteReasons": {
        "cpu-vs-gpu": "The stated objective requires CPU vs GPU: the complete explanation applies it in this mechanism: A streaming multiprocessor (an SM) is the GPU's core compute building block, and a GPU is essentially an array of many of them — about 132 on an NVIDIA H100. Each SM bundles four things: many simple arithmetic lanes (in the prerequisite cpu-vs-gpu sense — a lane is one position",
        "memory-hierarchy": "The stated objective requires Memory Hierarchy: the complete explanation applies it in this mechanism: - A block of fast on-chip scratchpad memory plus a small cache. This is storage on a high rung of the memory-hierarchy — far faster than the chip's distant main memory (HBM), which sits several rungs down the ladder. On an H100 it is 228 KB per SM. Threads working on the same"
      }
    },
    "structured-output": {
      "prerequisiteReasons": {
        "softmax": "The stated objective requires Softmax: the complete explanation applies it in this mechanism: Structured output (a.k.a. constrained decoding, grammar-guided generation) forces a language model's text generation to conform to a formal structure — a JSON schema, a regular expression, a grammar. The trick: at every generation step, before turning the model's scores into",
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: A language model generates text one token at a time (a token is a chunk of text — a word, a fragment, a punctuation mark — drawn from a fixed vocabulary, the full list of tokens the model knows). At each step the model emits one real number per vocabulary token, called a logit"
      }
    },
    "subject-driven-generation": {
      "prerequisiteReasons": {
        "denoising-diffusion-probabilistic-model": "The stated objective requires Denoising Diffusion Probabilistic Model: the complete explanation applies it in this mechanism: DreamBooth adapts a pretrained denoising-diffusion-probabilistic-model using prompts that pair a rare identifier with a class noun. The target is not memorization of the training backgrounds: it is identity preservation across new poses, scenes, views, and styles.",
        "regularization": "The stated objective requires Regularization: the complete explanation applies it in this mechanism: Few examples create competing risks. Too little adaptation loses subject details; too much can reproduce backgrounds, collapse diversity, or shift the meaning of the whole class. DreamBooth's class-specific prior-preservation term generates class images from the frozen base and",
        "text-image-attribute-binding": "The stated objective requires Text–Image Attribute Binding: the complete explanation applies it in this mechanism: Hold out views and backgrounds. During training, track subject similarity alongside generic class quality, prompt adherence, diversity, and memorization. Ablate identifier choice, caption variation, update scope, prior-loss weight, synthetic class set, learning rate, and"
      }
    },
    "supervised-contrastive-learning": {
      "prerequisiteReasons": {
        "embedding": "The stated objective requires Embedding: the complete explanation applies it in this mechanism: Supervised contrastive learning uses labels to shape an embedding space. For each anchor, other examples with the same class are positives and examples with different classes are negatives.",
        "softmax": "The stated objective requires Softmax: the complete explanation applies it in this mechanism: The denominator is a softmax over similarities. Minimizing the loss raises similarity to every same-label positive relative to different-label examples. Unlike ordinary cross-entropy on classifier logits, the objective directly organizes representation geometry and can use",
        "loss-function": "The stated objective requires Loss function: the complete explanation applies it in this mechanism: Let $zi$ be a normalized embedding for anchor $i$, $P(i)$ its positive indices, $A(i)$ all other compared examples, and $\\tau0$ a temperature. A common supervised contrastive loss-function is"
      }
    },
    "swap": {
      "prerequisiteReasons": {
        "demand-paging": "The stated objective requires Demand Paging: the complete explanation applies it in this mechanism: Swap is a reserved area on disk — a dedicated disk partition or a regular file — that the kernel uses as an overflow extension of physical RAM. When RAM fills up and the kernel needs a free physical frame (a 4 KB block of real memory) but has none, it picks a resident page that",
        "page-fault": "The stated objective requires Page Fault: the complete explanation applies it in this mechanism: Paging in (return). Sometime later the program executes an instruction that touches that very page. The hardware finds the entry \"not present\" and raises a page-fault — the ordinary demand-paging fault, restartable as always. But now the handler sees that this is not a brand-new"
      }
    },
    "symmetric-antisymmetric-decomposition": {
      "prerequisiteReasons": {
        "matrix-multiplication": "The stated objective requires Matrix Multiplication: the complete explanation applies it in this mechanism: Here M + Mᵀ and M − Mᵀ are formed entry-by-entry (add or subtract the matching numbers), and ½(…) halves every entry. These are not the matrix product of the prerequisite matrix-multiplication; they are the simpler entrywise sum, difference, and scaling — the same arithmetic"
      }
    },
    "system-call": {
      "prerequisiteReasons": {
        "user-mode-vs-kernel-mode": "The stated objective requires User Mode vs Kernel Mode: the complete explanation applies it in this mechanism: A system call is the controlled gateway by which a program running in user mode asks the kernel to perform a privileged service on its behalf — opening a file, reading from a disk, sending a network packet. It is the one sanctioned way to cross the user-mode-vs-kernel-mode"
      }
    },
    "tcp": {
      "prerequisiteReasons": {
        "network-stack": "The stated objective requires Network Stack: the complete explanation applies it in this mechanism: TCP (Transmission Control Protocol) is the rule set that runs at the transport layer of the network-stack — the layer whose header names which program — and its job is to turn that stack's crude, unreliable delivery into a clean, reliable one. By itself the stack only makes a",
        "socket": "The stated objective requires Socket: the complete explanation applies it in this mechanism: From network-stack we have the whole road between two programs. A program pushes bytes into one mouth of the pipe (the socket endpoint), and they descend layer by layer — each layer wrapping them in one more header, a small label glued to the front saying what that layer needs."
      }
    },
    "telemetry-context": {
      "prerequisiteReasons": {
        "key-value": "The stated objective requires Key–Value Mapping: the complete explanation applies it in this mechanism: Telemetry context is the small, execution-scoped bundle of correlation data associated with the operation currently running. Conceptually it is an immutable key-value mapping: keys identify fields such as a trace ID, span ID, or baggage item, and values hold the corresponding"
      }
    },
    "telemetry-metric": {
      "prerequisiteReasons": {
        "measurement": "The stated objective requires Measurement: the complete explanation applies it in this mechanism: A telemetry metric turns repeated runtime measurement values into a named time series or aggregation. It answers quantitative questions such as “how many requests completed?”, “how much memory is used now?”, and “how are request durations distributed?”"
      }
    },
    "telemetry-signal": {
      "prerequisiteReasons": {
        "event": "The stated objective requires Event: the complete explanation applies it in this mechanism: A telemetry signal is an output a running system emits so an observer can learn about activity that would otherwise remain internal. Its payload is built from a discrete event, a numeric measurement, or a combination of both.",
        "measurement": "The stated objective requires Measurement: the complete explanation applies it in this mechanism: A telemetry signal is an output a running system emits so an observer can learn about activity that would otherwise remain internal. Its payload is built from a discrete event, a numeric measurement, or a combination of both."
      }
    },
    "temporal-data-leakage": {
      "prerequisiteReasons": {
        "dataset-lineage": "The stated objective requires Dataset Lineage: the complete explanation applies it in this mechanism: A chronological split is necessary but not sufficient. Fit scalers, feature selection, imputation, and thresholds only on each training interval. Reconstruct features with point-in-time joins and record their source versions in dataset-lineage. Add a gap when labels or features",
        "concept-drift": "The stated objective requires Concept Drift: the complete explanation applies it in this mechanism: For example, with months 1–6, one backtest can train on 1–3, leave month 4 as a one-month gap, and test on 5; the next can train on 1–4, gap 5, and test on 6. This prevents the evaluated model from learning directly from its future, while multiple cutoffs reveal time variation."
      }
    },
    "temporal-knowledge-graph": {
      "prerequisiteReasons": {
        "graph": "The stated objective requires Graph (Adjacency List): the complete explanation applies it in this mechanism: A temporal knowledge graph extends a knowledge-graph—and its underlying graph structure—with time-qualified facts. Instead of overwriting “Alice works at A” when a later assertion names B, it can preserve both claims with their applicable intervals and provenance.",
        "knowledge-graph": "The stated objective requires Knowledge Graph: the complete explanation applies it in this mechanism: A temporal knowledge graph extends a knowledge-graph—and its underlying graph structure—with time-qualified facts. Instead of overwriting “Alice works at A” when a later assertion names B, it can preserve both claims with their applicable intervals and provenance.",
        "event": "The stated objective requires Event: the complete explanation applies it in this mechanism: Suppose one event records “Alice joined A in 2020,” and a later event says “Alice moved to B in 2023.” The graph can close the first edge at 2023 and open the second, while retaining both. A current-state query selects edges valid now; an as-of query reconstructs the state at a",
        "agent-memory": "The stated objective requires Agent Memory: the complete explanation applies it in this mechanism: For agent-memory, separate raw episodes from extracted entities and relations so answers can cite their evidence. Community summaries may speed broad retrieval, but they are derived artifacts that require timestamps and invalidation when underlying edges change."
      }
    },
    "tensor": {
      "prerequisiteReasons": {
        "dynamic-array": "The stated objective requires Dynamic Array: the complete explanation applies it in this mechanism: A dynamic-array gives two ingredients needed to understand a tensor: ordered storage and integer indexing. A tensor extends that idea to several axes while requiring a rectangular shape. Shape [2, 3], for example, means two rows of three elements. Index [1, 2] selects the second",
        "numeric-precision-formats": "The stated objective requires Numeric Precision Formats: the complete explanation applies it in this mechanism: The other ingredient is a single element type from numeric-precision-formats. A tensor declared as float32[2,3] contains six values, each encoded in the same 32-bit floating-point format. Shape and element type are distinct: float32[2,3] and int8[2,3] have the same locations but"
      }
    },
    "tensor-core": {
      "prerequisiteReasons": {
        "streaming-multiprocessor": "The stated objective requires Streaming Multiprocessor: the complete explanation applies it in this mechanism: A Tensor Core is a specialized piece of hardware sitting inside each streaming-multiprocessor whose one job is to compute a small fixed-size matrix-multiplication — and to do it as a single hardware operation. Where the SM's ordinary arithmetic lanes (the \"CUDA cores\" from",
        "matrix-multiplication": "The stated objective requires Matrix Multiplication: the complete explanation applies it in this mechanism: A Tensor Core is a specialized piece of hardware sitting inside each streaming-multiprocessor whose one job is to compute a small fixed-size matrix-multiplication — and to do it as a single hardware operation. Where the SM's ordinary arithmetic lanes (the \"CUDA cores\" from",
        "fma": "The stated objective requires Fused Multiply-Add: the complete explanation applies it in this mechanism: The prerequisite streaming-multiprocessor established what an SM's ordinary lanes are: simple arithmetic positions (NVIDIA's \"CUDA cores\"), each able to do one multiply or one add per cycle. The single most common thing those lanes do is a fused multiply-add (fma): one hardware"
      }
    },
    "tensor-parallelism": {
      "prerequisiteReasons": {
        "matrix-multiplication": "The stated objective requires Matrix Multiplication: the complete explanation applies it in this mechanism: Tensor parallelism splits a single layer's weight matrices across GPUs so that one matrix-multiplication too big for one device is computed as several smaller ones in parallel. Unlike data parallelism (which replicates the whole neural-network and splits the data), tensor",
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: Tensor parallelism splits a single layer's weight matrices across GPUs so that one matrix-multiplication too big for one device is computed as several smaller ones in parallel. Unlike data parallelism (which replicates the whole neural-network and splits the data), tensor",
        "all-reduce": "The stated objective requires All-reduce: the complete explanation applies it in this mechanism: Tensor parallelism splits a single layer's weight matrices across GPUs so that one matrix-multiplication too big for one device is computed as several smaller ones in parallel. Unlike data parallelism (which replicates the whole neural-network and splits the data), tensor"
      }
    },
    "tensorrt": {
      "prerequisiteReasons": {
        "jit-vs-aot-compilation": "The stated objective requires JIT vs AOT Compilation: the complete explanation applies it in this mechanism: TensorRT is NVIDIA's inference optimizer and runtime: it takes a trained neural-network and compiles it, ahead of time (per jit-vs-aot-compilation), into a GPU-specific \"engine\" that runs the same forward pass much faster. It is not a training framework and adds no new math — it",
        "cuda-kernel": "The stated objective requires CUDA Kernel: the complete explanation applies it in this mechanism: TensorRT is NVIDIA's inference optimizer and runtime: it takes a trained neural-network and compiles it, ahead of time (per jit-vs-aot-compilation), into a GPU-specific \"engine\" that runs the same forward pass much faster. It is not a training framework and adds no new math — it",
        "roofline-model": "The stated objective requires Roofline Model: the complete explanation applies it in this mechanism: TensorRT is NVIDIA's inference optimizer and runtime: it takes a trained neural-network and compiles it, ahead of time (per jit-vs-aot-compilation), into a GPU-specific \"engine\" that runs the same forward pass much faster. It is not a training framework and adds no new math — it",
        "quantization": "The stated objective requires Quantization: the complete explanation applies it in this mechanism: TensorRT is NVIDIA's inference optimizer and runtime: it takes a trained neural-network and compiles it, ahead of time (per jit-vs-aot-compilation), into a GPU-specific \"engine\" that runs the same forward pass much faster. It is not a training framework and adds no new math — it",
        "neural-network": "The stated objective requires Neural network: the complete explanation applies it in this mechanism: TensorRT is NVIDIA's inference optimizer and runtime: it takes a trained neural-network and compiles it, ahead of time (per jit-vs-aot-compilation), into a GPU-specific \"engine\" that runs the same forward pass much faster. It is not a training framework and adds no new math — it"
      }
    },
    "test-time-compute": {
      "prerequisiteReasons": {
        "chain-of-thought": "The stated objective requires Chain-of-Thought: the complete explanation applies it in this mechanism: Test-time compute is the idea that a model's accuracy can be raised by spending more computation at inference time — when it is actually answering a question — as a lever distinct from spending more during training. Inference time (also called test time) is the moment the"
      }
    },
    "text-image-attribute-binding": {
      "prerequisiteReasons": {
        "transformer-attention": "The stated objective requires Transformer attention: the complete explanation applies it in this mechanism: Conditioning architecture can influence binding. transformer-attention can let image locations interact selectively with text tokens, while pooled global modulation applies one shared condition to many locations. Yet architecture alone does not identify the cause. Ambiguous",
        "multi-head-attention": "The stated objective requires Multi-Head Attention: the complete explanation applies it in this mechanism: Conditioning architecture can influence binding. transformer-attention can let image locations interact selectively with text tokens, while pooled global modulation applies one shared condition to many locations. Yet architecture alone does not identify the cause. Ambiguous"
      }
    },
    "thread": {
      "prerequisiteReasons": {
        "process": "The stated objective requires Process: the complete explanation applies it in this mechanism: A thread is an independent flow of execution running inside a process — one \"line\" of activity stepping through the program's instructions. A plain process has exactly one such flow; a multithreaded process has several that run, or rapidly take turns, at the same time. The",
        "context-switch": "The stated objective requires Context Switch: the complete explanation applies it in this mechanism: (How the kernel then juggles many flows onto a few CPUs — rapidly switching the running flow's execution state in and out — a context-switch — under a component called the scheduler — is a separate topic. What matters here is the structure, not the timing.)"
      }
    },
    "thread-synchronization": {
      "prerequisiteReasons": {
        "cuda-thread-hierarchy": "The stated objective requires CUDA Thread Hierarchy: the complete explanation applies it in this mechanism: Recall from cuda-thread-hierarchy that a block is a bundle of threads placed together on one chip unit, and that the hardware does not run those threads all at once — it slices the block into groups of 32 (warps) and runs the groups at its own pace, interleaving them so that one",
        "warp": "The stated objective requires Warp: the complete explanation applies it in this mechanism: Recall from cuda-thread-hierarchy that a block is a bundle of threads placed together on one chip unit, and that the hardware does not run those threads all at once — it slices the block into groups of 32 (warps) and runs the groups at its own pace, interleaving them so that one",
        "streaming-multiprocessor": "The stated objective requires Streaming Multiprocessor: the complete explanation applies it in this mechanism: The prerequisite cuda-thread-hierarchy established the layering you need here: grid ⊃ block ⊃ warp ⊃ thread. A thread is one independent execution of your GPU function; a block is a fixed-size group of threads guaranteed to share one physical unit (the streaming-multiprocessor)",
        "barrier": "The stated objective requires Barrier: the complete explanation applies it in this mechanism: Recall from cuda-thread-hierarchy that a block is a bundle of threads placed together on one chip unit, and that the hardware does not run those threads all at once — it slices the block into groups of 32 (warps) and runs the groups at its own pace, interleaving them so that one"
      }
    },
    "three-dimensional-parallelism": {
      "prerequisiteReasons": {
        "data-parallelism": "The stated objective requires Data parallelism: the complete explanation applies it in this mechanism: 3D parallelism trains a model too large for any single strategy by running all three of tensor-parallelism, pipeline-parallelism, and data-parallelism at the same time. The three are orthogonal: tensor-parallelism (TP) splits each layer's weight matrices across a few GPUs;",
        "tensor-parallelism": "The stated objective requires Tensor parallelism: the complete explanation applies it in this mechanism: 3D parallelism trains a model too large for any single strategy by running all three of tensor-parallelism, pipeline-parallelism, and data-parallelism at the same time. The three are orthogonal: tensor-parallelism (TP) splits each layer's weight matrices across a few GPUs;",
        "pipeline-parallelism": "The stated objective requires Pipeline parallelism: the complete explanation applies it in this mechanism: 3D parallelism trains a model too large for any single strategy by running all three of tensor-parallelism, pipeline-parallelism, and data-parallelism at the same time. The three are orthogonal: tensor-parallelism (TP) splits each layer's weight matrices across a few GPUs;",
        "all-reduce": "The stated objective requires All-reduce: the complete explanation applies it in this mechanism: - TP is communication-heavy. Per the tensor-parallelism node it does an all-reduce every layer (the source counts two per transformer block — one for the MLP, one for attention). That much collective traffic only stays cheap on the fastest interconnect, so TP is confined within"
      }
    },
    "tiny-subset-overfit-test": {
      "prerequisiteReasons": {
        "loss-function": "The stated objective requires Loss function: the complete explanation applies it in this mechanism: A tiny-subset overfit test trains a model repeatedly on a handful of examples and checks whether it can drive their data loss-function very low. It is a plumbing and optimization diagnostic, not evidence that the model will generalize.",
        "gradient-descent": "The stated objective requires Gradient descent: the complete explanation applies it in this mechanism: Full training entangles data loading, augmentation, distributed communication, numerical precision, gradient-descent, and evaluation. Restricting the run to perhaps one batch makes failures cheaper and easier to localize. Use deterministic inputs, turn off stochastic",
        "regularization": "The stated objective requires Regularization: the complete explanation applies it in this mechanism: Full training entangles data loading, augmentation, distributed communication, numerical precision, gradient-descent, and evaluation. Restricting the run to perhaps one batch makes failures cheaper and easier to localize. Use deterministic inputs, turn off stochastic"
      }
    },
    "tlb": {
      "prerequisiteReasons": {
        "page-table": "The stated objective requires Page Table: the complete explanation applies it in this mechanism: A translation lookaside buffer (TLB) is a small, very fast hardware cache, built into the CPU, that remembers recent virtual-page-to-physical-frame translations so the processor can skip the expensive lookup through the page-table on almost every memory access. The page-table is",
        "mmu": "The stated objective requires Memory Management Unit: the complete explanation applies it in this mechanism: The translation lookaside buffer is the fix. Define it precisely: the TLB is a small, very fast cache — a few dozen to a few thousand slots — sitting inside the mmu, the hardware unit that performs address translation on every memory access, where each slot stores one recently",
        "context-switch": "The stated objective requires Context Switch: the complete explanation applies it in this mechanism: But the TLB is full of A's translations — A's virtual-page-to-frame pairs. The instant the register swings to B's page-table, every one of those cached translations is wrong: B's same page numbers map to entirely different frames. If the hardware kept trusting them, B would read"
      }
    },
    "token-and-duration-transducer": {
      "prerequisiteReasons": {
        "rnnt": "The stated objective requires RNN Transducer: the complete explanation applies it in this mechanism: A Token-and-Duration Transducer (TDT) is an rnnt whose decoder predicts both a transcript token (or blank) and a duration, allowing one transition to consume several acoustic frames instead of exactly one.",
        "probability-distribution": "The stated objective requires Probability Distribution: the complete explanation applies it in this mechanism: A TDT retains the transducer structure but adds a duration head. Alongside the token probability-distribution, it produces a distribution over permitted frame advances. A transition now chooses a token or blank and a duration d. A blank advances from (t, u) to (t + d, u); a"
      }
    },
    "trace-span": {
      "prerequisiteReasons": {
        "event": "The stated objective requires Event: the complete explanation applies it in this mechanism: A trace span is the record of one bounded operation: it has a start, an end, a name, and correlation identifiers. It may also contain attributes, status, and point-in-time event records that occurred during the operation."
      }
    },
    "transaction": {
      "prerequisiteReasons": {
        "atomic-operation": "The stated objective requires Atomic Operation: the complete explanation applies it in this mechanism: A transaction is a unit of database work that the system promises to treat as all-or-nothing. You group several statements — reads and writes that together accomplish one logical change — and the database guarantees that either all of their effects take hold (the transaction"
      }
    },
    "transaction-isolation": {
      "prerequisiteReasons": {
        "transaction": "The stated objective requires Transaction (ACID): the complete explanation applies it in this mechanism: Isolation is the I of ACID — the property that governs what concurrently-running transactions are allowed to see of each other's in-progress, not-yet-committed work. A single transaction running alone is already safe by Atomicity; the danger appears the instant two or more run"
      }
    },
    "tree-search-decoding": {
      "prerequisiteReasons": {
        "test-time-compute": "The stated objective requires Test-Time Compute: the complete explanation applies it in this mechanism: Tree-search decoding is a concrete way to spend test-time-compute: instead of producing one reasoning chain left-to-right and committing to each next step as it comes, you treat generation as a search over a tree of partial continuations. At a choice point you branch into"
      }
    },
    "trie": {
      "prerequisiteReasons": {
        "hash-map": "The stated objective requires Hash Map: the complete explanation applies it in this mechanism: A trie (also called a prefix tree) stores a set of strings as a tree in which every edge is labelled with a single character, so the path from the root down to any node spells out one prefix, and strings that begin the same way share the same upper path. In the representation"
      }
    },
    "typed-dict": {
      "prerequisiteReasons": {
        "static-type-checking": "The stated objective requires Static Type Checking: the complete explanation applies it in this mechanism: A TypedDict describes the expected shape of a Python dictionary to a static type checker: each permitted string key has its own value type, and each key is required or optional. At runtime, the value remains an ordinary dict; the declaration adds no automatic input validation or",
        "hash-map": "The stated objective requires Hash Map: the complete explanation applies it in this mechanism: A checker can infer that movie[\"year\"] is an integer, reject {\"name\": 1982, \"year\": \"Blade Runner\"}, and report a missing required key. The runtime object is still the same hash-map used by an ordinary Python dict: Movie(name=\"Arrival\", year=2016) returns a dict, and"
      }
    },
    "udp": {
      "prerequisiteReasons": {
        "network-stack": "The stated objective requires Network Stack: the complete explanation applies it in this mechanism: UDP (User Datagram Protocol) is the minimal transport layer of the network-stack — the bare-bones alternative to TCP. Recall that in the network-stack the transport layer is the one whose header names which program the bytes are for, using a port (a number identifying one"
      }
    },
    "union-find": {
      "prerequisiteReasons": {
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: - arithmetic"
      }
    },
    "user-mode-vs-kernel-mode": {
      "prerequisiteReasons": {
        "kernel": "The stated objective requires Kernel: the complete explanation applies it in this mechanism: A CPU executes instructions in one of two privilege levels — a piece of internal state, set in the processor's own circuitry, that decides how much a running instruction is allowed to do. Kernel mode (on x86 chips, called Ring 0) is the fully privileged level: code there may run",
        "interrupt": "The stated objective requires Interrupt: the complete explanation applies it in this mechanism: 1. a system call — the program deliberately executes the special \"enter the kernel\" instruction, the elevator up; 2. an interrupt — a hardware device (a disk finishing, a key pressed) signals the CPU; 3. a fault — the running instruction did something illegal, so the CPU traps."
      }
    },
    "variational-autoencoder": {
      "prerequisiteReasons": {
        "autoencoder": "The stated objective requires Autoencoder: the complete explanation applies it in this mechanism: A variational autoencoder (VAE) combines an encoder–decoder autoencoder shape with a probabilistic latent-variable-model: the encoder approximates the posterior over latent codes, and both networks train by maximizing a reparameterized variational lower bound.",
        "latent-variable-model": "The stated objective requires Latent-Variable Model: the complete explanation applies it in this mechanism: A variational autoencoder (VAE) combines an encoder–decoder autoencoder shape with a probabilistic latent-variable-model: the encoder approximates the posterior over latent codes, and both networks train by maximizing a reparameterized variational lower bound.",
        "variational-inference": "The stated objective requires Variational Inference: the complete explanation applies it in this mechanism: The first term rewards reconstruction under the decoder. The second comes from variational-inference and keeps each encoded distribution near the prior, making the latent space sampleable. For a Gaussian encoder, the reparameterization-trick writes",
        "reparameterization-trick": "The stated objective requires Reparameterization Trick: the complete explanation applies it in this mechanism: The first term rewards reconstruction under the decoder. The second comes from variational-inference and keeps each encoded distribution near the prior, making the latent space sampleable. For a Gaussian encoder, the reparameterization-trick writes"
      }
    },
    "variational-inference": {
      "prerequisiteReasons": {
        "bayes-rule": "The stated objective requires Bayes' Rule: the complete explanation applies it in this mechanism: Variational inference approximates an intractable posterior by selecting, from a tractable family, the distribution that minimizes kl-divergence to the posterior implied by bayes-rule.",
        "kl-divergence": "The stated objective requires Kullback–Leibler Divergence: the complete explanation applies it in this mechanism: Variational inference approximates an intractable posterior by selecting, from a tractable family, the distribution that minimizes kl-divergence to the posterior implied by bayes-rule."
      }
    },
    "vector-database": {
      "prerequisiteReasons": {
        "embedding": "The stated objective requires Embedding: the complete explanation applies it in this mechanism: A vector database is a data store built specifically to hold large collections of embeddings — the dense meaning-vectors that place similar inputs at nearby points in space — and to answer \"which stored vectors are most similar to this query vector?\" quickly, by",
        "nearest-neighbor-search": "The stated objective requires Nearest-Neighbor Search: the complete explanation applies it in this mechanism: A vector database is a data store built specifically to hold large collections of embeddings — the dense meaning-vectors that place similar inputs at nearby points in space — and to answer \"which stored vectors are most similar to this query vector?\" quickly, by"
      }
    },
    "vector-dot-product": {
      "prerequisiteReasons": {
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: — a sum of products, i.e. pure arithmetic. Geometrically it measures how much the two vectors point the same way (it equals $\\lVert a\\rVert\\,\\lVert b\\rVert\\cos\\theta$). It is the atomic operation behind every entry of a matrix-multiplication: each output entry is one row ·"
      }
    },
    "vector-field": {
      "prerequisiteReasons": {
        "arithmetic": "The stated objective requires Arithmetic (+ − × ÷): the complete explanation applies it in this mechanism: Start with what a vector is, since the whole idea rests on it. A number on its own — say 3 — is a scalar: a bare magnitude, the sort of thing the four operations of arithmetic act on directly. A vector is an ordered list of such numbers written together, like (0, 1) or (−1, 0),",
        "scalar-field": "The stated objective requires Scalar Field: the complete explanation applies it in this mechanism: Now the central object. A field is the idea of attaching some value to every point of a space at once, the value being allowed to differ from point to point. Attach a single number to each point and you have a scalar-field — a temperature reading at every location. Attach a"
      }
    },
    "vfs": {
      "prerequisiteReasons": {
        "system-call": "The stated objective requires System Call: the complete explanation applies it in this mechanism: The Virtual File System (VFS) is a layer inside the kernel that presents one uniform interface — the file system-calls open, read, write, close, lseek, all acting on a small integer handle — over many different underlying implementations. Behind that single interface can sit a",
        "file-descriptor": "The stated objective requires File Descriptor: the complete explanation applies it in this mechanism: Recall from system-call what a program actually has in hand after it opens a file. It does not hold the file itself; it holds a file-descriptor — a small per-process integer (the kernel hands back 3 for the first file a program opens, since 0, 1, 2 are taken by standard input,"
      }
    },
    "virtual-memory": {
      "prerequisiteReasons": {
        "process": "The stated objective requires Process: the complete explanation applies it in this mechanism: Virtual memory is the trick that gives every process its own private, imaginary view of memory. Each process sees a clean, contiguous range of memory addresses — numbered as if it started near zero and owned the whole machine — even though dozens of processes are sharing one",
        "memory-hierarchy": "The stated objective requires Memory Hierarchy: the complete explanation applies it in this mechanism: Virtual memory is the trick that gives every process its own private, imaginary view of memory. Each process sees a clean, contiguous range of memory addresses — numbered as if it started near zero and owned the whole machine — even though dozens of processes are sharing one"
      }
    },
    "vllm": {
      "prerequisiteReasons": {
        "paged-attention": "The stated objective requires Paged attention: the complete explanation applies it in this mechanism: vLLM is an LLM inference and serving engine built to maximize throughput — tokens per second across many concurrent requests. Its contribution is not one new trick but an engine loop that fuses two mechanisms which attack the two things that throttle a naive server:",
        "continuous-batching": "The stated objective requires Continuous Batching: the complete explanation applies it in this mechanism: vLLM is an LLM inference and serving engine built to maximize throughput — tokens per second across many concurrent requests. Its contribution is not one new trick but an engine loop that fuses two mechanisms which attack the two things that throttle a naive server:",
        "prefill-vs-decode": "The stated objective requires Prefill vs Decode: the complete explanation applies it in this mechanism: vLLM is an LLM inference and serving engine built to maximize throughput — tokens per second across many concurrent requests. Its contribution is not one new trick but an engine loop that fuses two mechanisms which attack the two things that throttle a naive server:",
        "kv-cache": "The stated objective requires KV-cache: the complete explanation applies it in this mechanism: vLLM is an LLM inference and serving engine built to maximize throughput — tokens per second across many concurrent requests. Its contribution is not one new trick but an engine loop that fuses two mechanisms which attack the two things that throttle a naive server:"
      }
    },
    "warp": {
      "prerequisiteReasons": {
        "streaming-multiprocessor": "The stated objective requires Streaming Multiprocessor: the complete explanation applies it in this mechanism: A warp is a fixed group of 32 threads that a streaming-multiprocessor runs in lockstep: each cycle the SM's scheduler issues one instruction, and all 32 threads execute that same instruction on the same cycle, each on its own data. The 32 positions a warp occupies are called its"
      }
    },
    "warp-shuffle": {
      "prerequisiteReasons": {
        "warp": "The stated objective requires Warp: the complete explanation applies it in this mechanism: A warp shuffle is a single hardware instruction (shflsync and its variants) that lets the 32 threads of a warp read each other's register values directly — one lane hands another lane the contents of one of its registers without that value ever touching memory. A register is the"
      }
    },
    "web-text-extraction": {
      "prerequisiteReasons": {
        "precision-recall": "The stated objective requires Precision and Recall: the complete explanation applies it in this mechanism: Measure extraction against labeled page regions with precision-recall. Here, precision is the fraction of emitted text regions judged useful for the intended corpus, while recall is the fraction of useful source regions that survive. Removing more aggressively may raise",
        "dataset-lineage": "The stated objective requires Dataset Lineage: the complete explanation applies it in this mechanism: Version the extractor, configuration, source snapshot, and output hashes with dataset-lineage so a result can be replayed. Build a stratified labeled sample across ordinary prose, documentation, forums, code, tables, mathematics, languages, and page templates. Report region- or"
      }
    },
    "weight-load-amortization": {
      "prerequisiteReasons": {
        "roofline-model": "The stated objective requires Roofline Model: the complete explanation applies it in this mechanism: Weight-load amortization is the reason batching rescues the slow, memory-starved generation step of a large language model. To compute one layer, the machine must first drag that layer's weight matrix up from the slow main memory (HBM, the high-bandwidth DRAM beside the chip)",
        "memory-hierarchy": "The stated objective requires Memory Hierarchy: the complete explanation applies it in this mechanism: The physical fact that drives everything: the cores cannot multiply by a weight they have not yet fetched, and the weights live in the slow main memory (HBM) — the bottom rung of the memory-hierarchy that the cores can actually address — not in the fast on-chip SRAM beside the",
        "matrix-multiplication": "The stated objective requires Matrix Multiplication: the complete explanation applies it in this mechanism: The central object is a single matrix-multiplication inside one layer of the model, written O = X · W. Here W is the layer's weight matrix — a fixed table of learned numbers, the same for every token the model ever processes. X holds the inputs: one row of X is one token's"
      }
    },
    "writeback": {
      "prerequisiteReasons": {
        "page-cache": "The stated objective requires Page Cache: the complete explanation applies it in this mechanism: Writeback is the deferred flushing of dirty pages from the page-cache out to persistent disk. Recall from page-cache that an ordinary write does not put your data on disk: the kernel copies the bytes into the cached RAM page, marks that page dirty (changed in RAM but not yet",
        "kernel-thread": "The stated objective requires Kernel Thread: the complete explanation applies it in this mechanism: Writeback is not done by your program and not done inside write. It is done by background kernel-threads — long-lived threads inside the kernel whose job is to scan the cache for dirty pages and push them to disk, asynchronously, while application code runs on undisturbed. The",
        "dma": "The stated objective requires Direct Memory Access: the complete explanation applies it in this mechanism: Writeback is not done by your program and not done inside write. It is done by background kernel-threads — long-lived threads inside the kernel whose job is to scan the cache for dirty pages and push them to disk, asynchronously, while application code runs on undisturbed. The",
        "interrupt": "The stated objective requires Interrupt: the complete explanation applies it in this mechanism: Writeback is not done by your program and not done inside write. It is done by background kernel-threads — long-lived threads inside the kernel whose job is to scan the cache for dirty pages and push them to disk, asynchronously, while application code runs on undisturbed. The"
      }
    },
    "zero-redundancy-optimizer": {
      "prerequisiteReasons": {
        "data-parallelism": "The stated objective requires Data parallelism: the complete explanation applies it in this mechanism: The Zero Redundancy Optimizer (ZeRO) removes the wasteful replication built into plain data-parallelism. Plain data parallelism keeps a full copy of three things on every worker: the model parameters (the trained numbers), the gradients (one adjustment-number per parameter,",
        "gather": "The stated objective requires Gather: the complete explanation applies it in this mechanism: The Zero Redundancy Optimizer (ZeRO) removes the wasteful replication built into plain data-parallelism. Plain data parallelism keeps a full copy of three things on every worker: the model parameters (the trained numbers), the gradients (one adjustment-number per parameter,"
      }
    }
  }
}
